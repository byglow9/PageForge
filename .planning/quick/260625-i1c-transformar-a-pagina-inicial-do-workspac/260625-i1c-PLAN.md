---
phase: quick-260625-i1c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/w/[slug]/SidebarNav.tsx
  - apps/web/src/app/w/[slug]/page.tsx
autonomous: true
requirements:
  - quick-i1c-dashboard
must_haves:
  truths:
    - "SidebarNav shows 'Dashboard' as the first item (top of the list)"
    - "Dashboard link only highlights when pathname === /w/${slug} exactly (not on /w/${slug}/templates or any sub-route)"
    - "All other nav items retain the existing prefix-match active logic"
    - "Dashboard page title reads 'Dashboard', not the workspace slug"
    - "No stale 'Phase 3', 'Phase 4' placeholder text remains on the page"
    - "Metric cards show real counts: Templates, Landing Pages, Members"
    - "Shortcut cards for Templates and Brand Settings are hidden when the role lacks permission"
  artifacts:
    - path: "apps/web/src/app/w/[slug]/SidebarNav.tsx"
      provides: "NavItem with exact flag; Dashboard item at index 0; conditional active logic"
    - path: "apps/web/src/app/w/[slug]/page.tsx"
      provides: "Async RSC with Prisma count queries; metric cards; role-gated shortcut cards"
  key_links:
    - from: "SidebarNav NavItem.exact"
      to: "active check"
      via: "item.exact ? pathname === href : pathname === href || pathname.startsWith(`${href}/`)"
      pattern: "exact.*pathname.*startsWith"
    - from: "page.tsx prisma counts"
      to: "metric cards"
      via: "Promise.all([prisma.template.count, prisma.landingPage.count, prisma.member.count])"
      pattern: "prisma\\.template\\.count|prisma\\.landingPage\\.count|prisma\\.member\\.count"
---

<objective>
Transform /w/[slug] into a real dashboard: fix stale content, add a Dashboard nav item with exact active-match, and populate metric cards with live Prisma counts + role-gated shortcut links.

Purpose: Users land on a page that still says "Coming in Phase 3/4". This replaces placeholders with working content.
Output: Two modified files — SidebarNav.tsx (Dashboard item + exact flag) and page.tsx (real data, proper UI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Key patterns discovered from codebase read:
- `requireWorkspace(slug)` returns `WorkspaceContext { workspaceId, workspaceSlug, userId, role }` — already called in page.tsx.
- `can(role, resource, action)` is the permission gate — used identically in layout.tsx: `can(ctx.role, "template", "create")` and `can(ctx.role, "brand", "update")`.
- For members count: `prisma.member.count({ where: { organizationId: ctx.workspaceId } })` — authoritative better-auth table, direct prisma (per pattern in members/page.tsx line 72).
- For template/lp counts: `prisma.template.count({ where: { workspaceId: ctx.workspaceId } })` and `prisma.landingPage.count({ where: { workspaceId: ctx.workspaceId } })` — app-level isolation via workspaceId filter (ctx is session-validated; RLS is backstop). `withTenantDb` does not expose a `.count()` helper, so direct prisma with workspaceId filter is the correct approach for this readonly operation.
- UI components available: `Card, CardHeader, CardTitle, CardContent` from `@/components/ui/card`; `Badge` from `@/components/ui/badge`; `cn` from `@/lib/utils`.
- Icons in SidebarNav: imported from `lucide-react` as named imports. `LayoutDashboard` is the correct icon name for Dashboard.
</context>

<interfaces>
<!-- From apps/web/src/app/w/[slug]/SidebarNav.tsx — current NavItem interface -->
interface NavItem {
  href: string;
  label: string;
  icon: LucideIcon;
  // ADD: exact?: boolean
}

<!-- Active check (current, line 57) -->
const active = pathname === href || pathname.startsWith(`${href}/`);
// REPLACE WITH:
const active = item.exact ? pathname === href : (pathname === href || pathname.startsWith(`${href}/`));

<!-- From apps/web/src/lib/workspaces/guards.ts -->
export interface WorkspaceContext {
  workspaceId: string;
  workspaceSlug: string;
  userId: string;
  role: Role;
}
export function can(role: Role, resource: string, action: string): boolean;

<!-- From apps/web/src/app/w/[slug]/members/page.tsx — direct prisma pattern for authoritative tables -->
const members = await prisma.member.findMany({
  where: { organizationId: ctx.workspaceId },
  ...
});
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SidebarNav — add Dashboard item with exact-match active flag</name>
  <files>apps/web/src/app/w/[slug]/SidebarNav.tsx</files>
  <action>
    1. Add `exact?: boolean` to the `NavItem` interface.

    2. Add `LayoutDashboard` to the lucide-react import (alongside FileText, LayoutTemplate, Palette, Users).

    3. At the TOP of the `items` array (before the `canAuthorTemplates` spread), insert:
       `{ href: `/w/${slug}`, label: "Dashboard", icon: LayoutDashboard, exact: true }`

    4. In the `.map()` loop, rename the destructured variable to `item` (or keep as-is but reference `item.exact`). Update the `active` constant:
       `const active = item.exact ? pathname === item.href : (pathname === item.href || pathname.startsWith(`${item.href}/`));`

       The existing `({ href, label, icon: Icon })` destructuring must be updated to also destructure `exact` from the item: `({ href, label, icon: Icon, exact })`. Then: `const active = exact ? pathname === href : (pathname === href || pathname.startsWith(`${href}/`));`

    This preserves the existing prefix-match behavior for all other nav items (Templates, Landing Pages, Brand Settings, Members) while ensuring the Dashboard link only highlights on `/w/${slug}` exactly.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && npx tsc --project apps/web/tsconfig.json --noEmit 2>&1 | grep -i "SidebarNav\|error" | head -20</automated>
  </verify>
  <done>
    - `NavItem` has `exact?: boolean`.
    - First item in list is Dashboard with href `/w/${slug}` and `exact: true`.
    - Active check uses exact flag: Dashboard highlights only on exact match; other items use prefix match.
    - `LayoutDashboard` imported from `lucide-react`.
    - TypeScript compiles without errors in this file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page.tsx — real counts, metric cards, shortcut cards</name>
  <files>apps/web/src/app/w/[slug]/page.tsx</files>
  <action>
    Replace the entire file content. Keep the same async Server Component pattern with `requireWorkspace`. Make the following changes:

    **Imports to add:**
    - `import { prisma } from "@/lib/db/prisma";`
    - `import { can } from "@/lib/workspaces/guards";` (already imports requireWorkspace, add can)
    - `import Link from "next/link";`
    - `import { LayoutTemplate, FileText, Palette, Users } from "lucide-react";`
    - `import type { LucideIcon } from "lucide-react";`
    - `import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";`

    **Data fetching (after `const ctx = await requireWorkspace(slug)`):**
    Fetch all three counts in parallel:
    ```
    const [templateCount, lpCount, memberCount] = await Promise.all([
      prisma.template.count({ where: { workspaceId: ctx.workspaceId } }),
      prisma.landingPage.count({ where: { workspaceId: ctx.workspaceId } }),
      prisma.member.count({ where: { organizationId: ctx.workspaceId } }),
    ]);
    ```
    Note: `prisma.member.count` uses `organizationId` (not `workspaceId`) because the `member` model is the better-auth authoritative table — same pattern as members/page.tsx line 72.

    **Permission gates (matching layout.tsx exactly):**
    ```
    const canAuthorTemplates = can(ctx.role, "template", "create");
    const canEditBrand = can(ctx.role, "brand", "update");
    ```

    **JSX structure:**
    ```
    <div className="px-8 py-6 space-y-8">
      {/* Page header */}
      <h1 className="text-2xl font-semibold text-gray-900">Dashboard</h1>

      {/* Metric cards row */}
      <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
        <Card>
          <CardHeader className="pb-2">
            <CardTitle className="text-sm font-medium text-muted-foreground">Templates</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-3xl font-bold text-gray-900">{templateCount}</p>
          </CardContent>
        </Card>
        <Card>
          <CardHeader className="pb-2">
            <CardTitle className="text-sm font-medium text-muted-foreground">Landing Pages</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-3xl font-bold text-gray-900">{lpCount}</p>
          </CardContent>
        </Card>
        <Card>
          <CardHeader className="pb-2">
            <CardTitle className="text-sm font-medium text-muted-foreground">Members</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-3xl font-bold text-gray-900">{memberCount}</p>
          </CardContent>
        </Card>
      </div>

      {/* Role card */}
      <Card className="max-w-xs">
        <CardHeader className="pb-2">
          <CardTitle className="text-sm font-medium text-muted-foreground">Your role</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-xl font-bold capitalize text-gray-900">{ctx.role}</p>
        </CardContent>
      </Card>

      {/* Shortcut links */}
      <div>
        <h2 className="text-sm font-semibold text-gray-500 uppercase tracking-wide mb-3">Quick access</h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3">
          {/* Landing Pages — always visible */}
          <Link href={`/w/${slug}/lps`} className="flex items-center gap-3 rounded-lg border border-gray-200 bg-white p-4 hover:bg-gray-50 transition-colors">
            <FileText className="h-5 w-5 text-gray-500 shrink-0" aria-hidden="true" />
            <span className="text-sm font-medium text-gray-900">Landing Pages</span>
          </Link>

          {/* Members — always visible */}
          <Link href={`/w/${slug}/members`} className="flex items-center gap-3 rounded-lg border border-gray-200 bg-white p-4 hover:bg-gray-50 transition-colors">
            <Users className="h-5 w-5 text-gray-500 shrink-0" aria-hidden="true" />
            <span className="text-sm font-medium text-gray-900">Members</span>
          </Link>

          {/* Templates — canAuthorTemplates only */}
          {canAuthorTemplates && (
            <Link href={`/w/${slug}/templates`} className="flex items-center gap-3 rounded-lg border border-gray-200 bg-white p-4 hover:bg-gray-50 transition-colors">
              <LayoutTemplate className="h-5 w-5 text-gray-500 shrink-0" aria-hidden="true" />
              <span className="text-sm font-medium text-gray-900">Templates</span>
            </Link>
          )}

          {/* Brand Settings — canEditBrand only */}
          {canEditBrand && (
            <Link href={`/w/${slug}/brand`} className="flex items-center gap-3 rounded-lg border border-gray-200 bg-white p-4 hover:bg-gray-50 transition-colors">
              <Palette className="h-5 w-5 text-gray-500 shrink-0" aria-hidden="true" />
              <span className="text-sm font-medium text-gray-900">Brand Settings</span>
            </Link>
          )}
        </div>
      </div>
    </div>
    ```

    Remove ALL inline style attributes from the old implementation — use Tailwind classes only.
    No DB migrations. No new Prisma models. Only read queries.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && npx tsc --project apps/web/tsconfig.json --noEmit 2>&1 | grep -i "page\|error" | head -20</automated>
  </verify>
  <done>
    - h1 reads "Dashboard".
    - No "Phase 3", "Phase 4", or slug-as-title text in the file.
    - Three metric Card components rendering templateCount, lpCount, memberCount (from real Prisma counts).
    - Role card shows ctx.role.
    - Quick access section with 2 always-visible links (LPs, Members) and 2 conditional links (Templates if canAuthorTemplates, Brand if canEditBrand).
    - No inline style attributes remain.
    - TypeScript compiles without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL slug → workspace | Slug from URL is resolved through requireWorkspace() which validates session membership — never trusted directly |
| Prisma count queries | workspaceId comes from requireWorkspace() (session-validated), not from URL or client input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-i1c-01 | Information Disclosure | prisma.template.count | mitigate | workspaceId filter on count query (app-level isolation); ctx.workspaceId sourced from requireWorkspace() session check |
| T-i1c-02 | Information Disclosure | prisma.member.count | accept | organizationId filter; member table is already auth-layer data, same pattern as members/page.tsx |
| T-i1c-03 | Spoofing | Dashboard link active state | accept | Exact-match flag is UI-only; no auth implications; server guards unchanged |
</threat_model>

<verification>
After both tasks complete, verify:
1. Navigate to /w/{slug} — page shows "Dashboard" as h1 with metric cards showing real numbers.
2. Navigate to /w/{slug}/templates — Dashboard nav item is NOT highlighted; Templates item IS highlighted.
3. Navigate back to /w/{slug} — Dashboard nav item IS highlighted.
4. As a viewer role: Templates shortcut and Brand Settings shortcut do not appear in Quick access.
5. TypeScript build passes: `npx tsc --project apps/web/tsconfig.json --noEmit` exits 0.
</verification>

<success_criteria>
- SidebarNav: Dashboard item first in list, exact-only highlight, LayoutDashboard icon.
- page.tsx: h1 = "Dashboard", 3 Prisma count metrics, role card, role-gated shortcut links, no stale placeholder text, no inline style attributes.
- Zero TypeScript errors introduced.
</success_criteria>

<output>
After completion, create `.planning/quick/260625-i1c-transformar-a-pagina-inicial-do-workspac/260625-i1c-SUMMARY.md`
</output>
