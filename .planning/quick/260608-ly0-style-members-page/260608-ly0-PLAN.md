---
phase: 260608-ly0-style-members-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/w/[slug]/members/page.tsx
autonomous: true
requirements:
  - STYLE-MEMBERS-01
must_haves:
  truths:
    - "Page header shows title 'Members' with workspace slug and current role in a Badge"
    - "Invite section is wrapped in a Card, visible only when canManage is true"
    - "Invite URL (when present) is shown in an Alert block with the URL in <code>"
    - "Pending invitations section is a Card with a styled table (muted thead, divide-y rows)"
    - "Current members section is a Card with a styled table; Actions column has inline-flex forms with styled native selects, Button for change role, and destructive Button for Remove"
    - "States '(you)' and '(owner)' rendered as Badge variant=outline"
    - "All server actions, Prisma queries, hidden inputs, name/value attributes, and async Server Component structure are untouched"
  artifacts:
    - path: apps/web/src/app/w/[slug]/members/page.tsx
      provides: "Styled members page — presentational markup only"
  key_links:
    - from: "apps/web/src/app/w/[slug]/members/page.tsx"
      to: "apps/web/src/components/ui/card.tsx"
      via: "Card / CardHeader / CardTitle / CardContent imports"
      pattern: "from.*components/ui/card"
    - from: "apps/web/src/app/w/[slug]/members/page.tsx"
      to: "apps/web/src/components/ui/badge.tsx"
      via: "Badge import"
      pattern: "from.*components/ui/badge"
---

<objective>
Restyle apps/web/src/app/w/[slug]/members/page.tsx from raw unstyled markup to the
PageForge design system.

Purpose: The members page currently has no CSS classes. Adding Tailwind utility classes and
shadcn/ui component wrappers (Card, Badge, Button, Input, Label, Alert) makes it visually
consistent with the rest of the dashboard — with zero changes to server logic.

Output: A single updated file. All server actions, Prisma queries, guards, redirects,
input names/values, and the async Server Component declaration are byte-for-byte identical.
Only presentation (JSX structure + class names) changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Reference page for layout/spacing conventions (already styled):
@apps/web/src/app/w/[slug]/brand/page.tsx
</context>

<interfaces>
<!-- Confirmed component APIs — use exactly these. Do NOT re-read component files. -->

Badge variants: default | secondary | destructive | outline | ghost | link
  Usage: <Badge variant="outline">{ctx.role}</Badge>
  (renders as <span> via useRender — works in Server Components)

Button variants: default | outline | secondary | ghost | destructive | link
Button sizes:   default (h-8) | xs | sm | lg | icon | icon-xs | icon-sm | icon-lg
  Usage: <Button type="submit" size="sm">Generate invite link</Button>
  Usage: <Button type="submit" variant="destructive" size="sm">Remove</Button>
  (ButtonPrimitive from @base-ui/react/button — works in Server Components as a plain button)

Card sub-components: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, CardAction

Input: renders an <input> via @base-ui/react/input — pass all standard HTML input props.
  (Works in Server Components; it's a thin wrapper with no client state.)

Label: has "use client" at the top of its file — CANNOT be imported in a Server Component
  directly. Use a plain <label> element with Tailwind classes instead:
  className="text-sm font-medium leading-none"

Alert variants: default | destructive
  Sub-components: Alert, AlertTitle, AlertDescription, AlertAction
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restyle members/page.tsx with design system components</name>
  <files>apps/web/src/app/w/[slug]/members/page.tsx</files>
  <action>
Rewrite the JSX return of MembersPage, preserving ALL logic above the return statement
verbatim (server actions, Prisma queries, userMap, canManage). Only the returned JSX
and the import block change.

IMPORT ADDITIONS (add alongside the existing imports — do NOT remove any existing import):
  import { Badge } from "@/components/ui/badge";
  import { Button } from "@/components/ui/button";
  import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";
  import { Input } from "@/components/ui/input";
  import { Alert, AlertTitle, AlertDescription } from "@/components/ui/alert";

Do NOT import Label (it is "use client" — use plain <label> with Tailwind classes).

--- PAGE SHELL ---
Outer wrapper: <div className="px-8 py-6 space-y-8">

--- HEADER SECTION ---
Replace the plain <div>/<h1>/<p> block with:
  <div className="space-y-1">
    <h1 className="text-2xl font-semibold">Members</h1>
    <p className="text-sm text-muted-foreground">
      Workspace: <span className="font-medium text-foreground">{slug}</span>
      {" — "}Your role:{" "}
      <Badge variant="secondary">{ctx.role}</Badge>
    </p>
  </div>

--- INVITE SECTION (canManage guard stays exactly as-is) ---
Wrap section content in a Card:
  <Card>
    <CardHeader>
      <CardTitle>Invite a member</CardTitle>
      <CardDescription>
        Enter an email address and select a role to generate a copyable invite link.
        No email is sent automatically.
      </CardDescription>
    </CardHeader>
    <CardContent className="space-y-4">
      {inviteUrl && (
        <Alert>
          <AlertTitle>Invite link generated</AlertTitle>
          <AlertDescription>
            Share the link below. It expires after use.{" "}
            <code className="rounded bg-muted px-1 py-0.5 font-mono text-xs break-all">
              {inviteUrl}
            </code>
          </AlertDescription>
        </Alert>
      )}
      <form action={inviteAction} className="space-y-4">
        <div className="space-y-1.5">
          <label htmlFor="invite-email" className="text-sm font-medium leading-none">
            Email address
          </label>
          <Input
            id="invite-email"
            name="email"
            type="email"
            placeholder="colleague@example.com"
            required
          />
        </div>
        <div className="space-y-1.5">
          <label htmlFor="invite-role" className="text-sm font-medium leading-none">
            Role
          </label>
          <select
            id="invite-role"
            name="role"
            defaultValue="editor"
            className="h-8 w-full rounded-lg border border-input bg-transparent px-2.5 py-1 text-sm outline-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50"
          >
            <option value="admin">Admin — manages members and settings</option>
            <option value="editor">Editor — creates and edits content</option>
            <option value="viewer">Viewer — read and export only</option>
          </select>
        </div>
        <Button type="submit" size="sm">Generate invite link</Button>
      </form>
    </CardContent>
  </Card>

--- PENDING INVITATIONS SECTION (canManage && invitations.length > 0 guard stays) ---
Wrap in a Card with a styled native table:
  <Card>
    <CardHeader>
      <CardTitle>Pending invitations</CardTitle>
    </CardHeader>
    <CardContent className="p-0">
      <table className="w-full text-sm">
        <thead className="bg-muted/50">
          <tr>
            <th className="px-4 py-2 text-left font-medium text-muted-foreground">Email</th>
            <th className="px-4 py-2 text-left font-medium text-muted-foreground">Role</th>
            <th className="px-4 py-2 text-left font-medium text-muted-foreground">Expires</th>
            <th className="px-4 py-2 text-left font-medium text-muted-foreground">Invite link</th>
          </tr>
        </thead>
        <tbody className="divide-y divide-border">
          {invitations.map((inv) => {
            const inviteUrl = invitationLinks.getInvitationUrl(inv.id);
            return (
              <tr key={inv.id} className="hover:bg-muted/30 transition-colors">
                <td className="px-4 py-2">{inv.email}</td>
                <td className="px-4 py-2">
                  <Badge variant="outline">{inv.role}</Badge>
                </td>
                <td className="px-4 py-2 text-muted-foreground">
                  {inv.expiresAt.toLocaleDateString("en-US", {
                    year: "numeric",
                    month: "short",
                    day: "numeric",
                  })}
                </td>
                <td className="px-4 py-2">
                  <code className="rounded bg-muted px-1 py-0.5 font-mono text-xs break-all">
                    {inviteUrl}
                  </code>
                </td>
              </tr>
            );
          })}
        </tbody>
      </table>
    </CardContent>
  </Card>

--- CURRENT MEMBERS SECTION ---
Wrap in a Card with a styled native table. The Actions column exists only when canManage.
Remove the inline style={{ display: "inline" }} and style={{ marginLeft: "0.5rem" }} —
replace with Tailwind flex layout on the td:

  <Card>
    <CardHeader>
      <CardTitle>Current members</CardTitle>
    </CardHeader>
    <CardContent className="p-0">
      <table className="w-full text-sm">
        <thead className="bg-muted/50">
          <tr>
            <th className="px-4 py-2 text-left font-medium text-muted-foreground">Name</th>
            <th className="px-4 py-2 text-left font-medium text-muted-foreground">Email</th>
            <th className="px-4 py-2 text-left font-medium text-muted-foreground">Role</th>
            {canManage && (
              <th className="px-4 py-2 text-left font-medium text-muted-foreground">Actions</th>
            )}
          </tr>
        </thead>
        <tbody className="divide-y divide-border">
          {members.map((member) => {
            const user = userMap.get(member.userId);
            const isCurrentUser = member.userId === ctx.userId;
            return (
              <tr key={member.id} className="hover:bg-muted/30 transition-colors">
                <td className="px-4 py-2 font-medium">{user?.name ?? "(unknown)"}</td>
                <td className="px-4 py-2 text-muted-foreground">{user?.email ?? "(unknown)"}</td>
                <td className="px-4 py-2">
                  <Badge variant="outline">{member.role}</Badge>
                </td>
                {canManage && (
                  <td className="px-4 py-2">
                    {!isCurrentUser && member.role !== "owner" && (
                      <div className="flex items-center gap-2">
                        <form action={changeRoleAction} className="flex items-center gap-2">
                          <input type="hidden" name="memberId" value={member.id} />
                          <select
                            name="role"
                            defaultValue={member.role}
                            className="h-7 rounded-md border border-input bg-transparent px-2 text-sm outline-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50"
                          >
                            <option value="admin">Admin</option>
                            <option value="editor">Editor</option>
                            <option value="viewer">Viewer</option>
                          </select>
                          <Button type="submit" size="xs" variant="outline">
                            Change role
                          </Button>
                        </form>
                        <form action={removeAction}>
                          <input type="hidden" name="memberId" value={member.id} />
                          <Button type="submit" size="xs" variant="destructive">
                            Remove
                          </Button>
                        </form>
                      </div>
                    )}
                    {isCurrentUser && (
                      <Badge variant="secondary">(you)</Badge>
                    )}
                    {member.role === "owner" && !isCurrentUser && (
                      <Badge variant="outline">(owner)</Badge>
                    )}
                  </td>
                )}
              </tr>
            );
          })}
        </tbody>
      </table>
    </CardContent>
  </Card>

INVARIANTS — verify before saving:
- "use server" appears inside inviteAction, changeRoleAction, and removeAction (3 occurrences)
- All three redirect() calls are present (inviteAction has two; changeRoleAction and removeAction have one each)
- encodeURIComponent(result.data.inviteUrl) is present in inviteAction
- input name="email", name="role", name="memberId" attributes are unchanged
- option value="admin", value="editor", value="viewer" are present in both selects
- No "use client" directive at the top of the file
- The file is still an async function component (export default async function MembersPage)
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web build 2>&1 | tail -20</automated>
  </verify>
  <done>
    pnpm --filter web build exits 0. The members page renders with Card-wrapped sections,
    Badge-labeled role indicators, styled native selects and tables, Alert for invite URLs,
    and destructive/outline Button variants — with all server logic untouched.
  </done>
</task>

</tasks>

<verification>
After the task completes:
1. `pnpm --filter web build` exits 0 (TypeScript + Next.js compilation clean)
2. Grep confirms "use server" appears 3 times in members/page.tsx
3. Grep confirms no "use client" directive in members/page.tsx
4. Grep confirms `encodeURIComponent` is still present
</verification>

<success_criteria>
- Build passes with zero type errors
- All server actions preserved verbatim
- Page visually matches the design system: Cards, Badges, styled tables, Input, Alert
- No new dependencies added
</success_criteria>

<output>
After completion, create `.planning/quick/260608-ly0-style-members-page/260608-ly0-01-SUMMARY.md`
</output>
