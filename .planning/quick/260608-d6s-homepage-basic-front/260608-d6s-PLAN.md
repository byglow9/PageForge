---
phase: quick/260608-d6s-homepage-basic-front
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/page.tsx
autonomous: true
requirements:
  - QUICK-260608-d6s

must_haves:
  truths:
    - "Homepage shows 'PageForge' as the main heading"
    - "A tagline is visible below the heading"
    - "Two CTA buttons ('Log in' and 'Sign up') are present and navigate to /login and /signup"
    - "Hero is centered vertically and horizontally on the screen"
    - "Page is visually consistent with the existing design tokens (no raw colors, no inline styles)"
  artifacts:
    - path: "apps/web/src/app/page.tsx"
      provides: "Styled hero homepage"
      contains: "buttonVariants"
  key_links:
    - from: "apps/web/src/app/page.tsx"
      to: "/login"
      via: "Next.js Link with buttonVariants class"
      pattern: "href=\"/login\""
    - from: "apps/web/src/app/page.tsx"
      to: "/signup"
      via: "Next.js Link with buttonVariants class"
      pattern: "href=\"/signup\""
---

<objective>
Replace the bare, unstyled homepage with a centered hero section that matches the project's design system.

Purpose: The homepage is the first screen a visitor sees; currently it renders raw HTML with no styling. This plan makes it presentable using existing Tailwind 4 tokens and the project's button variants — no new dependencies.
Output: A rewritten `apps/web/src/app/page.tsx` with a full-screen centered hero, product name, tagline, and two styled CTA links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/app/page.tsx
@apps/web/src/components/ui/button.tsx
@apps/web/src/app/globals.css
</context>

<interfaces>
<!-- Extracted from apps/web/src/components/ui/button.tsx -->
<!-- Button uses @base-ui/react/button as its primitive — NO asChild prop. -->
<!-- To use button styling on a Next.js Link (anchor), import buttonVariants directly
     and apply as className. Do NOT wrap Link inside Button. -->

```typescript
// Named export from apps/web/src/components/ui/button.tsx
export { Button, buttonVariants }

// buttonVariants accepts: variant ("default" | "outline" | "secondary" | "ghost" | "destructive" | "link")
//                         size ("default" | "xs" | "sm" | "lg" | "icon" | "icon-xs" | "icon-sm" | "icon-lg")
// Usage on an anchor/Link:
//   import { buttonVariants } from "@/components/ui/button"
//   <Link href="/login" className={buttonVariants({ variant: "default", size: "lg" })}>Log in</Link>
```

Available Tailwind 4 theme tokens (from globals.css @theme):
- bg-background, text-foreground
- text-muted-foreground
- bg-primary, text-primary-foreground
- border-border
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite homepage with styled hero</name>
  <files>apps/web/src/app/page.tsx</files>
  <action>
    Rewrite `apps/web/src/app/page.tsx` to render a full-screen centered hero. Specific implementation notes:

    - Import: `Link` from "next/link", `buttonVariants` from "@/components/ui/button".
    - Remove the `Button` component import — do not use it here. The Button primitive is @base-ui/react/button which has no asChild; use `buttonVariants` on the Link directly.
    - Outer wrapper: `<main className="flex min-h-screen flex-col items-center justify-center bg-background">`.
    - Inner container: `<div className="flex flex-col items-center gap-6 text-center px-4">` (max-w-md or similar to keep text readable on wide screens, e.g., `max-w-sm`).
    - Heading: `<h1 className="text-4xl font-bold tracking-tight text-foreground">PageForge</h1>`.
    - Tagline `<p>`: `className="text-muted-foreground text-base"`. Text: "Generate landing pages from templates — without touching code." (or equivalent short product description).
    - CTA row: `<div className="flex gap-3 flex-wrap justify-center">`.
    - "Sign up" link (primary action, variant default, size lg): `<Link href="/signup" className={buttonVariants({ variant: "default", size: "lg" })}>Sign up</Link>`.
    - "Log in" link (secondary action, variant outline, size lg): `<Link href="/login" className={buttonVariants({ variant: "outline", size: "lg" })}>Log in</Link>`.
    - No inline styles. No new imports beyond what is listed above. No "use client" directive (this is a Server Component).
    - Remove the nav element and the pipe separator from the original file entirely.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Build succeeds with no TypeScript or import errors. The page renders a centered hero with heading "PageForge", a tagline paragraph, a primary "Sign up" button-link, and an outline "Log in" button-link. No raw unstyled HTML remains.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This is a static Server Component with no user input, no data fetching, and no auth. No trust boundaries cross this file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-d6s-01 | Information Disclosure | page.tsx (public) | accept | Page is intentionally public; no sensitive data rendered. |
</threat_model>

<verification>
1. Run `pnpm --filter web build` — must complete with exit code 0.
2. Run `pnpm --filter web dev` and open http://localhost:3000 — hero must be centered, heading and tagline visible, two styled buttons present.
3. Click "Log in" — must navigate to /login. Click "Sign up" — must navigate to /signup.
</verification>

<success_criteria>
- `apps/web/src/app/page.tsx` compiles without errors.
- Hero is vertically and horizontally centered on a full-height viewport.
- "PageForge" heading, tagline, "Sign up" (default variant), and "Log in" (outline variant) are all rendered.
- No inline styles, no hardcoded color values, no new dependencies.
- Visual appearance is consistent with the auth pages' token usage.
</success_criteria>

<output>
After completion, create `.planning/quick/260608-d6s-homepage-basic-front/260608-d6s-01-SUMMARY.md` using the summary template.
</output>
