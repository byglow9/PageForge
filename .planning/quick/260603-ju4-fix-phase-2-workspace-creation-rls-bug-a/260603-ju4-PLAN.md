---
phase: quick
plan: 260603-ju4
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/workspaces/actions.ts
  - apps/web/src/lib/auth/auth.ts
  - apps/web/tests/workspaces.test.ts
  - apps/web/tests/auth.test.ts
  - apps/web/.gitignore
autonomous: true
requirements:
  - D-02
  - D-04
  - D-13
  - D-14
  - T-02-02-04

must_haves:
  truths:
    - "Creating a workspace via createWorkspaceAction succeeds end-to-end (rows land in workspace and workspace_member tables)"
    - "After signup, a row exists in the verification table and a verification URL is logged by the console transport"
    - "git status is clean of pnpm-workspace.yaml, pnpm-lock.yaml, auth.ts in-flight changes after Task C commits"
    - "next-env.d.ts is gitignored and not tracked"
    - "All 172 existing tests remain green"
  artifacts:
    - path: "apps/web/src/lib/workspaces/actions.ts"
      provides: "createWorkspaceAction with set_config call before workspace/workspace_member inserts"
      contains: "set_config('app.current_workspace_id'"
    - path: "apps/web/src/lib/auth/auth.ts"
      provides: "emailVerification block with sendOnSignUp: true"
      contains: "sendOnSignUp: true"
    - path: "apps/web/.gitignore"
      provides: "next-env.d.ts entry"
      contains: "next-env.d.ts"
    - path: "apps/web/tests/workspaces.test.ts"
      provides: "test verifying set_config is present in createWorkspaceAction source"
  key_links:
    - from: "apps/web/src/lib/workspaces/actions.ts"
      to: "PostgreSQL RLS policy tenant_isolation on workspace and workspace_member"
      via: "tx.$executeRaw set_config before inserts"
      pattern: "set_config\\('app.current_workspace_id'"
    - from: "apps/web/src/lib/auth/auth.ts"
      to: "verification table row"
      via: "better-auth sendOnSignUp hook calling sendVerificationEmail"
      pattern: "sendOnSignUp"
---

<objective>
Fix two Phase-2 UAT blockers and commit in-flight dependency/config changes.

Purpose: Workspace creation is blocked by RLS because the transaction never sets
app.current_workspace_id before inserting into the workspace and workspace_member
tables. Email verification tokens are never generated at signup because sendOnSignUp
is absent from the emailVerification config. Both bugs prevent the core Phase-2 flow
from working end-to-end.

Output:
- createWorkspaceAction sets the RLS workspace context inside its transaction
- emailVerification fires at signup via sendOnSignUp: true
- kysely override, trustedOrigins, tsconfig.json, and gitignore hygiene committed cleanly
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/src/lib/workspaces/actions.ts
@apps/web/src/lib/db/tenant-db.ts
@apps/web/src/lib/auth/auth.ts
@apps/web/prisma/migrations/0002_rls_real_tenant_tables/migration.sql

<interfaces>
<!-- From apps/web/src/lib/db/tenant-db.ts — the set_config pattern to reuse: -->

```typescript
// Inside prisma.$transaction callback (tx is the transaction client):
await tx.$executeRaw`SELECT set_config('app.current_workspace_id', ${workspaceId}, true)`;
// - Third argument `true` = transaction-local (resets when transaction ends)
// - Parameter substitution via tagged template — NOT string interpolation
// - Must be called BEFORE any INSERT on workspace or workspace_member tables
```

<!-- From 0002_rls_real_tenant_tables/migration.sql — the RLS WITH CHECK clauses that fail: -->

```sql
-- workspace table:
WITH CHECK ("id" = current_setting('app.current_workspace_id', true)::text)
-- workspace_member table:
WITH CHECK ("workspaceId" = current_setting('app.current_workspace_id', true)::text)
-- Fix: set_config must set workspaceId BEFORE these inserts inside the tx
```

<!-- From apps/web/src/lib/workspaces/actions.ts — transaction structure to modify: -->

```typescript
const workspaceId = randomUUID(); // already generated before the tx
await prisma.$transaction(async (tx) => {
  // ADD set_config HERE — before tx.workspace.create and tx.workspaceMember.create
  await tx.workspace.create({ data: { id: workspaceId, name, slug } });
  await tx.organization.create({ data: { id: workspaceId, name, slug } }); // no RLS
  await tx.member.create({ ... });              // no RLS
  await tx.workspaceMember.create({ data: { workspaceId, userId: user.id, role: "owner" } });
});
```

<!-- From apps/web/src/lib/auth/auth.ts — emailVerification block to extend: -->

```typescript
emailVerification: {
  sendVerificationEmail: async ({ user, url }) => { ... },
  sendOnSignIn: true,      // already present (uncommitted)
  expiresIn: 60 * 60 * 24,
  // ADD: sendOnSignUp: true
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Fix RLS block in createWorkspaceAction — set workspace context before inserts</name>
  <files>
    apps/web/src/lib/workspaces/actions.ts
    apps/web/tests/workspaces.test.ts
  </files>
  <read_first>
    apps/web/src/lib/workspaces/actions.ts (lines 93-128 — the $transaction block)
    apps/web/src/lib/db/tenant-db.ts (lines 96-100 — the set_config pattern)
    apps/web/tests/workspaces.test.ts (lines 303-327 — existing source-level assertion tests to extend)
  </read_first>
  <action>
    In apps/web/src/lib/workspaces/actions.ts, inside the `prisma.$transaction(async (tx) => { ... })` block
    in `createWorkspaceAction` (around line 96), add the following as the FIRST statement inside the callback,
    before `tx.workspace.create`:

      await tx.$executeRaw`SELECT set_config('app.current_workspace_id', ${workspaceId}, true)`;

    This matches exactly the pattern in tenant-db.ts line 100. Use the tagged-template form — do NOT
    string-interpolate workspaceId into SQL. The third argument `true` makes the setting transaction-local.

    The `workspaceId` variable is already declared with `randomUUID()` before the transaction starts
    (line 93 of actions.ts), so it is in scope. The setting must be applied before `tx.workspace.create`
    (RLS on workspace table checks `id = current_setting(...)`) and before `tx.workspaceMember.create`
    (RLS on workspace_member checks `workspaceId = current_setting(...)`). The `tx.organization.create`
    and `tx.member.create` calls have no RLS, so their order relative to set_config does not matter
    functionally, but keep them after set_config for consistency.

    In apps/web/tests/workspaces.test.ts, add a new describe block after the existing
    "changeMemberRoleAction — prevents promoting to owner" block. The test reads the source of actions.ts
    and asserts it contains the set_config call — matching the pattern of the existing source-level assertion
    tests (lines 303-327). The assertion: the source must contain
    `set_config('app.current_workspace_id'` and the call must appear before `tx.workspace.create`
    within the transaction.

    Example test block (adapt as needed — the exact string content is load-bearing):

      describe("createWorkspaceAction — RLS context fix (D-13, T-02-02-04)", () => {
        it("sets app.current_workspace_id before workspace insert (RLS backstop)", async () => {
          const fs = await import("fs");
          const path = await import("path");
          const source = fs.readFileSync(
            path.join(__dirname, "../src/lib/workspaces/actions.ts"),
            "utf-8"
          );
          expect(source).toContain("set_config('app.current_workspace_id'");
          // Verify set_config appears before workspace.create in the source
          const setConfigPos = source.indexOf("set_config('app.current_workspace_id'");
          const workspaceCreatePos = source.indexOf("tx.workspace.create");
          expect(setConfigPos).toBeGreaterThan(-1);
          expect(workspaceCreatePos).toBeGreaterThan(-1);
          expect(setConfigPos).toBeLessThan(workspaceCreatePos);
        });
      });
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge/apps/web && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests pass (172+ with the new test). The source of actions.ts contains
    `set_config('app.current_workspace_id'` and the call appears before `tx.workspace.create`
    in the transaction block. Live proof: starting the dev server and submitting the
    /workspaces/new form succeeds without "Failed to create workspace" error.
  </done>
</task>

<task type="auto">
  <name>Task B: Fix email verification token not generated at signup — add sendOnSignUp: true</name>
  <files>
    apps/web/src/lib/auth/auth.ts
    apps/web/tests/auth.test.ts
  </files>
  <read_first>
    apps/web/src/lib/auth/auth.ts (lines 46-59 — the emailVerification block)
    apps/web/tests/auth.test.ts (lines 87-103 — existing auth config tests to extend)
  </read_first>
  <action>
    In apps/web/src/lib/auth/auth.ts, inside the `emailVerification` object (lines 47-59),
    add `sendOnSignUp: true` as a new property. The object already has `sendVerificationEmail`,
    `sendOnSignIn: true`, and `expiresIn`. Insert `sendOnSignUp: true` immediately after
    `sendVerificationEmail` (before `sendOnSignIn`) or grouped with `sendOnSignIn` — the order
    within the object does not matter. The result should be:

      emailVerification: {
        sendVerificationEmail: async ({ user, url }) => { ... },
        sendOnSignUp: true,
        sendOnSignIn: true,
        expiresIn: 60 * 60 * 24,
      },

    Note on sendOnSignIn: if better-auth v1.6.13 does not recognize `sendOnSignIn` as a valid
    option (it may not — check the better-auth typings for emailVerification; the option that
    fires on login-while-unverified may be the default behavior of requireEmailVerification rather
    than an explicit flag), remove `sendOnSignIn` if it causes a TypeScript error. The must-have
    is `sendOnSignUp: true` — that is the missing flag that causes the token to never be created.

    In apps/web/tests/auth.test.ts, add a source-level assertion test verifying that auth.ts
    contains `sendOnSignUp: true`. Add it to the existing
    "Auth configuration — email/password requirements (D-01, D-02, D-03)" describe block or
    as a new describe. Example:

      it("emailVerification config includes sendOnSignUp: true (D-02)", async () => {
        const fs = await import("fs");
        const path = await import("path");
        const source = fs.readFileSync(
          path.join(__dirname, "../src/lib/auth/auth.ts"),
          "utf-8"
        );
        expect(source).toContain("sendOnSignUp: true");
      });
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge/apps/web && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    Tests pass. auth.ts contains `sendOnSignUp: true` in the emailVerification block.
    TypeScript compiles without errors (run `pnpm exec tsc --noEmit` to confirm).
    Live proof: after signup, a row exists in the verification table and the console
    transport logs a URL matching /api/auth/verify-email?token=...
  </done>
</task>

<task type="auto">
  <name>Task C: Commit in-flight dependency/origin fixes and gitignore hygiene</name>
  <files>
    apps/web/.gitignore
    apps/web/tsconfig.json
    pnpm-workspace.yaml
    pnpm-lock.yaml
    apps/web/src/lib/auth/auth.ts
  </files>
  <read_first>
    apps/web/.gitignore (full file — verify next-env.d.ts absent)
    apps/web/tsconfig.json (verify it is the auto-generated canonical Next.js tsconfig, safe to commit)
  </read_first>
  <action>
    Step 1 — Add next-env.d.ts to gitignore.
    In apps/web/.gitignore, add `next-env.d.ts` under the "# Next.js" section (after `.next/`).
    Do NOT commit apps/web/next-env.d.ts — it is a generated type reference file that Next.js
    regenerates on every build/dev start. Confirm `git ls-files apps/web/next-env.d.ts` returns
    empty (not yet tracked).

    Step 2 — Verify tsconfig.json is safe to commit.
    apps/web/tsconfig.json was auto-edited by Next.js on first run (added incremental, target,
    .next/types/**/*.ts include). It is the canonical tsconfig for this project. Stage it.

    Step 3 — Create two git commits (do NOT squash into one; keep dependency fix separate from bugs):

    Commit 1 — dependency and origin fixes (includes auth.ts changes from Task B, which is
    already modified at this point):
      git add pnpm-workspace.yaml pnpm-lock.yaml apps/web/src/lib/auth/auth.ts apps/web/tsconfig.json
      git commit -m "fix(deps): pin kysely to 0.28.17 and add trustedOrigins + sendOnSignUp to auth"

    Commit 2 — RLS fix and gitignore (actions.ts from Task A, test changes, gitignore):
      git add apps/web/src/lib/workspaces/actions.ts apps/web/tests/workspaces.test.ts apps/web/tests/auth.test.ts apps/web/.gitignore
      git commit -m "fix(phase-02): set RLS workspace context in createWorkspaceAction transaction"

    After both commits, `git status` must show a clean working tree (no untracked
    apps/web/next-env.d.ts because it is now gitignored).

    If git status still shows next-env.d.ts as untracked after adding to .gitignore:
    run `git rm --cached apps/web/next-env.d.ts` (only if it was accidentally staged;
    given `git ls-files` returned empty, this should not be needed).
  </action>
  <verify>
    <automated>git status --short</automated>
  </verify>
  <done>
    git status is clean (no untracked or modified files). The kysely 0.28.17 override
    is in effect (verify: `grep kysely pnpm-workspace.yaml` shows `kysely: 0.28.17`).
    next-env.d.ts is listed in apps/web/.gitignore. Two new commits appear in git log.
    The dev server boots without Kysely adapter errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action input → DB | workspaceId is server-generated (randomUUID), never from client payload |
| RLS set_config → Postgres | Transaction-local setting; resets on commit/rollback, no cross-request leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ju4-01 | Tampering | set_config call in actions.ts | mitigate | Use tagged template ($executeRaw`...`) — workspaceId is parameterized, not interpolated into SQL string |
| T-ju4-02 | Elevation of Privilege | RLS bypass if set_config missing | mitigate | This task adds set_config; RLS policy is the DB-level backstop enforcing the boundary |
| T-ju4-03 | Spoofing | Email verification token not sent | mitigate | sendOnSignUp: true ensures token is generated at signup, user cannot bypass verification |
</threat_model>

<verification>
1. All existing tests pass: `cd apps/web && pnpm test` exits 0 with 172+ tests green.
2. New test in workspaces.test.ts asserts set_config appears before workspace.create in actions.ts source.
3. New test in auth.test.ts asserts auth.ts contains sendOnSignUp: true.
4. TypeScript compiles: `cd apps/web && pnpm exec tsc --noEmit` exits 0.
5. git status is clean after Task C commits.
6. `grep "next-env.d.ts" apps/web/.gitignore` returns a match.
7. `grep "kysely" pnpm-workspace.yaml` shows `kysely: 0.28.17`.
</verification>

<success_criteria>
- createWorkspaceAction inserts a row into workspace and workspace_member without RLS error (live dev server)
- A signup on the live dev server writes a row to the verification table and logs a verify URL to console
- git history has two new commits with no in-flight modified/untracked files remaining
- All tests green, TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/quick/260603-ju4-fix-phase-2-workspace-creation-rls-bug-a/260603-ju4-SUMMARY.md`
</output>
