---
phase: 11-imagens-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/overrides/validate-url.ts
  - apps/web/src/lib/overrides/apply-shim.ts
  - apps/web/src/lib/overrides/apply-shim.test.ts
  - apps/web/src/lib/lps/schema.ts
  - apps/web/src/lib/overrides/edit-script.ts
  - apps/web/src/lib/overrides/edit-script.test.ts
autonomous: true
requirements: [EDIT-04, EDIT-05]

must_haves:
  truths:
    - "O apply-shim aplica overrides de type:'image' via setAttribute('src') no <img> e type:'href' via setAttribute('href') no <a>"
    - "O edit-script detecta clicks em <img> e <a href> no modo edição e emite ELEMENT_SELECTED com elementType e currentValue"
    - "O edit-script responde ao PREVIEW_OVERRIDE do parent e adiciona o override ao pendingMap (fonte de verdade para REQUEST_SAVE)"
    - "REQUEST_DISCARD restaura src/href originais e limpa originalMap/pendingMap para os três tipos"
    - "URLs inválidas (javascript:, data:, malformada) retornam ok:false de validateOverrideUrl"
  artifacts:
    - path: "apps/web/src/lib/overrides/validate-url.ts"
      provides: "validateOverrideUrl utility — contrato literal do UI-SPEC"
      exports: ["validateOverrideUrl"]
    - path: "apps/web/src/lib/overrides/apply-shim.ts"
      provides: "shimScript com ramos image/href ativos"
      contains: "setAttribute('src'"
    - path: "apps/web/src/lib/overrides/edit-script.ts"
      provides: "IIFE com isSelectableImage, isSelectableHref, PREVIEW_OVERRIDE handler"
      contains: "isSelectableImage"
  key_links:
    - from: "edit-script IIFE click handler"
      to: "parent (ViteSpaPreviewEditor)"
      via: "postMessage ELEMENT_SELECTED { elementType, currentValue }"
      pattern: "ELEMENT_SELECTED"
    - from: "parent PREVIEW_OVERRIDE"
      to: "edit-script IIFE message handler"
      via: "pendingMap update + setAttribute"
      pattern: "PREVIEW_OVERRIDE"
    - from: "apply-shim shimScript loop"
      to: "<img>/<a> nodes via pathToNode"
      via: "setAttribute('src'/'href')"
      pattern: "setAttribute"
---

<objective>
Estender o runtime de override (apply-shim e edit-script) e criar o utilitário compartilhado de validação de URL para ativar os tipos image e href no pipeline de edição visual VITE_SPA.

Purpose: Ativa os dois tipos de override que estavam "silently skipped" desde a Fase 9 e estabelece a fronteira de validação de protocolo no utilitário client-safe. O apply-shim aplica os overrides persistidos; o edit-script detecta e sinaliza os novos tipos de elemento; o validate-url.ts é o contrato de validação compartilhado entre cliente e servidor.

Output: validate-url.ts (novo), apply-shim.ts estendido (+ test), schema.ts atualizado (JSDoc), edit-script.ts estendido (+ test). Sem mudança de rotas, sem migração de BD.
</objective>

<execution_context>
@/home/glow/.claude/get-shit-done/workflows/execute-plan.md
@/home/glow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-imagens-links/11-RESEARCH.md
@.planning/phases/11-imagens-links/11-PATTERNS.md
@.planning/phases/11-imagens-links/11-UI-SPEC.md
@.planning/phases/10-editor-visual-in-iframe-texto/10-02-SUMMARY.md

<interfaces>
De apps/web/src/lib/overrides/apply-shim.ts (verificado na Fase 9/10):
  export function buildOverrideInjection(values: ViteSpaValues | null | undefined): { shimScript: string; overridesJson: string }
  export function injectOverrides(html: string, injection: { shimScript: string; overridesJson: string }): string
  shimScript string — loop interno: if (ov.type === 'text') { ... } else if (ov.type === 'color') { ... }
  // Hook point: linhas 150-157. Após ov.type === 'color', ANTES do comentário "silently skipped".

De apps/web/src/lib/overrides/edit-script.ts (verificado na Fase 10):
  export function buildEditScript(dashboardOrigin: string): string
  export function injectEditScript(html: string, script: string): string
  // isTextLeaf inclui 'img' em skipTags (linha 106-113) — MANTER assim
  // click handler tem guarda: if (!editMode || !isTextLeaf(e.target)) return; — SUBSTITUIR
  // originalMap[path] = string (textContent) na Fase 10 — ALTERAR para { elementType, value }
  // REQUEST_SAVE filter (linha 171-174): ov.value !== originalMap[ov.path] — ATUALIZAR para .value
  // REQUEST_DISCARD (linhas 175-195): node.textContent = originalMap[p] — ATUALIZAR para entrada tipada
  // window.addEventListener('message', ...) — handler unificado, NUNCA criar novo addEventListener

De apps/web/src/lib/lps/schema.ts:
  PfOverrideSchema — type: z.enum(["text", "color", "image", "href"]) — SEM MUDANÇA
  ViteSpaValues — interface já contém overrides?: PfOverride[]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: validate-url.ts (NEW) + apply-shim extension + apply-shim.test.ts + schema.ts JSDoc</name>
  <files>
    apps/web/src/lib/overrides/validate-url.ts
    apps/web/src/lib/overrides/apply-shim.ts
    apps/web/src/lib/overrides/apply-shim.test.ts
    apps/web/src/lib/lps/schema.ts
  </files>
  <behavior>
    Behaviors testáveis para validate-url.ts:
    - validateOverrideUrl("") → { ok: true } (campo vazio não é erro)
    - validateOverrideUrl("https://cdn.example.com/img.jpg") → { ok: true }
    - validateOverrideUrl("http://example.com/page") → { ok: true }
    - validateOverrideUrl("javascript:alert(1)") → { ok: false, error: "invalid-protocol" }
    - validateOverrideUrl("data:text/html,<h1>x</h1>") → { ok: false, error: "invalid-protocol" }
    - validateOverrideUrl("vbscript:msgbox(1)") → { ok: false, error: "invalid-protocol" }
    - validateOverrideUrl("ftp://files.example.com/img.png") → { ok: false, error: "invalid-protocol" }
    - validateOverrideUrl("not a url at all") → { ok: false, error: "malformed" }
    - validateOverrideUrl("//example.com/img.png") → { ok: false, error: "malformed" } (protocol-relative sem scheme → new URL lança)

    Behaviors testáveis para apply-shim (extensão via string containment):
    - buildOverrideInjection com type:'image' → shimScript contém "setAttribute('src'"
    - buildOverrideInjection com type:'image' → shimScript contém "tagName === 'IMG'"
    - buildOverrideInjection com type:'href' → shimScript contém "setAttribute('href'"
    - buildOverrideInjection com type:'href' → shimScript contém "tagName === 'A'"
    - ATUALIZAR: o teste "image type silently skipped" existente em apply-shim.test.ts deve ser SUBSTITUÍDO pelo comportamento ativo acima (image não é mais skipped)
  </behavior>
  <read_first>
    apps/web/src/lib/overrides/apply-shim.ts (ler completo — identificar linhas 150-157 do shimScript, o comentário "silently skipped", e a estrutura do loop)
    apps/web/src/lib/overrides/apply-shim.test.ts (ler completo — identificar e planejar a atualização do teste "image type silently skipped")
    apps/web/src/lib/lps/schema.ts (ler linhas 185-215 — PfOverrideSchema JSDoc e campos type/value)
    .planning/phases/11-imagens-links/11-RESEARCH.md (seção "Hook Points — 1. apply-shim.ts" e "validate-url.ts spec")
    .planning/phases/11-imagens-links/11-UI-SPEC.md (seção "URL Validation Contract")
  </read_first>
  <action>
    Criar apps/web/src/lib/overrides/validate-url.ts com o contrato EXATO da UI-SPEC "URL Validation Contract": exportar o tipo UrlValidResult (union de {ok:true} e {ok:false; error:'invalid-protocol'|'malformed'}) e a função validateOverrideUrl(raw: string): UrlValidResult. A implementação usa new URL(raw) dentro de try/catch (malformed → retorna error:'malformed') e checa parsed.protocol !== 'http:' && parsed.protocol !== 'https:' (retorna error:'invalid-protocol'). Campo vazio (raw falsy) retorna {ok:true}. Sem dependências externas — new URL() é built-in Node.js 18+ e browser. Não adicionar export default; apenas named export.

    Criar apps/web/src/lib/overrides/validate-url.test.ts com testes Vitest (import de validate-url.ts) cobrindo os 9 behaviors listados em behavior block acima. Seguir o padrão de import existente nos outros arquivos de teste na pasta.

    Estender apps/web/src/lib/overrides/apply-shim.ts: localizar o loop dentro da constante shimScript (string JavaScript embarcada) que tem os ramos ov.type === 'text' e ov.type === 'color'. Inserir dois novos ramos } else if (ov.type === 'image') { e } else if (ov.type === 'href') { ANTES do comentário "silently skipped". O ramo image: declarar var imgNode = pathToNode(ov.path); verificar imgNode && imgNode.tagName === 'IMG'; chamar imgNode.setAttribute('src', ov.value). O ramo href: declarar var aNode = pathToNode(ov.path); verificar aNode && aNode.tagName === 'A'; chamar aNode.setAttribute('href', ov.value). NUNCA usar .src = ou .href = (IDL property); SEMPRE usar setAttribute — padrão T-09-02-02. Ambos os ramos devem ficar dentro do try/catch por-override já existente (linha ~148). Atualizar o JSDoc do buildOverrideInjection para documentar os ramos image e href como ativos (não mais "reserved").

    Atualizar apps/web/src/lib/overrides/apply-shim.test.ts: localizar e SUBSTITUIR o teste "image type silently skipped (no innerHTML)" pelo comportamento correto da Fase 11 — buildOverrideInjection com override image deve resultar num shimScript que CONTÉM "setAttribute('src'" e "tagName === 'IMG'". Adicionar teste análogo para href: shimScript contém "setAttribute('href'" e "tagName === 'A'". Não remover outros testes existentes — apenas substituir o silently-skipped test.

    Atualizar apps/web/src/lib/lps/schema.ts: localizar o bloco JSDoc do PfOverrideSchema (comentários nas linhas ~190-193 que descrevem type e value). Substituir a parte "image and href: reserved for Phase 11" pelo texto correto: image aplica via set src no Phase 11 shim; href aplica via set href; ambos validados server-side (SEC-02). NÃO alterar os campos Zod — type enum e value: z.string() permanecem sem mudança.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web vitest run src/lib/overrides/apply-shim.test.ts src/lib/overrides/validate-url.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - validate-url.ts existe em apps/web/src/lib/overrides/validate-url.ts e exporta validateOverrideUrl
    - grep -c "validateOverrideUrl" apps/web/src/lib/overrides/validate-url.ts retorna >= 1
    - grep -c "setAttribute\('src'" apps/web/src/lib/overrides/apply-shim.ts retorna >= 1 (dentro da string shimScript)
    - grep -c "setAttribute\('href'" apps/web/src/lib/overrides/apply-shim.ts retorna >= 1
    - grep -c "tagName === 'IMG'" apps/web/src/lib/overrides/apply-shim.ts retorna >= 1
    - grep -c "tagName === 'A'" apps/web/src/lib/overrides/apply-shim.ts retorna >= 1
    - grep -c "silently skipped" apps/web/src/lib/overrides/apply-shim.ts retorna 0 (comentário "image/href silently skipped" deve ser removido ou atualizado)
    - pnpm --filter web vitest run src/lib/overrides/apply-shim.test.ts passa sem erros
    - pnpm --filter web vitest run src/lib/overrides/validate-url.test.ts passa sem erros
    - schema.ts JSDoc não menciona mais "reserved for Phase 11" para image/href
  </acceptance_criteria>
  <done>validate-url.ts criado com contrato UI-SPEC; apply-shim.ts com ramos image/href ativos via setAttribute; testes passando; schema.ts JSDoc atualizado</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: edit-script.ts extension + edit-script.test.ts update</name>
  <files>
    apps/web/src/lib/overrides/edit-script.ts
    apps/web/src/lib/overrides/edit-script.test.ts
  </files>
  <behavior>
    Behaviors testáveis via string containment em buildEditScript(dashboardOrigin):
    - script contém "isSelectableImage" (nova função predicado)
    - script contém "isSelectableHref" (nova função predicado)
    - script contém "hasAttribute('href')" (guard: <a> sem href não é selecionável)
    - script contém "PREVIEW_OVERRIDE" (novo tipo de mensagem parent→iframe)
    - script contém "elementType" (novo campo em ELEMENT_SELECTED e em originalMap)
    - script contém "currentValue" (novo campo em ELEMENT_SELECTED)
    - script contém "pendingMap[msg.path]" (PREVIEW_OVERRIDE adiciona ao pendingMap)
    - isTextLeaf ainda contém 'img' em skipTags (NÃO deve ser removido — isSelectableImage é separado)
    - script NÃO contém "innerHTML" (nenhum uso de innerHTML para aplicar overrides)
  </behavior>
  <read_first>
    apps/web/src/lib/overrides/edit-script.ts (ler completo — identificar: isTextLeaf função, mouseover/mouseout handlers, click handler com guarda !isTextLeaf, originalMap uso, REQUEST_SAVE filter, REQUEST_DISCARD handler, window.addEventListener message handler bloco)
    apps/web/src/lib/overrides/edit-script.test.ts (ler completo — entender padrão de testes via string containment para planejar adições)
    .planning/phases/11-imagens-links/11-RESEARCH.md (seção "Hook Points — 2. edit-script.ts" completa: extensões A-G)
    .planning/phases/11-imagens-links/11-PATTERNS.md (seção "edit-script.ts — Pattern Assignment" completa: extensões A-G com código exato)
    .planning/phases/11-imagens-links/11-UI-SPEC.md (seção "In-Iframe Highlight Contract — Phase 11 additions" e "Extended postMessage Protocol")
  </read_first>
  <action>
    Estender apps/web/src/lib/overrides/edit-script.ts. Todas as modificações são DENTRO da string retornada por buildEditScript (IIFE JavaScript embarcado). Nunca criar um segundo window.addEventListener — todos os handlers ficam dentro do bloco existente.

    Extensão A — novos predicados (adicionar após isTextLeaf, antes de sendToParent): adicionar isSelectableImage(el) que retorna el.nodeType === Node.ELEMENT_NODE && el.tagName === 'IMG'. Adicionar isSelectableHref(el) que retorna el.nodeType === Node.ELEMENT_NODE && el.tagName === 'A' && el.hasAttribute('href'). <a> sem href retorna false — bare <a> não é selecionável per D-11-02.

    Extensão B — hover mouseover: substituir a guarda !isTextLeaf(e.target) pela tripla !isTextLeaf(e.target) && !isSelectableImage(e.target) && !isSelectableHref(e.target). Manter restante intacto.

    Extensão C — hover mouseout: mesma substituição de guarda que B.

    Extensão D — click handler: substituir a guarda if (!editMode || !isTextLeaf(e.target)) return; pelo bloco multitype: verificar isSelectableImage → elementType='image'; isSelectableHref → elementType='href'; isTextLeaf → elementType='text'; caso nenhum → return. Capturar currentValue por tipo: image usa el.getAttribute('src') || el.src || ''; href usa el.getAttribute('href') || ''; text usa el.textContent || ''. Lazy-capture no originalMap: if (!originalMap[path]) { originalMap[path] = { elementType: elementType, value: currentValue }; } (shape novo: objeto {elementType, value}, não string). Calcular originalHash = fnv1a(originalMap[path].value). Emitir sendToParent({ type: 'ELEMENT_SELECTED', path, originalHash, elementType, currentValue }). Para elementType === 'text': continuar o fluxo contenteditable existente (setAttribute('contenteditable','true'), cursor, focus, blur/keydown handlers). Para 'image' e 'href': NÃO setar contenteditable — controle fica no parent. Regra de prioridade: isSelectableImage verificado ANTES de isSelectableHref (img dentro de <a> → seleciona imagem, conforme UI-SPEC Precedence rule).

    Extensão E — REQUEST_SAVE filter: localizar o filtro (linha ~171-174) que faz var overrides = Object.values(pendingMap).filter(function(ov) { return ov.value !== originalMap[ov.path]; }). ATUALIZAR para comparar ov.value !== (originalMap[ov.path] && originalMap[ov.path].value). Crítico: sem essa atualização, o formato {elementType,value} quebraria o filtro de texto existente.

    Extensão F — PREVIEW_OVERRIDE handler (novo): dentro do switch/if-else do window.addEventListener('message'), adicionar handler para msg.type === 'PREVIEW_OVERRIDE'. O handler: obter ovEl = pathToNode(msg.path); retornar se !ovEl. Se msg.elementType === 'image' && ovEl.tagName === 'IMG': ovEl.setAttribute('src', msg.value). Se msg.elementType === 'href' && ovEl.tagName === 'A': ovEl.setAttribute('href', msg.value). CRÍTICO (Pitfall 2): adicionar ao pendingMap — pendingMap[msg.path] = { path: msg.path, originalHash: origHash, type: msg.elementType, value: msg.value } onde origHash vem do originalMap[msg.path]?.value ou ''. Sem esse pendingMap update, REQUEST_SAVE não enviaria os overrides de imagem/href ao parent.

    Extensão G — REQUEST_DISCARD handler: atualizar o loop de restauração para usar o novo shape {elementType, value}. Para entry.elementType === 'text': node.textContent = entry.value. Para 'image' && node.tagName === 'IMG': node.setAttribute('src', entry.value). Para 'href' && node.tagName === 'A': node.setAttribute('href', entry.value). Limpar pendingMap = {} e originalMap = {} ao final (comportamento herdado, mantido).

    Atualizar apps/web/src/lib/overrides/edit-script.test.ts: adicionar testes de string containment para os 9 behaviors listados no behavior block acima. Seguir padrão de it("...", () => { const result = buildEditScript("http://localhost:3000"); expect(result).toContain("..."); }). Não remover testes existentes.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web vitest run src/lib/overrides/edit-script.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "isSelectableImage" apps/web/src/lib/overrides/edit-script.ts retorna >= 3 (definição + uso em hover + uso em click)
    - grep -c "isSelectableHref" apps/web/src/lib/overrides/edit-script.ts retorna >= 3
    - grep -c "PREVIEW_OVERRIDE" apps/web/src/lib/overrides/edit-script.ts retorna >= 1
    - grep -c "pendingMap\[msg\.path\]" apps/web/src/lib/overrides/edit-script.ts retorna >= 1 (PREVIEW_OVERRIDE adiciona ao pendingMap)
    - grep -c "'img'" apps/web/src/lib/overrides/edit-script.ts retorna >= 1 (img continua em skipTags de isTextLeaf)
    - grep -c "elementType" apps/web/src/lib/overrides/edit-script.ts retorna >= 5 (predicados + click + originalMap + ELEMENT_SELECTED + REQUEST_DISCARD)
    - grep -c "innerHTML" apps/web/src/lib/overrides/edit-script.ts retorna 0 (nenhum uso de innerHTML)
    - pnpm --filter web vitest run src/lib/overrides/edit-script.test.ts passa sem erros
    - pnpm --filter web tsc --noEmit (no apps/web) retorna 0 erros de TypeScript
  </acceptance_criteria>
  <done>edit-script.ts estendido com detecção de img/a, PREVIEW_OVERRIDE handler, originalMap shape atualizado, REQUEST_DISCARD corrigido; testes passando</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Iframe LP → parent dashboard | postMessage com origin check (event.origin === dashboardOrigin no iframe; event.origin === serveOrigin no parent) |
| pf-overrides JSON blob → browser | HTML injected via script tag; escapeJsonForHtml (já implementado) unicode-escapa `<>&` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | edit-script.ts: PREVIEW_OVERRIDE handler | mitigate | Handler dentro do bloco message que já valida event.origin !== dashboardOrigin — origin check herdado da Fase 10 (edit-script.ts linha ~159). Novo handler DEVE estar DENTRO do mesmo bloco, nunca em addEventListener separado sem origin check. |
| T-11-01-02 | Tampering | apply-shim.ts: aplicação via setAttribute | accept | setAttribute('src'/'href') com URL já validada server-side (SEC-02, Plan 11-02). tagName guards (==='IMG'/==='A') previnem aplicação em nó errado se DOM mudou. |
| T-11-01-03 | Information Disclosure | ELEMENT_SELECTED emite currentValue (src/href) | accept | currentValue é o valor público do atributo DOM — já visível ao usuário no browser. Emitido apenas para o parent via targetOrigin (nunca '*'). |
| T-11-01-04 | Tampering | edit-script.ts: innerHTML | mitigate | Proibido — checagem: grep de 'innerHTML' deve retornar 0 no arquivo. Todos os writes via textContent (texto) ou setAttribute (src/href). |
</threat_model>

<verification>
Verificação integral do plano:

1. pnpm --filter web vitest run src/lib/overrides/ — todos os testes da pasta passam
2. pnpm --filter web tsc --noEmit — 0 erros TypeScript
3. grep -c "setAttribute\('src'" apps/web/src/lib/overrides/apply-shim.ts — >= 1
4. grep -c "setAttribute\('href'" apps/web/src/lib/overrides/apply-shim.ts — >= 1
5. grep -c "isSelectableImage" apps/web/src/lib/overrides/edit-script.ts — >= 3
6. grep -c "PREVIEW_OVERRIDE" apps/web/src/lib/overrides/edit-script.ts — >= 1
7. grep -c "innerHTML" apps/web/src/lib/overrides/edit-script.ts — deve ser 0
8. grep -c "validateOverrideUrl" apps/web/src/lib/overrides/validate-url.ts — >= 1
</verification>

<success_criteria>
- validate-url.ts criado com contrato literal do UI-SPEC; rejeita javascript:/data:/ftp:/malformada; aceita http:/https: e campo vazio
- apply-shim.ts aplica overrides image via setAttribute('src') e href via setAttribute('href') com guards tagName
- edit-script.ts detecta <img> e <a href> no click/hover; emite ELEMENT_SELECTED com elementType+currentValue; responde a PREVIEW_OVERRIDE adicionando ao pendingMap; REQUEST_DISCARD restaura src/href originais
- originalMap shape mudou de string para {elementType, value} consistentemente (REQUEST_SAVE filter + REQUEST_DISCARD + click handler)
- Todos os testes Vitest na pasta overrides/ passam
- 0 erros TypeScript (pnpm --filter web tsc --noEmit)
</success_criteria>

<output>
Após conclusão, criar .planning/phases/11-imagens-links/11-01-SUMMARY.md
</output>
