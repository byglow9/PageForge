---
phase: 11-imagens-links
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - apps/web/src/components/ui/popover.tsx
  - apps/web/src/components/lps/ImageSwapPanel.tsx
autonomous: true
requirements: [EDIT-04]

must_haves:
  truths:
    - "ImageSwapPanel exibe uma zona de upload (drag-and-drop + clique) e um campo de URL — juntos, em um único painel"
    - "Upload de arquivo via S3 presigned funciona (idle → uploading com progress → uploaded com thumbnail → confirm chama onConfirm com publicUrl)"
    - "Fechar o Popover sem confirmar aborta upload em curso e descarta urlValue"
    - "URL externa válida (http/https) inserida no campo permite confirmar via onConfirm(urlValue)"
    - "URL inválida (javascript:, data:, malformada) exibe erro inline — botão Aplicar desabilitado"
  artifacts:
    - path: "apps/web/src/components/ui/popover.tsx"
      provides: "Shadcn Popover component"
      contains: "PopoverContent"
    - path: "apps/web/src/components/lps/ImageSwapPanel.tsx"
      provides: "ImageSwapPanel React component"
      exports: ["ImageSwapPanel"]
      contains: "requestPresignedUploadAction"
  key_links:
    - from: "ImageSwapPanel: handleFile (upload)"
      to: "requestPresignedUploadAction + XHR PUT + validateUploadedImageAction"
      via: "Server Actions re-usados de ImageUploadField"
      pattern: "requestPresignedUploadAction"
    - from: "ImageSwapPanel: handleConfirm"
      to: "props.onConfirm(finalUrl)"
      via: "uploadedUrl se uploaded; urlValue se url válida"
      pattern: "onConfirm"
---

<objective>
Criar o componente ImageSwapPanel — um Popover com zona de upload S3 presigned e campo de URL externa — que será plugado no slot D-04 da toolbar do ViteSpaPreviewEditor para troca de imagens.

Purpose: Encapsula todo o fluxo de troca de imagem (upload via S3 presigned ou URL externa) num componente autocontido que comunica o resultado via callback onConfirm(finalUrl). Reutiliza o mecanismo de upload existente (requestPresignedUploadAction, validateUploadedImageAction, XHR PUT com progress) sem usar RHF Controller.

Output: shadcn Popover instalado + ImageSwapPanel.tsx criado e funcional (upload + URL + confirm). Pronto para ser referenciado por ViteSpaPreviewEditor no Plan 11-04.
</objective>

<execution_context>
@/home/glow/.claude/get-shit-done/workflows/execute-plan.md
@/home/glow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-imagens-links/11-RESEARCH.md
@.planning/phases/11-imagens-links/11-PATTERNS.md
@.planning/phases/11-imagens-links/11-UI-SPEC.md
@.planning/phases/11-imagens-links/11-01-SUMMARY.md

<interfaces>
De apps/web/src/components/lps/ImageUploadField.tsx (analog — ler para extrair padrões, não reutilizar como componente):
  Imports reusáveis: useRef, useState, useCallback de 'react'; UploadCloud, AlertCircle, X de 'lucide-react'; Progress de '@/components/ui/progress'
  Server Actions: requestPresignedUploadAction(slug, { filename, contentType, fileSize, firstBytes }): ActionResult<{ presignedUrl, publicUrl, key }>
  Server Actions: validateUploadedImageAction(slug, { key }): ActionResult<{ width, height }>
  ALLOWED_MIME_TYPES: ['image/jpeg', 'image/png', 'image/webp']
  MAX_FILE_SIZE: 5 * 1024 * 1024 (5 MB)
  Upload states: 'idle' | 'uploading' | 'uploaded' | 'error'
  Fluxo completo: handleFile callback (linhas 89-202) — pre-validate MIME/size → firstBytes → requestPresignedUploadAction → XHR PUT → validateUploadedImageAction → success
  Cancel: xhrRef.current?.abort() + resetar estado
  DIFERENÇA principal: ImageSwapPanel NÃO usa RHF Controller; resultado entregue via onConfirm(finalUrl)

De apps/web/src/lib/overrides/validate-url.ts (criado em Plan 11-01):
  export function validateOverrideUrl(raw: string): { ok: true } | { ok: false; error: 'invalid-protocol' | 'malformed' }
  Importar como: import { validateOverrideUrl } from '@/lib/overrides/validate-url'

Props contract para ImageSwapPanel (UI-SPEC — LOCKED):
  interface ImageSwapPanelProps {
    slug: string
    currentSrc: string          // pré-preenche campo de URL
    onConfirm: (value: string) => void
    open: boolean
    onOpenChange: (open: boolean) => void
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Instalar shadcn Popover + criar ImageSwapPanel — zona de upload + estados</name>
  <files>
    apps/web/src/components/ui/popover.tsx
    apps/web/src/components/lps/ImageSwapPanel.tsx
  </files>
  <read_first>
    apps/web/src/components/lps/ImageUploadField.tsx (ler completo — extrair: (a) imports de Server Actions, (b) lógica do handleFile callback linhas 89-202, (c) handleCancel, (d) handleRemove, (e) UI do estado idle/uploading/uploaded/error, (f) constantes ALLOWED_MIME_TYPES e MAX_FILE_SIZE)
    apps/web/src/components/ui/progress.tsx (verificar que Progress está instalado e exportado)
    .planning/phases/11-imagens-links/11-UI-SPEC.md (seção "ImageSwapPanel Component" completa — props, layout, upload zone states, accessible form)
    .planning/phases/11-imagens-links/11-PATTERNS.md (seção "ImageSwapPanel.tsx — Pattern Assignment" com state pattern, core upload flow, cancel pattern, remove pattern, uploaded state UI)
  </read_first>
  <action>
    Primeiro, instalar o componente Popover do shadcn executando npx shadcn@latest add popover --yes na raiz do workspace (apps/web). Isso cria apps/web/src/components/ui/popover.tsx. Verificar que o arquivo foi criado antes de prosseguir.

    Criar apps/web/src/components/lps/ImageSwapPanel.tsx como Client Component ('use client'). Importar: useRef, useState, useCallback de 'react'; UploadCloud, AlertCircle, X de 'lucide-react'; Progress de '@/components/ui/progress'; Button de '@/components/ui/button'; Separator de '@/components/ui/separator'; requestPresignedUploadAction e validateUploadedImageAction de '@/lib/lps/actions'. NÃO importar nada de react-hook-form.

    Declarar interface ImageSwapPanelProps com campos slug (string), currentSrc (string), onConfirm ((value: string) => void), open (boolean), onOpenChange ((open: boolean) => void).

    Declarar state interno via useState: uploadState ('idle'|'uploading'|'uploaded'|'error'), uploadProgress (number, 0), uploadedUrl (string|null, null), urlValue (string, ''), urlError (string|null, null). Declarar xhrRef via useRef<XMLHttpRequest|null>(null).

    Implementar useEffect que re-preenche urlValue com currentSrc quando o painel abre (deps: [open, currentSrc]) — se !open, resetar uploadState para 'idle', uploadProgress para 0, uploadedUrl para null, xhrRef para null. Isso implementa "fechar sem confirmar reseta o estado".

    Implementar handleFile(file: File) como useCallback: (a) validar MIME contra ALLOWED_MIME_TYPES e size contra MAX_FILE_SIZE — erros de UX setam uploadState('error') e errorMessage adequado (ver cópias na UI-SPEC); (b) setUploadState('uploading'), setUploadProgress(0); (c) ler firstBytes via file.slice(0,4100).arrayBuffer(); (d) chamar requestPresignedUploadAction(slug, {filename, contentType, fileSize, firstBytes}); (e) XHR PUT para presignedUrl com progress listener (setUploadProgress), ouvindo 'load'/'error'/'abort'; (f) após upload bem-sucedido, chamar validateUploadedImageAction(slug, {key}); (g) em sucesso: setUploadState('uploaded'), setUploadedUrl(publicUrl); (h) em qualquer erro: setUploadState('error') com errorMessage específico. Padrão idêntico a ImageUploadField.tsx linhas 89-202 — extrair a lógica (não o componente).

    Implementar handleCancel: xhrRef.current?.abort(); xhrRef.current = null; setUploadState('idle'); setUploadProgress(0).
    Implementar handleRemove: setUploadState('idle'); setUploadedUrl(null); setUploadProgress(0).

    Renderizar a zona de upload (h-20, 80px) com quatro estados via condicionais:
    - idle: div com cursor-pointer + border dashed + UploadCloud icon + copy "Arraste uma imagem ou clique para fazer upload" (text-sm) + "PNG, JPG, WEBP · Máx 5 MB" (text-xs text-muted-foreground) + drag-over state border-blue-300 bg-blue-50
    - uploading: Progress bar (h-1.5) + "Enviando… {uploadProgress}%" + botão "Cancelar envio" (text/ghost, ao clicar: handleCancel)
    - uploaded: div bg-green-50 border border-green-200 rounded-lg p-3 flex items-center gap-3; img 48×48 (w-12 h-12 object-cover rounded); span "Imagem enviada" (text-sm font-medium text-gray-900); Button variant="ghost" size="icon" aria-label="Remover imagem" com X icon aria-hidden (handleRemove)
    - error: div bg-red-50 border border-red-300; AlertCircle; errorMessage (text-sm text-red-600); botão "Tentar novamente" que reseta para idle

    Input de arquivo oculto (type="file" accept="image/jpeg,image/png,image/webp" className="sr-only") com ref para disparo ao clicar na zona de upload. Handler onDrop via onDragOver/onDragLeave/onDrop.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/ui/popover.tsx existe (shadcn instalou)
    - apps/web/src/components/lps/ImageSwapPanel.tsx existe
    - grep -c "requestPresignedUploadAction" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 1
    - grep -c "validateUploadedImageAction" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 1
    - grep -c "xhrRef" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 2 (declaração + uso no abort)
    - grep -c "Controller" apps/web/src/components/lps/ImageSwapPanel.tsx retorna 0 (sem RHF)
    - grep -c "uploadState" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 4 (idle/uploading/uploaded/error checks)
    - grep -c "aria-label=\"Remover imagem\"" apps/web/src/components/lps/ImageSwapPanel.tsx retorna 1
    - pnpm --filter web tsc --noEmit retorna 0 erros TypeScript
  </acceptance_criteria>
  <done>shadcn Popover instalado; ImageSwapPanel criado com fluxo de upload completo (5 steps), cancel, remove e todos os estados de UI per UI-SPEC</done>
</task>

<task type="auto">
  <name>Tarefa 2: ImageSwapPanel — campo URL + confirm logic + botão Aplicar + validação inline</name>
  <files>
    apps/web/src/components/lps/ImageSwapPanel.tsx
  </files>
  <read_first>
    apps/web/src/components/lps/ImageSwapPanel.tsx (ler o estado atual após Tarefa 1 — identificar onde inserir Separator, Input, Tooltip e Button "Aplicar imagem")
    apps/web/src/components/ui/ (verificar quais componentes existem: Input, Label, Tooltip — se não existirem, instalar via shadcn; mas devem existir desde fases anteriores)
    .planning/phases/11-imagens-links/11-UI-SPEC.md (seção "ImageSwapPanel Component" — layout descrito: Separator com "ou" + Input URL + botão "Aplicar imagem" + disabled states + Tooltip para disabled)
    .planning/phases/11-imagens-links/11-RESEARCH.md (seção "ImageSwapPanel.tsx — spec do componente": lógica handleConfirm, fechar sem confirmar, estado urlValue/urlError)
    .planning/phases/11-imagens-links/11-PATTERNS.md (seção "Lógica de 'Aplicar imagem'" com handleConfirm exato)
  </read_first>
  <action>
    Adicionar ao ImageSwapPanel.tsx (após a zona de upload renderizada na Tarefa 1) os seguintes elementos adicionais:

    Importar adicionalmente: Input de '@/components/ui/input'; Tooltip, TooltipContent, TooltipTrigger de '@/components/ui/tooltip'; Label de '@/components/ui/label'; cn de '@/lib/utils'; validateOverrideUrl de '@/lib/overrides/validate-url'.

    Após a zona de upload, renderizar Separator de shadcn com texto "ou" centralizado (texto text-xs text-muted-foreground) — reproduz o padrão do divider UI-SPEC.

    Renderizar Label htmlFor="image-url-input" com className="sr-only" e conteúdo "URL da imagem".
    Renderizar Input id="image-url-input" type="url" placeholder="https://exemplo.com/imagem.jpg" value={urlValue} onChange e onBlur. No onChange: setUrlValue(e.target.value); const r = validateOverrideUrl(e.target.value); setUrlError(r.ok ? null : (r.error === 'invalid-protocol' ? 'URL inválida — apenas http:// e https:// são permitidos.' : 'URL inválida — verifique o endereço.')). Se urlError, adicionar className border-destructive ao Input. Abaixo do Input, quando urlError: renderizar p className="text-xs text-destructive mt-1" com texto do urlError.

    Implementar handleConfirm(): if uploadState === 'uploaded' e uploadedUrl: finalUrl = uploadedUrl. Else if urlValue e validateOverrideUrl(urlValue).ok e urlValue: finalUrl = urlValue. Else: return (sem ação). Chamar props.onConfirm(finalUrl). O painel não fecha por si — o parent (ViteSpaPreviewEditor) fecha via onOpenChange(false) ao receber o onConfirm.

    Calcular isUrlValid = urlValue !== '' && validateOverrideUrl(urlValue).ok.
    Calcular isApplyDisabled = uploadState === 'uploading' || (uploadState !== 'uploaded' && !isUrlValid).

    Renderizar Tooltip wrapping Button: TooltipTrigger asChild > Button variant="default" w-full mt-4 onClick={handleConfirm} disabled={isApplyDisabled} com texto "Aplicar imagem". TooltipContent visível APENAS quando isApplyDisabled — exibir cópia "Envie um arquivo ou insira uma URL válida para aplicar".

    Adicionar título do painel acima da zona de upload: p ou span com texto "Trocar imagem" (text-sm font-semibold) — este fica fora do Popover porque o painel é renderizado DENTRO do PopoverContent pelo parent; portanto é apenas o header interno do painel.

    Certificar que o efeito de reset de estado ao fechar (open === false, implementado na Tarefa 1) inclui também: setUrlValue(currentSrc) — pré-preenche com o src atual ao reabrir — e setUrlError(null).
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "validateOverrideUrl" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 2 (onChange + handleConfirm)
    - grep -c "onConfirm" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 2 (props type + chamada em handleConfirm)
    - grep -c "Aplicar imagem" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 1 (botão CTA)
    - grep -c "isApplyDisabled" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 2 (cálculo + uso no disabled)
    - grep -c "text-destructive" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 1 (erro de URL)
    - grep -c "Envie um arquivo ou insira" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 1 (Tooltip disabled CTA)
    - grep -c "URL inválida — apenas http" apps/web/src/components/lps/ImageSwapPanel.tsx retorna >= 1 (cópia de erro UI-SPEC)
    - Export nomeado ImageSwapPanel: grep -c "export function ImageSwapPanel" retorna >= 1 (ou export const ImageSwapPanel)
    - pnpm --filter web tsc --noEmit retorna 0 erros TypeScript
  </acceptance_criteria>
  <done>ImageSwapPanel completo: zona de upload + campo URL com validação inline + botão Aplicar com disabled logic + confirm via onConfirm; TypeScript compila</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → S3 (XHR PUT direto) | Upload direto; app server só gera presigned URL |
| ImageSwapPanel → requestPresignedUploadAction | Server Action; verifica magic-bytes, MIME, size cap 5 MB, workspace-scoped S3 key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | ImageSwapPanel: client URL validation | accept | Validação cliente é UX only, não a fronteira de confiança. Autoridade está em updateLpAction (Plan 11-02 T-11-02-01). Documentado em D-11-03. |
| T-11-03-02 | Tampering | S3 key forgery via ImageSwapPanel | accept | validateUploadedImageAction verifica prefixo workspaces/{workspaceId}/lps/assets/ (CR-01 herdado da Fase 9) — chave forjada de outro tenant é rejeitada. |
| T-11-03-03 | Denial of Service | Upload sem cancelamento | mitigate | handleCancel aborta XHR via xhrRef.current?.abort(); fechar Popover sem confirmar também aborta via useEffect (open === false → abort). |
| T-11-03-04 | Tampering | dangerouslySetInnerHTML em preview da imagem | accept | Thumbnail usa tag img com src={uploadedUrl} (S3 URL validada). NÃO usa innerHTML. |
</threat_model>

<verification>
Verificação integral do plano:

1. pnpm --filter web tsc --noEmit — 0 erros TypeScript
2. apps/web/src/components/ui/popover.tsx existe
3. apps/web/src/components/lps/ImageSwapPanel.tsx existe com export nomeado
4. grep -c "requestPresignedUploadAction" ImageSwapPanel.tsx — >= 1
5. grep -c "validateOverrideUrl" ImageSwapPanel.tsx — >= 2
6. grep -c "Controller" ImageSwapPanel.tsx — 0 (sem RHF)
7. grep -c "onConfirm" ImageSwapPanel.tsx — >= 2
</verification>

<success_criteria>
- shadcn Popover instalado em apps/web/src/components/ui/popover.tsx
- ImageSwapPanel exportado como named export de apps/web/src/components/lps/ImageSwapPanel.tsx
- Componente gerencia upload S3 presigned (5 steps de ImageUploadField) sem RHF Controller
- Campo URL valida com validateOverrideUrl e exibe erro inline per UI-SPEC copy
- "Aplicar imagem" desabilitado quando uploading OU (não uploaded E URL inválida)
- onConfirm chamado com finalUrl (uploadedUrl ou urlValue válido)
- 0 erros TypeScript
</success_criteria>

<output>
Após conclusão, criar .planning/phases/11-imagens-links/11-03-SUMMARY.md
</output>
