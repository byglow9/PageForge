---
phase: 11-imagens-links
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/lps/actions.ts
  - apps/web/src/app/api/lps/[lpId]/export/route.ts
autonomous: true
requirements: [EDIT-04, EDIT-05]

must_haves:
  truths:
    - "Tentar salvar um override de image ou href com URL javascript:, data:, ftp: ou malformada é rejeitado por updateLpAction com {ok:false} — nenhum override inválido é persistido"
    - "Overrides image com URL S3 são baixados para assets/ no ZIP e o src é reescrito como ./assets/{filename} no index.html exportado"
    - "Overrides image com URL externa (não S3) mantêm a URL absoluta no HTML exportado — não são baixados"
    - "Override href com URL http(s) válida passa a validação e é persistido normalmente"
  artifacts:
    - path: "apps/web/src/lib/lps/actions.ts"
      provides: "updateLpAction com loop de validação URL após safeParse, antes de persist"
      contains: "parsedUrl.protocol"
    - path: "apps/web/src/app/api/lps/[lpId]/export/route.ts"
      provides: "VITE_SPA branch com download de imagens S3 de overrides e reescrita de src"
      contains: "imageOverrides"
  key_links:
    - from: "updateLpAction: overridesParsed.success === true"
      to: "DB persist via prisma.landingPage.update"
      via: "URL validation loop (new URL() + protocol check) — SEC-02"
      pattern: "parsedUrl.protocol"
    - from: "export route: buildOverrideInjection + injectOverrides"
      to: "viteSpaArchive.append(processedHtml)"
      via: "S3 image download loop + .split(url).join('./assets/...')"
      pattern: "processedHtml"
---

<objective>
Adicionar a camada de segurança server-side de validação de URL para overrides image/href e implementar a reescrita de imagens S3 de override no export ZIP (D-11-04).

Purpose: Validação autoritativa de URL — o servidor é a única fronteira de confiança real (SEC-02). O cliente pré-valida para UX, mas o servidor bloqueia URLs inválidas independentemente. O export ZIP precisa baixar imagens S3 de overrides e reescrevê-las como paths relativos para que o ZIP seja self-contained (D-11-04).

Output: actions.ts com loop de validação URL em updateLpAction; export/route.ts com branch VITE_SPA que coleta, baixa e reescreve imagens S3 de overrides no ZIP.
</objective>

<execution_context>
@/home/glow/.claude/get-shit-done/workflows/execute-plan.md
@/home/glow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-imagens-links/11-RESEARCH.md
@.planning/phases/11-imagens-links/11-PATTERNS.md
@.planning/phases/10-editor-visual-in-iframe-texto/10-02-SUMMARY.md

<interfaces>
De apps/web/src/lib/lps/actions.ts (VITE_SPA branch — linhas ~389-427, verificado):
  overridesParsed = SaveViteSpaOverridesSchema.safeParse({ id, overrides, primaryColorOverride })
  // Hook point: APÓS if (!overridesParsed.success) { return error }, ANTES de const existingValues = ...
  // Padrão de retorno: { ok: false, error: string } (sem fieldErrors — alinha com saveError Alert existente)
  // new URL() disponível em Node.js 18+ sem dependência externa

De apps/web/src/app/api/lps/[lpId]/export/route.ts (VITE_SPA branch — linhas ~259-298, verificado):
  const injection = buildOverrideInjection(lpValues);
  const finalHtml = injectOverrides(themedHtml, injection);
  // Hook point: APÓS finalHtml, ANTES de viteSpaArchive.append(Buffer.from(finalHtml, 'utf-8'), { name: 'index.html' })
  // viteSpaArchive é um Archiver instance (archiver npm, stream-based)
  // lpValues.overrides?: PfOverride[] — pode ser undefined
  // S3_PUBLIC_BASE_URL env var — base URL do bucket S3 próprio
  // Padrão anti-SSRF: redirect:'error' + startsWith(s3BaseUrl) — igual ao LIQUID path (linhas 99-164)
  // Padrão de substituição global: .split(url).join(replacement) — NÃO .replace() (Pitfall 5)

De apps/web/src/lib/overrides/apply-shim.ts (sem mudança neste plano):
  interface PfOverride { path: string; originalHash: string; type: 'text'|'color'|'image'|'href'; value: string }
  interface ViteSpaValues { overrides?: PfOverride[]; primaryColorOverride?: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: updateLpAction — URL validation loop (SEC-02)</name>
  <files>
    apps/web/src/lib/lps/actions.ts
  </files>
  <read_first>
    apps/web/src/lib/lps/actions.ts (ler a função updateLpAction completa — branch VITE_SPA, aproximadamente linhas 370-430; identificar: (a) onde SaveViteSpaOverridesSchema.safeParse é chamado, (b) o bloco if (!overridesParsed.success) com retorno de erro, (c) onde começa const existingValues = ... — o hook point de inserção é entre b e c)
    .planning/phases/11-imagens-links/11-RESEARCH.md (seção "Hook Points — 4. updateLpAction")
    .planning/phases/11-imagens-links/11-PATTERNS.md (seção "actions.ts — Pattern Assignment" e "Shared Patterns — ActionResult retorno")
  </read_first>
  <action>
    Localizar em apps/web/src/lib/lps/actions.ts o ponto de inserção dentro de updateLpAction, branch VITE_SPA: imediatamente após o bloco if (!overridesParsed.success) { ... return { ok: false, error: ..., fieldErrors }; } e ANTES da linha que lê const existingValues = (ou const valuesUpdate =, conforme a implementação real).

    Inserir um loop de validação URL para overrides image/href: se overridesParsed.data.overrides for definido e não vazio, iterar sobre cada ov. Para ov.type === 'image' ou ov.type === 'href', tentar construir new URL(ov.value) dentro de try/catch — se o construtor lançar, retornar { ok: false, error: 'URL de override inválida ou malformada.' }. Se parsed.protocol não for 'http:' nem 'https:', retornar { ok: false, error: 'URL de override com protocolo não permitido.' }. O return deve ter o shape { ok: false, error: string } sem fieldErrors (o Alert existente no ViteSpaPreviewEditor exibe saveError dessa forma).

    Não importar nenhuma dependência externa para este loop — new URL() é built-in Node.js 18+. O loop não deve alterar o comportamento para overrides de type 'text' ou 'color'.

    Não modificar nenhum outro aspecto de updateLpAction — RBAC (requireWorkspaceRole), workspaceId da sessão, prisma.landingPage.update — tudo permanece inalterado.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "parsedUrl.protocol" apps/web/src/lib/lps/actions.ts retorna >= 1
    - grep -c "new URL(ov.value)" apps/web/src/lib/lps/actions.ts retorna >= 1
    - grep -c "javascript:" apps/web/src/lib/lps/actions.ts retorna 0 (blocklist não precisa de string literal — o check de protocol cobre tudo)
    - O bloco de validação está DEPOIS do if (!overridesParsed.success) e ANTES do acesso a existingValues/valuesUpdate (ordem correta)
    - pnpm --filter web tsc --noEmit retorna 0 erros TypeScript
    - Overrides de type 'text' e 'color' NÃO passam pelo bloco de validação URL (condição if ov.type === 'image' || ov.type === 'href')
  </acceptance_criteria>
  <done>updateLpAction rejeita overrides image/href com URL javascript:, data:, ftp: ou malformada; aceita http/https; TypeScript compila sem erros</done>
</task>

<task type="auto">
  <name>Tarefa 2: Export route VITE_SPA branch — D-11-04 (S3 image download + src rewrite)</name>
  <files>
    apps/web/src/app/api/lps/[lpId]/export/route.ts
  </files>
  <read_first>
    apps/web/src/app/api/lps/[lpId]/export/route.ts (ler completo — identificar: (a) a branch LIQUID com extractS3ImageUrls e rewriteImageSrcs para entender o padrão análogo, linhas ~99-170; (b) a branch VITE_SPA, especificamente buildOverrideInjection + injectOverrides + viteSpaArchive.append(finalHtml), linhas ~259-298; (c) onde S3_PUBLIC_BASE_URL é referenciado no arquivo; (d) o tipo e nome da variável do archiver no branch VITE_SPA)
    .planning/phases/11-imagens-links/11-RESEARCH.md (seção "Hook Points — 5. Export route" e a nota sobre "Assumptions Log — A1")
    .planning/phases/11-imagens-links/11-PATTERNS.md (seção "export/route.ts — Pattern Assignment" com o código completo da extensão)
  </read_first>
  <action>
    Localizar no branch VITE_SPA de route.ts o trecho: const injection = buildOverrideInjection(lpValues); const finalHtml = injectOverrides(themedHtml, injection); seguido por viteSpaArchive.append(Buffer.from(finalHtml, 'utf-8'), { name: 'index.html' }).

    Substituir o append final por um bloco processado: declarar let processedHtml = finalHtml. Se S3_PUBLIC_BASE_URL estiver definido (process.env.S3_PUBLIC_BASE_URL ?? '') e lpValues.overrides?.length, filtrar overrides de type:'image' cuja value começa com s3BaseUrl (predicado anti-SSRF — apenas URLs do bucket próprio são baixadas, identicamente ao LIQUID path). Para cada override de imagem S3 filtrado, fazer fetch(ov.value, { redirect: 'error' }) (anti-SSRF: rejeita redirects). Se resp.ok, buffer = Buffer.from(await resp.arrayBuffer()); extrair filename da URL via new URL(ov.value).pathname.split('/').at(-1); deduplicar filename se já usado (prefixar com índice). Adicionar ao ZIP via viteSpaArchive.append(buf, { name: 'assets/{filename}' }). Reescrever URL no HTML via processedHtml = processedHtml.split(ov.value).join('./assets/{filename}') — NUNCA usar .replace() (Pitfall 5: substitui apenas a primeira ocorrência). Erros individuais (URL inválida, rede) são silenciados com catch vazio — um asset que falha não aborta o export inteiro. Ao final, usar viteSpaArchive.append(Buffer.from(processedHtml, 'utf-8'), { name: 'index.html' }) no lugar do append original.

    Overrides de type:'image' com URL externa (não começa com s3BaseUrl) não entram no loop — permanecem com URL absoluta no HTML (D-11-04). Overrides de type:'href' nunca precisam de tratamento no export (link hrefs são inline no HTML, não arquivos para baixar).

    Se S3_PUBLIC_BASE_URL não estiver configurado (string vazia), o filtro de overrides retorna array vazio e processedHtml === finalHtml — comportamento igual ao de antes (sem regressão para ambientes sem S3).
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "imageOverrides" apps/web/src/app/api/lps/\[lpId\]/export/route.ts retorna >= 1
    - grep -c "processedHtml" apps/web/src/app/api/lps/\[lpId\]/export/route.ts retorna >= 3 (declaração + reescrita + append)
    - grep -c "redirect.*error" apps/web/src/app/api/lps/\[lpId\]/export/route.ts retorna >= 1 (anti-SSRF flag)
    - grep -c "split(ov.value)" apps/web/src/app/api/lps/\[lpId\]/export/route.ts retorna >= 1 (substituição global, não .replace())
    - grep -c "startsWith(s3BaseUrl)" apps/web/src/app/api/lps/\[lpId\]/export/route.ts retorna >= 1 (filtro SSRF)
    - O append final usa processedHtml (não finalHtml) — grep -c "Buffer.from(processedHtml" retorna >= 1
    - O append do finalHtml original foi REMOVIDO — grep -c "Buffer.from(finalHtml" retorna 0 na branch VITE_SPA (ou foi substituído)
    - pnpm --filter web tsc --noEmit retorna 0 erros TypeScript
  </acceptance_criteria>
  <done>Export route baixa imagens S3 de overrides para ./assets/ no ZIP com src reescrito relativo; URLs externas ficam absolutas; export TypeScript compila sem erros</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → updateLpAction (Server Action) | Override values cruzam a fronteira aqui; validação autoritativa acontece no servidor |
| Export route → S3 bucket externo | fetch de URLs de overrides; SSRF é a ameaça principal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | updateLpAction: URL override value | mitigate | Loop de validação: new URL(ov.value) + parsed.protocol allowlist (http/https). Bloqueia javascript:, data:, vbscript:, ftp:, qualquer não-http(s) e URLs malformadas. Posicionado ANTES do persist. |
| T-11-02-02 | Elevation of Privilege | export route: fetch de URLs de overrides | mitigate | Anti-SSRF duplo: (1) filtro startsWith(s3BaseUrl) — apenas URLs do bucket próprio são baixadas; (2) redirect:'error' — sem SSRF via redirect para endereços internos. Padrão idêntico ao LIQUID path já auditado. |
| T-11-02-03 | Tampering | export route: filename de URL S3 path | mitigate | Filename extraído de urlObj.pathname.split('/').at(-1) — o path já passou pela validação http(s) de updateLpAction. Deduplicação via Set evita colisão de nomes. Arquivo só vai para assets/ dentro do ZIP. |
| T-11-02-04 | Tampering | updateLpAction: overrides type 'text'/'color' | accept | O loop de validação URL tem condição if ov.type === 'image' || ov.type === 'href' — não afeta tipos existentes. Regressão impossível por design. |
| T-11-02-05 | Tampering | Script-tag breakout via URL em pf-overrides JSON | accept | escapeJsonForHtml (apply-shim.ts linha ~70-75) já unicode-escapa <>&. Validação de URL server-side bloqueia URLs com chars problemáticos antes de persistir. Cadeia de defesas já completa (verificado em RESEARCH.md Pitfall 4). |
</threat_model>

<verification>
Verificação integral do plano:

1. pnpm --filter web tsc --noEmit — 0 erros TypeScript (ambos os arquivos)
2. grep -c "parsedUrl.protocol" apps/web/src/lib/lps/actions.ts — >= 1
3. grep -c "imageOverrides" apps/web/src/app/api/lps/\[lpId\]/export/route.ts — >= 1
4. grep -c "redirect.*error" apps/web/src/app/api/lps/\[lpId\]/export/route.ts — >= 1
5. grep -c "split(ov.value)" apps/web/src/app/api/lps/\[lpId\]/export/route.ts — >= 1
6. grep -c "startsWith(s3BaseUrl)" apps/web/src/app/api/lps/\[lpId\]/export/route.ts — >= 1
</verification>

<success_criteria>
- updateLpAction: URL javascript:, data:, ftp: ou malformada em override image/href retorna {ok:false} sem persistir
- updateLpAction: URL https://cdn.s3.amazonaws.com/img.jpg em override image passa e persiste
- Export route: override {type:'image', value:'https://s3.bucket/img.jpg'} resulta em assets/img.jpg no ZIP e ./assets/img.jpg no index.html
- Export route: override {type:'image', value:'https://cdn.external.com/img.jpg'} mantém URL absoluta no HTML (não baixado)
- 0 erros TypeScript em pnpm --filter web tsc --noEmit
</success_criteria>

<output>
Após conclusão, criar .planning/phases/11-imagens-links/11-02-SUMMARY.md
</output>
