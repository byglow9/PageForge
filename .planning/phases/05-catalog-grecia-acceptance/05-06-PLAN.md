---
phase: 05-catalog-grecia-acceptance
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/lps/RepeaterBlock.tsx
  - apps/web/src/components/lps/ImageUploadField.tsx
  - apps/web/src/app/w/[slug]/lps/new/TemplatePickerForm.tsx
  - apps/web/src/app/w/[slug]/layout.tsx
  - apps/web/src/app/w/[slug]/page.tsx
autonomous: true
gap_closure: true
requirements:
  - GEN-04
  - CAT-04
must_haves:
  truths:
    - "Repeater fields with repeated names across blocks no longer produce duplicate React keys in the console"
    - "Editing an LP re-displays previously uploaded images instead of an empty dropzone, and saving without re-uploading preserves the image"
    - "The template picker shows the template name with no overlapping native cuid control"
    - "Workspace shell page content has padding from the <main> edges"
    - "The inline-applied LpForm and renderer.ts fixes (already committed in 8674702) are reviewed and confirmed: engine corpus is green"
  artifacts:
    - path: "apps/web/src/components/lps/RepeaterBlock.tsx"
      provides: "Composite unique keys for repeater field rendering"
    - path: "apps/web/src/components/lps/ImageUploadField.tsx"
      provides: "Initial-value hydration of the image preview in edit mode"
      contains: "publicUrl"
  key_links:
    - from: "apps/web/src/components/lps/ImageUploadField.tsx"
      to: "react-hook-form Controller field.value"
      via: "initialize previewUrl from field.value.publicUrl when present"
      pattern: "field\\.value|publicUrl"
---

<objective>
Close the remaining LP form/render and shell cosmetic gaps from 05-UAT.md (Tests 10, 11, 12, 1) and formally review the two inline fixes that were applied during UAT and committed in 8674702 ("save"): the LpForm `_lpName` read-from-getValues guard and the renderer.ts `resolveImageUrl` helper. Gaps: duplicate React keys in repeaters (major), image preview not hydrating on edit with data-loss risk (minor→major if data is lost), template picker cuid overlay (minor), <main> padding (cosmetic).

Purpose: Eliminate console key collisions that can corrupt repeater form state, prevent silent image data-loss on edit, clean up the picker + shell padding, and confirm the committed inline engine/form fixes are sound.

Output: Composite repeater keys, hydrated image field on edit, a single-control template picker, shell padding fix, and a confirmation that the committed inline engine/form fixes pass the engine corpus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-catalog-grecia-acceptance/05-UAT.md

<interfaces>
<!-- Contracts the executor needs. -->

RepeaterBlock (apps/web/src/components/lps/RepeaterBlock.tsx):
  Renders each field inside a repeater item with key={field.name} (lines ~175, 200, 230, 255 for
  richtext/button/image/text branches). `fieldId` is already the composite path
  `${repeaterName}.${index}.${field.name}` (used for register()/htmlFor). The Grécia template reuses field
  names (titulo, texto) across blocks → key collisions. Fix: use the composite `fieldId` (or `${index}.${field.name}`)
  as the React key on each branch's outer wrapper <div>.

ImageUploadField (apps/web/src/components/lps/ImageUploadField.tsx):
  Props: { name, slug, control: Control<any>, label, required }. Uses RHF Controller; on upload success calls
  field.onChange({ publicUrl, s3Key }) and setPreviewUrl(publicUrl). State previewUrl starts as "" (line ~74) and
  is never seeded from the existing field value → edit mode shows an empty dropzone even when the LP already has
  an image. The stored value shape is { publicUrl, s3Key } (string also supported for back-compat).
  The component renders <img src={previewUrl} …> (around line ~372) when previewUrl is set.

TemplatePickerForm (apps/web/src/app/w/[slug]/lps/new/TemplatePickerForm.tsx):
  Uses the Base UI Select (@/components/ui/select wraps @base-ui/react/select). SelectItem value={template.id},
  label "{name} (schema v{n})". UAT: the styled trigger shows the name but a second overlapping control shows the
  cuid. Investigate the Base UI Select wiring — SelectValue should render the selected item's label, not the
  raw value. Functionally the correct id is selected (generation used the right templateId).

Workspace shell layout (apps/web/src/app/w/[slug]/layout.tsx, line ~91):
  <main className="flex-1 overflow-auto">{children}</main> — no padding. Pages that set their own padding
  (e.g. /lps uses px-8 py-6) are unaffected; placeholder pages (dashboard) have content flush to the edges.

Already-committed inline fixes to REVIEW (commit 8674702 "save"; working tree is clean):
  - apps/web/src/components/lps/LpForm.tsx onSubmit: reads name from getValues("_lpName") + empty-name guard
    (toast "Informe um nome para a landing page."). Confirmed present in working tree.
  - src/engine/renderer.ts: resolveImageUrl(value) helper extracting .publicUrl from {publicUrl,s3Key}, applied
    at repeater (line ~90), top-level (line ~157), brand (line ~180) before sanitizeUrl; resolveButtonUrl already
    existed. Engine corpus reported 118/118 green during UAT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composite repeater keys + ImageUploadField edit hydration (Tests 10, 12)</name>
  <files>
    apps/web/src/components/lps/RepeaterBlock.tsx
    apps/web/src/components/lps/ImageUploadField.tsx
  </files>
  <read_first>
    apps/web/src/components/lps/RepeaterBlock.tsx
    apps/web/src/components/lps/ImageUploadField.tsx
  </read_first>
  <action>
    Part A — RepeaterBlock duplicate keys (major). In each field-rendering branch (richtext, button, image,
    text/color fallback) the outer wrapper <div> uses key={field.name}. The Grécia template reuses field names
    (titulo, texto) across repeater blocks, so keys collide → React "two children with the same key" warnings
    and potential field-state corruption. Replace key={field.name} with the already-computed composite path
    key={fieldId} (which is `${repeaterName}.${index}.${field.name}`) on all four branches (~lines 175, 200,
    230, 255). Do not change register()/htmlFor wiring — only the React key.

    Part B — ImageUploadField edit hydration (minor, data-loss risk). The component never seeds previewUrl from
    the existing field value, so edit mode shows an empty dropzone even when the LP has an image; saving without
    re-uploading risks losing the image. Hydrate the preview from the Controller field value:
    - Inside the Controller render (where `field` is available), if previewUrl is empty AND field.value holds an
      image, initialize the preview. Use a useEffect keyed on field.value that runs once: if previewUrl === "" and
      field.value is truthy, derive the url — field.value.publicUrl when field.value is an object {publicUrl,s3Key},
      or field.value itself when it is a non-empty string (back-compat) — and setPreviewUrl(url) plus set
      uploadState to a "done"/success-equivalent state so the preview <img> renders instead of the dropzone.
    - Crucially, do NOT call field.onChange during hydration (that would dirty the form / risk clobbering the value);
      only seed local preview state. The existing field.value must remain the source of truth so saving without
      re-upload preserves {publicUrl,s3Key}.
    - Verify the render branch shows the <img src={previewUrl}> when previewUrl is set in edit mode.
    Confirm the data-loss concern explicitly in the SUMMARY: after hydration, saving an edited LP without
    re-uploading the hero image must keep the original image (re-test UAT 12).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep -iE "RepeaterBlock|ImageUploadField" | head -10</automated>
  </verify>
  <done>All repeater field branches use composite (fieldId) React keys; ImageUploadField seeds its preview from an existing {publicUrl,s3Key} (or string) value in edit mode without calling onChange; saving without re-upload preserves the image.</done>
</task>

<task type="auto">
  <name>Task 2: Template picker single-control + <main> shell padding (Tests 10, 1)</name>
  <files>
    apps/web/src/app/w/[slug]/lps/new/TemplatePickerForm.tsx
    apps/web/src/app/w/[slug]/layout.tsx
  </files>
  <read_first>
    apps/web/src/app/w/[slug]/lps/new/TemplatePickerForm.tsx
    apps/web/src/components/ui/select.tsx
    apps/web/src/app/w/[slug]/layout.tsx
    apps/web/src/app/w/[slug]/page.tsx
  </read_first>
  <action>
    Part A — template picker overlapping cuid (minor). UAT: the Base UI Select shows the styled name in the
    trigger but a second overlapping control displays the raw cuid value. Inspect @/components/ui/select.tsx
    (Base UI @base-ui/react/select wrapper) and TemplatePickerForm's usage. The likely cause is the SelectValue/
    SelectTrigger rendering the value rather than the selected item's text, or a stray native control. Fix so a
    SINGLE control is visible showing the template name + "(schema v{n})". Options to evaluate:
      - Ensure SelectValue renders the selected item's label (Base UI SelectValue typically takes a render/children
        function receiving the value; map the value→template name from the templates prop and render the name).
      - Confirm no duplicate/native <select> is emitted by the wrapper for this usage.
    Keep value={template.id} as the option value (functionally correct — generation uses the id). The visible text
    must be the name, not the cuid.

    Part B — shell padding (cosmetic, Test 1). In apps/web/src/app/w/[slug]/layout.tsx line ~91, the <main> has
    no padding, so placeholder pages (the dashboard /w/[slug]/page.tsx) render flush to the edges, while pages
    that set their own padding (/lps uses px-8 py-6) are fine. To avoid double-padding the self-padded pages,
    take the LOWER-RISK path: add padding to the diagnosed offender — the dashboard page wrapper in
    /w/[slug]/page.tsx (line ~23 wrapper) — rather than to the shared <main>. If you instead choose to add p-6
    to <main>, you MUST also remove the now-redundant px-8/py-6 from the self-padded pages (/lps) to prevent
    double padding and must not regress the /lps two-panel h-full flex layout. Pick the page-wrapper approach
    unless a broader shell padding is explicitly preferred; document the choice in the SUMMARY.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep -iE "TemplatePickerForm|layout|w/\[slug\]/page" | head -10</automated>
  </verify>
  <done>Template picker shows a single control displaying the template name (no cuid overlap); workspace shell placeholder page content has edge padding without double-padding /lps; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Review + confirm the committed inline LpForm/renderer fixes (Tests 10, 11)</name>
  <files>
    apps/web/src/components/lps/LpForm.tsx
    src/engine/renderer.ts
  </files>
  <read_first>
    apps/web/src/components/lps/LpForm.tsx
    src/engine/renderer.ts
    src/engine/renderer.test.ts
  </read_first>
  <action>
    The two BLOCKER/major fixes from Tests 10 and 11 were applied inline during UAT and are ALREADY COMMITTED in
    git (commit 8674702 "save"); the working tree is clean. Do NOT re-apply or duplicate them — this task is a
    REVIEW/confirmation pass to formalize them.

    Step 1 — confirm LpForm onSubmit: verify it reads the LP name from getValues("_lpName") (because the derived
    Zod schema strips the unknown _lpName key) and guards an empty name with a toast before submitting, for BOTH
    generate and edit modes. Confirm the destructure of _lpName out of the parsed `data` so it is not sent as a
    field value. This is the correct fix per the UAT diagnosis (schema-derive.ts z.object strips unknown keys).
    If anything is loose/incomplete (e.g. guard missing on edit mode, or _lpName leaking into fieldValues),
    tighten it minimally. Otherwise leave as-is and record "confirmed sound" in the SUMMARY.

    Step 2 — confirm renderer.ts resolveImageUrl: verify the helper extracts .publicUrl from {publicUrl,s3Key}
    (and passes strings through for back-compat) and is applied before sanitizeUrl at all three image points —
    repeater (~line 90), top-level (~line 157), and brand (~line 180). Confirm image fields no longer stringify
    to "[object Object]".

    Step 3 — run the engine test corpus to confirm green (the 118-test security/render corpus). From the repo
    root run the engine unit tests. They must pass after these fixes. If any fail, fix the root cause minimally.

    No new behavior is introduced here beyond confirming/solidifying the committed fixes; do not refactor
    unrelated code.
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -25</automated>
  </verify>
  <done>LpForm _lpName guard confirmed sound for generate+edit; renderer.ts resolveImageUrl confirmed applied at repeater/top-level/brand; engine corpus passes (target 118/118); findings recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LP form values → generateLpAction/updateLpAction | _lpName is read from form state, not the parsed payload; field values still pass through server-side Zod validation |
| Image field value → LiquidJS render → static HTML | resolveImageUrl extracts publicUrl, then sanitizeUrl enforces safe schemes (D-12) before injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering / XSS | renderer.ts image url resolution | mitigate | resolveImageUrl output still passes through sanitizeUrl (same javascript:/data: blocking as button fields, D-12); engine corpus re-run confirms |
| T-05-06-02 | Data integrity (loss) | ImageUploadField edit hydration | mitigate | Hydrate preview from field.value WITHOUT calling field.onChange, so the stored {publicUrl,s3Key} remains source of truth; re-test UAT 12 save-without-reupload |
| T-05-06-03 | Information Disclosure | template picker cuid display | accept | A cuid is an opaque internal id, not sensitive; fix is cosmetic (show name) — no data-exposure change |
</threat_model>

<verification>
1. `cd apps/web && npx tsc --noEmit 2>&1 | grep -v node_modules` — no new errors.
2. Re-run UAT Test 10: generate a Grécia LP with multiple repeaters → no "two children with the same key" console warnings; template picker shows the name (no cuid overlap).
3. Re-run UAT Test 12: edit an LP with a hero image → the image preview is shown (not empty dropzone); save without re-uploading → preview still shows the original image.
4. Re-run UAT Test 11: image fields render real URLs (no [object Object]).
5. `pnpm test` — engine corpus passes (target 118/118).
6. Visual: dashboard shell content has padding.
</verification>

<success_criteria>
- No duplicate React keys for repeated repeater field names (UAT 10 fixed).
- ImageUploadField hydrates the existing image on edit and preserves it on save-without-reupload (UAT 12 fixed; data-loss risk closed).
- Template picker shows a single control with the template name (UAT 10 picker fixed).
- <main>/dashboard padding cosmetic resolved (UAT 1).
- Committed inline LpForm + renderer.ts fixes reviewed and confirmed; engine corpus green (UAT 10, 11).
</success_criteria>

<output>
After completion, create `.planning/phases/05-catalog-grecia-acceptance/05-06-SUMMARY.md`
</output>
