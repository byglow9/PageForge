---
phase: 05-catalog-grecia-acceptance
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/templates/TemplateEditor.tsx
  - apps/web/src/app/w/[slug]/lps/page.tsx
  - apps/web/src/components/catalog/CatalogGrid.tsx
autonomous: true
gap_closure: true
requirements:
  - TPL-05
  - CAT-04
must_haves:
  truths:
    - "Saving a brand-new template and clicking Save a second time updates the same template instead of creating a duplicate"
    - "The template editor shows a single primary 'Save Template' CTA per screen state"
    - "When the LP catalog is empty, only one 'Generate LP' CTA is shown"
    - "The catalog search bar has vertical breathing room below the header separator"
  artifacts:
    - path: "apps/web/src/components/templates/TemplateEditor.tsx"
      provides: "Post-create redirect (or mode→edit switch) so repeat saves call updateTemplateAction; single Save CTA"
      contains: "router"
    - path: "apps/web/src/app/w/[slug]/lps/page.tsx"
      provides: "Single Generate LP CTA pattern coordinated with CatalogGrid empty state"
  key_links:
    - from: "apps/web/src/components/templates/TemplateEditor.tsx"
      to: "/w/[slug]/templates/[id]/edit"
      via: "router.replace after createTemplateAction.ok"
      pattern: "router\\.(push|replace)"
---

<objective>
Close the template-editor and duplicate-CTA gaps from 05-UAT.md (Test 9 + Test 2). The major bug: TemplateEditor stays in mode='create' after a successful create, so a second Save click creates a SECOND template. Plus two minor duplicate-CTA gaps: two 'Save Template' buttons (header + footer) and two 'Generate LP' buttons when the catalog is empty (page header + empty-state). Also the cosmetic catalog search-bar spacing (Test 2) — grouped here because it touches the same CatalogGrid file.

Purpose: Eliminate accidental duplicate templates and converge on a single-primary-CTA-per-state pattern across the template editor and the empty catalog, plus tidy the search-bar spacing.

Output: A template editor that transitions to edit mode after first save, a single Save Template / single Generate LP CTA per state, and a search bar with breathing room.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-catalog-grecia-acceptance/05-UAT.md

<interfaces>
<!-- Contracts the executor needs. -->

TemplateEditor (apps/web/src/components/templates/TemplateEditor.tsx):
  Props: { slug, mode: "create"|"edit", initialTemplate?: { id, name, markup, schema, metadataOverlay, schemaVersion } }
  handleSave (lines ~115-147): in mode 'create' calls createTemplateAction(slug, {name, markup, metadataOverlay});
    in 'edit' calls updateTemplateAction(slug, {id: initialTemplate!.id, …}).
    On success sets savedSchemaVersion + warnings + toast.success(`Template saved — schema v${n}`).
    BUG: after a successful create it does NOT redirect or switch mode → a 2nd click creates a 2nd template.
  createTemplateAction result shape: ActionResult<{ id: string; schemaVersion: number; warnings: string[] }>
    (confirm exact field name for the new id in @/lib/templates/actions — likely result.data.id).
  Two Save Template buttons render: header toolbar (line ~165) and bottom action bar (line ~333), both
    onClick={handleSave} disabled={isPending || !name.trim()}.
  Edit-mode route: /w/[slug]/templates/[id]/edit.

LP catalog Generate-LP duplication:
  apps/web/src/app/w/[slug]/lps/page.tsx (line ~60-67): page-header "Generate LP" Link, shown when canCreate.
  apps/web/src/components/catalog/CatalogGrid.tsx (line ~194-201): empty-state "Generate LP" Link inside the
    isWorkspaceEmpty branch, shown when canCreate. Both visible simultaneously when the workspace is empty.

CatalogGrid search spacing (apps/web/src/components/catalog/CatalogGrid.tsx, lines ~165, 167):
  Right panel is `flex-1 px-6 py-0`; SearchBar wrapper is `pt-0 pb-2` → search bar sits flush under the header
  border. Needs vertical breathing room (e.g. pt-4 on the search wrapper).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix template double-save + collapse to one Save CTA (Test 9)</name>
  <files>apps/web/src/components/templates/TemplateEditor.tsx</files>
  <read_first>
    apps/web/src/components/templates/TemplateEditor.tsx
    apps/web/src/lib/templates/actions.ts
  </read_first>
  <action>
    Part A — stop duplicate template creation (major). After createTemplateAction returns ok in mode 'create',
    the editor must stop being able to create again. Preferred fix per UAT diagnosis: redirect to the edit route
    so subsequent saves load in mode='edit' and call updateTemplateAction.
      In handleSave, in the create branch, on result.ok: capture the new id (result.data.id — confirm the field
      name against @/lib/templates/actions), keep the success toast, then call
      router.replace(`/w/${slug}/templates/${result.data.id}/edit`).
      Use next/navigation useRouter (import useRouter, instantiate const router = useRouter()).
      Use router.replace (not push) so the back button does not return to the now-stale create form.
      Keep the warnings handling (saveWarnings) before redirecting if the warnings need to be shown — since
      replace navigates away, prefer to redirect immediately after toast; the edit page will re-surface schema
      warnings on its next save. If preserving warnings matters, the acceptable alternative is the in-place
      mode switch described next.
      Acceptable alternative (no navigation): add local state `const [createdId, setCreatedId] = useState<string|null>(null)`
      and an effective mode `const effectiveMode = mode === "create" && createdId ? "edit" : mode`. On create
      success setCreatedId(result.data.id); branch handleSave on effectiveMode and pass createdId as the id to
      updateTemplateAction. This keeps the warnings panel visible. Either approach is fine — pick redirect unless
      keeping the warnings panel on first save is required by UI-SPEC.

    Part B — single Save CTA (minor). Remove ONE of the two duplicate 'Save Template' buttons. Keep the bottom
    action-bar button (line ~333, alongside Cancel + version badge) as the primary save, and remove the header
    toolbar Save button (line ~165), leaving the breadcrumb in the header. OR, if UI-SPEC specifies the header
    as primary, keep the header button and remove the footer one — but choose ONE and apply consistently with
    the Generate LP decision in Task 2 (footer-primary recommended for symmetry with form pages). Document the
    chosen pattern in the SUMMARY.

    Do NOT touch the toast copy in this task — the toast feedback gap is cosmetic and flagged as needing user
    clarification (see SUMMARY note); leave `Template saved — schema v${n}` as-is.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep -i "TemplateEditor" | head -10</automated>
  </verify>
  <done>After first successful create the editor redirects to the edit route (or switches to edit mode) so repeat saves update the same template; exactly one 'Save Template' button renders; tsc clean for TemplateEditor.</done>
</task>

<task type="auto">
  <name>Task 2: Single Generate LP CTA + search-bar spacing in the catalog (Test 2)</name>
  <files>
    apps/web/src/app/w/[slug]/lps/page.tsx
    apps/web/src/components/catalog/CatalogGrid.tsx
  </files>
  <read_first>
    apps/web/src/app/w/[slug]/lps/page.tsx
    apps/web/src/components/catalog/CatalogGrid.tsx
  </read_first>
  <action>
    Part A — single Generate LP CTA. When the workspace has zero LPs, both the page-header 'Generate LP' Link
    and the empty-state 'Generate LP' Link are visible — two competing primary CTAs. Converge on a
    single-primary-CTA-per-state rule consistent with the Save Template decision in Task 1.
      Option A (simplest, recommended): in CatalogGrid.tsx empty-state branch (isWorkspaceEmpty, line ~194-201),
        remove the empty-state 'Generate LP' Link, keeping the descriptive copy. The header CTA in page.tsx
        remains the single primary action. This requires no prop plumbing.
      Option B: keep the empty-state CTA and hide the header CTA only while the workspace is empty. page.tsx
        already knows `lps.length === 0`, so compute `const isWorkspaceEmpty = lps.length === 0` and conditionally
        render the header CTA.
    Pick Option A unless UI-SPEC mandates the empty-state CTA as primary. Keep `canCreate` gating intact on the
    surviving CTA. Document the chosen pattern in the SUMMARY so it matches the Save Template pattern.

    Part B — search-bar spacing (cosmetic, Test 2). In CatalogGrid.tsx the SearchBar wrapper is `pt-0 pb-2`
    (line ~167) inside the right panel `flex-1 px-6 py-0` (line ~165), so the search bar is flush under the
    header border. Add top breathing room: change the search wrapper to `pt-4 pb-2` (or add py to the right
    panel — but do not regress the two-panel flex/h-full layout).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep -iE "lps/page|CatalogGrid" | head -10</automated>
  </verify>
  <done>With an empty workspace, exactly one 'Generate LP' CTA renders; canCreate gating preserved; catalog search bar has top breathing room; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template editor → createTemplateAction/updateTemplateAction | Repeat-save now routes to update; no change to server-side authz (gated by template create/update permissions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering (duplicate writes) | TemplateEditor repeat create | mitigate | After create, redirect to edit route (or switch mode) so subsequent saves are updates; server-side create remains permission-gated |
| T-05-05-02 | DoS (accidental row spam) | createTemplateAction repeated | accept | Client-side mode switch removes the practical duplicate path; optional server unique [workspaceId,name] noted as future hardening, not required for this gap |
</threat_model>

<verification>
1. `cd apps/web && npx tsc --noEmit 2>&1 | grep -v node_modules` — no new errors.
2. Re-run UAT Test 9 duplicate-save: create a new template, click Save, click Save again → only ONE template exists in the templates list; second save updates (schema version unchanged or incremented per update logic, not a new row).
3. Template editor renders a single Save Template button.
4. Re-run UAT Test 2: open /w/[slug]/lps with an empty workspace → only one 'Generate LP' CTA visible; search bar has top breathing room.
</verification>

<success_criteria>
- Repeat Save on a new template updates the same record (UAT 9 duplicate-save fixed).
- Single 'Save Template' CTA per state (UAT 9 duplicate-CTA fixed).
- Single 'Generate LP' CTA in the empty catalog (UAT 2 duplicate-CTA fixed).
- Catalog search bar has vertical breathing room (UAT 2 cosmetic fixed).
- Save Template and Generate LP follow the same single-primary-CTA pattern, documented in SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/05-catalog-grecia-acceptance/05-05-SUMMARY.md`
</output>
