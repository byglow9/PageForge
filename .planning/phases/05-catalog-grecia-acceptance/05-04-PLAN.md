---
phase: 05-catalog-grecia-acceptance
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/catalog/LpCatalogCard.tsx
  - apps/web/src/lib/catalog/actions.ts
  - apps/web/prisma/migrations/0005_catalog_folders_tags/migration.sql
autonomous: true
gap_closure: true
requirements:
  - CAT-02
  - CAT-03
must_haves:
  truths:
    - "The LP catalog card kebab shows 'Move to folder…' and 'Edit tags…' as the topmost items, fully visible (not clipped by the card)"
    - "Selecting 'Move to folder…' opens the MoveLpDialog; selecting 'Edit tags…' opens the TagInputDialog"
    - "Deleting a folder re-parents its LPs and subfolders to root and removes the folder without a 'Failed to delete folder' error"
    - "A committed Prisma migration exists for the Phase 5 catalog tables (folder, tag, lp_tag, landing_page.folderId) so clean environments match db push"
  artifacts:
    - path: "apps/web/src/components/catalog/LpCatalogCard.tsx"
      provides: "Portaled kebab menu (DropdownMenu) escaping the card's overflow-hidden"
      contains: "DropdownMenu"
    - path: "apps/web/src/lib/catalog/actions.ts"
      provides: "deleteFolderAction re-parenting via Prisma updateMany (camelCase field names)"
      contains: "updateMany"
    - path: "apps/web/prisma/migrations/0005_catalog_folders_tags/migration.sql"
      provides: "Phase 5 catalog schema migration"
      contains: "CREATE TABLE"
  key_links:
    - from: "apps/web/src/components/catalog/LpCatalogCard.tsx"
      to: "MoveLpDialog / TagInputDialog"
      via: "DropdownMenuItem onClick opens the dialog"
      pattern: "setMoveOpen|setTagDialogOpen"
    - from: "apps/web/src/lib/catalog/actions.ts"
      to: "landing_page / folder tables"
      via: "tx.landingPage.updateMany + tx.folder.updateMany"
      pattern: "updateMany\\(\\{\\s*where:\\s*\\{\\s*workspaceId"
---

<objective>
Close the two major catalog blockers from 05-UAT.md: (1) the LpCatalogCard kebab clips its top items ('Move to folder…', 'Edit tags…') because it is a non-portaled absolute `bottom-full` div inside a `Card` with `overflow-hidden`; (2) `deleteFolderAction` fails because its raw SQL UPDATEs reference snake_case columns that do not exist (physical columns are camelCase). Also generate the missing Phase 5 catalog migration so clean environments do not break with "relation does not exist".

Purpose: This single plan unblocks UAT Tests 4 (move LP), 5 (tags + clipping), 7 (filter by tag), 16 (delete folder), and 18 (user-story coverage) — the highest-leverage fixes in the phase.

Output: Portaled kebab on the catalog card, working non-destructive folder delete, and a committed catalog migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-catalog-grecia-acceptance/05-UAT.md

<interfaces>
<!-- Contracts the executor needs; do not re-explore the codebase. -->

DropdownMenu (already installed at apps/web/src/components/ui/dropdown-menu.tsx, used by FolderContextMenu in 05-03):
  Exports: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator
  DropdownMenuContent portals to document.body → escapes the Card's `overflow-hidden`.
  Reference usage: apps/web/src/components/catalog/FolderContextMenu.tsx (the 05-03 upgrade).

LpCatalogCard current kebab (apps/web/src/components/catalog/LpCatalogCard.tsx):
  Lines ~304–388: a `relative` wrapper + <button> toggling `menuOpen`, then a fixed-inset backdrop
  and a `<div className="absolute right-0 bottom-full mb-1 z-20 …">` containing 5 buttons:
    Move to folder… (opens setMoveOpen) → Edit tags… (opens setTagDialogOpen) → separator
    → Duplicate (handleDuplicate) → Export ZIP (handleExportZip) → separator → Delete landing page (setDeleteOpen)
  State already present: menuOpen, isDuplicating, isExporting, setMoveOpen, setTagDialogOpen, setDeleteOpen.
  The Card root wrapping this is from ui/card.tsx which hardcodes `overflow-hidden` (line 15) — do NOT edit card.tsx.

deleteFolderAction (apps/web/src/lib/catalog/actions.ts, lines ~168–229):
  Runs inside prisma.$transaction(async (tx) => …) with set_config('app.current_workspace_id', …) for RLS.
  BUG: two `tx.$executeRaw` UPDATEs use snake_case columns:
    UPDATE landing_page SET folder_id = NULL WHERE workspace_id = … AND folder_id = …   (lines ~200-205)
    UPDATE folder       SET parent_id = NULL WHERE workspace_id = … AND parent_id = …    (lines ~208-213)
  Physical Postgres columns are camelCase (Prisma schema has @@map on tables only, no @map on columns):
    landing_page.folderId, landing_page.workspaceId, folder.parentId, folder.workspaceId.
  The folder.findFirst guard (line ~192) and tx.folder.delete (line ~216) already use Prisma field names and are CORRECT.

Prisma model field names (apps/web/prisma/schema.prisma — confirmed):
  LandingPage: id, workspaceId, folderId (nullable), … @@map("landing_page")
  Folder:      id, workspaceId, parentId (nullable), name, … @@map("folder")
  Prisma client delegates available on tx: tx.landingPage, tx.folder.

Migration state (apps/web/prisma/migrations/):
  Existing: 0001_multi_tenancy_foundation, 0002_rls_real_tenant_tables,
            0003_invitation_token_rls_lookup, 0004_add_template_brand_config.
  NO migration for the catalog tables (folder, tag, lp_tag) or landing_page.folderId — UAT used `db push`.
  migration_lock.toml provider = postgresql.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portalize the LpCatalogCard kebab via DropdownMenu (fixes clipping — Tests 4/5/7/18)</name>
  <files>apps/web/src/components/catalog/LpCatalogCard.tsx</files>
  <read_first>
    apps/web/src/components/catalog/LpCatalogCard.tsx
    apps/web/src/components/catalog/FolderContextMenu.tsx
    apps/web/src/components/ui/dropdown-menu.tsx
  </read_first>
  <action>
    Replace the custom kebab implementation (the `relative` wrapper containing the toggle button, the
    `fixed inset-0` backdrop, and the `absolute right-0 bottom-full` menu div — lines ~304-388) with the
    shadcn/Base UI DropdownMenu so the menu portals to document.body and is no longer clipped by the
    Card's `overflow-hidden`. Root cause (UAT, diagnosed): the menu opens upward (`bottom-full`) inside an
    `overflow-hidden` Card, so the top items 'Move to folder…' and 'Edit tags…' are cut off.

    Concrete edits:
    - Add imports from "@/components/ui/dropdown-menu": DropdownMenu, DropdownMenuTrigger,
      DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator. Mirror the import style used by
      FolderContextMenu.tsx.
    - Remove the `menuOpen` useState and the `setMenuOpen(...)` calls — DropdownMenu manages its own
      open state. Keep `isDuplicating` and `isExporting` (still used to disable the trigger + Export item).
    - Render:
        DropdownMenu
          DropdownMenuTrigger render/asChild → the existing kebab <button> (keep aria-label="Landing page options",
            keep the disabled={isDuplicating || isExporting}, keep the Loader2/MoreHorizontal icon swap).
          DropdownMenuContent align="end"
            DropdownMenuItem onClick={() => setMoveOpen(true)} → "Move to folder…"
            DropdownMenuItem onClick={() => setTagDialogOpen(true)} → "Edit tags…"
            DropdownMenuSeparator
            DropdownMenuItem onClick={handleDuplicate} → "Duplicate"
            DropdownMenuItem onClick={handleExportZip} disabled={isExporting} → "Export ZIP" (keep the Exporting… spinner state)
            DropdownMenuSeparator
            DropdownMenuItem onClick={() => setDeleteOpen(true)} className="text-red-600 focus:text-red-600 focus:bg-red-50" → "Delete landing page"
    - In handleDuplicate and handleExportZip, remove the now-obsolete `setMenuOpen(false)` lines (the menu
      closes itself on item select). Do NOT change the dialog wiring (DeleteLpDialog, MoveLpDialog, TagInputDialog
      and their open-state props remain exactly as-is).
    - Match FolderContextMenu's trigger pattern for how the button is passed to DropdownMenuTrigger (render prop
      vs asChild) — check which one dropdown-menu.tsx expects (Base UI uses `render`, Radix uses `asChild`).

    Do NOT edit apps/web/src/components/ui/card.tsx — the overflow-hidden is shared/global; portaling is the
    correct fix per the UAT diagnosis (preferred option over forcing overflow-visible).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep -i "LpCatalogCard\|dropdown" | head -10</automated>
  </verify>
  <done>LpCatalogCard kebab uses DropdownMenu (portaled); 'Move to folder…' and 'Edit tags…' are the top two items and render outside the card without clipping; tsc shows no LpCatalogCard/dropdown errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix deleteFolderAction raw SQL → Prisma updateMany (fixes Test 16)</name>
  <files>apps/web/src/lib/catalog/actions.ts</files>
  <read_first>
    apps/web/src/lib/catalog/actions.ts
  </read_first>
  <action>
    In deleteFolderAction (inside the prisma.$transaction callback), replace the two broken `tx.$executeRaw`
    UPDATE statements (the ones using snake_case `folder_id`, `parent_id`, `workspace_id`) with Prisma
    `updateMany` calls that use the camelCase field names. Root cause (UAT, diagnosed): the physical columns
    are camelCase, so the raw SQL throws `column "folder_id" does not exist`, swallowed by the generic catch
    → "Failed to delete folder" toast.

    Replace the LP re-parent UPDATE with:
      await tx.landingPage.updateMany({ where: { workspaceId, folderId }, data: { folderId: null } });
    Replace the subfolder re-parent UPDATE with:
      await tx.folder.updateMany({ where: { workspaceId, parentId: folderId }, data: { parentId: null } });

    Keep everything else unchanged: the set_config('app.current_workspace_id', …) RLS line (line ~189), the
    folder.findFirst guard (line ~192) that throws FOLDER_NOT_FOUND, and the final tx.folder.delete (line ~216).
    Update the inline comments referencing "$executeRaw" / "raw" to reflect the updateMany approach so the
    header doc stays accurate.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep -i "catalog/actions" | head -10</automated>
  </verify>
  <done>deleteFolderAction re-parents LPs and subfolders via tx.landingPage.updateMany and tx.folder.updateMany using camelCase field names; no remaining snake_case column references in the action; tsc clean for actions.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the missing Phase 5 catalog migration</name>
  <files>apps/web/prisma/migrations/0005_catalog_folders_tags/migration.sql</files>
  <read_first>
    apps/web/prisma/schema.prisma
    apps/web/prisma/migrations/0004_add_template_brand_config/migration.sql
    apps/web/prisma/migrations/migration_lock.toml
  </read_first>
  <action>
    The migrations stop at 0004, but the catalog tables (folder, tag, lp_tag) and landing_page.folderId were
    applied to the dev DB via `db push` — so a clean environment (CI, new clone) running migrate deploy will
    NOT have them, and deleteFolderAction (and all catalog actions) will fail with "relation does not exist".
    Generate the committed migration that captures the catalog schema delta.

    Preferred approach — let Prisma diff the schema against the migration history:
      From apps/web: `npx prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/schema.prisma --script > /tmp/catalog-diff.sql`
      Review /tmp/catalog-diff.sql — it should contain only the catalog delta (CREATE TABLE folder, tag, lp_tag;
      ALTER TABLE landing_page ADD COLUMN folderId; indexes; foreign keys; RLS policies if the schema/prior
      migrations established a pattern). If it contains unrelated drift, investigate before proceeding.
      Then create the migration directory apps/web/prisma/migrations/0005_catalog_folders_tags/ and write the
      reviewed SQL to migration.sql.

    Alternative if migrate diff is unavailable or the DB is the source of truth:
      Use `npx prisma migrate dev --name catalog_folders_tags --create-only` against a clean shadow DB to let
      Prisma author the migration, then move/rename the generated folder to 0005_catalog_folders_tags to match
      the existing 4-digit prefix convention.

    IMPORTANT — RLS parity: tables 0001-0003 establish row-level security on tenant tables. Inspect whether
    the catalog tables need matching RLS policies (the prior migrations + tenant-db.ts set_config pattern imply
    yes). If the schema-diff output omits RLS that the prior migrations applied to peer tenant tables, add the
    equivalent ENABLE ROW LEVEL SECURITY + CREATE POLICY statements for folder, tag, lp_tag, mirroring the
    0002_rls_real_tenant_tables pattern keyed on app.current_workspace_id. Document in the SUMMARY exactly what
    RLS was added vs. inherited.

    Verify the migration applies cleanly against a fresh database:
      Use a throwaway/shadow DB (or DROP SCHEMA + recreate locally) and run `npx prisma migrate deploy` 0001→0005.
      Confirm no errors and that folder/tag/lp_tag exist with folderId on landing_page.

    Do NOT modify schema.prisma in this task — the schema is already correct; you are only backfilling the
    migration artifact to match it.
  </action>
  <verify>
    <automated>ls apps/web/prisma/migrations/0005_catalog_folders_tags/migration.sql && grep -iE "create table.*(folder|tag|lp_tag)|landing_page.*folder" apps/web/prisma/migrations/0005_catalog_folders_tags/migration.sql | head</automated>
  </verify>
  <done>0005_catalog_folders_tags/migration.sql exists and creates folder/tag/lp_tag + landing_page.folderId (with RLS parity where prior migrations apply it); migrate deploy 0001→0005 applies cleanly on a fresh DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client kebab → Server Action | DropdownMenu items invoke the same already-gated server actions (move/tag/duplicate/delete); no new client-trusted input |
| Server Action → Postgres (raw vs ORM) | deleteFolderAction crosses into the DB; using Prisma updateMany keeps parameter binding + field-name safety |
| migrate deploy → tenant tables | New catalog tables must enforce the same workspace_id RLS as peer tenant tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering / Injection | deleteFolderAction DB writes | mitigate | Replace ad-hoc raw SQL with Prisma updateMany (parameterized, field-name-checked); workspaceId still sourced from requireWorkspaceRole session, never client |
| T-05-04-02 | Elevation / cross-tenant | catalog tables RLS in 0005 migration | mitigate | Migration must ENABLE ROW LEVEL SECURITY + policy keyed on app.current_workspace_id for folder/tag/lp_tag, mirroring 0002_rls_real_tenant_tables; verified by clean-DB migrate deploy |
| T-05-04-03 | Information Disclosure | DropdownMenu portal content | accept | Menu renders the same data already on the card; portaling changes only DOM mount point, not data exposure |
</threat_model>

<verification>
1. `cd apps/web && npx tsc --noEmit 2>&1 | grep -v node_modules` — no new errors.
2. Re-run UAT Test 4: card kebab shows 'Move to folder…' as the top item, opens MoveLpDialog, LP moves and folder badge appears.
3. Re-run UAT Test 5: card kebab shows 'Edit tags…', opens TagInput, no clipping.
4. Re-run UAT Test 16: deleting a folder with LPs+subfolders re-parents them to root and removes the folder (no error toast).
5. Re-run UAT Test 7: after adding tags, a tag pill appears in the FilterBar and filters the grid.
6. `cd apps/web && npx prisma migrate deploy` on a fresh DB applies 0001→0005 with no errors.
</verification>

<success_criteria>
- LpCatalogCard kebab uses a portaled DropdownMenu; 'Move to folder…' and 'Edit tags…' are visible and functional (UAT 4/5 pass, 7 unblocked).
- deleteFolderAction succeeds (UAT 16 passes); no snake_case column references remain.
- 0005_catalog_folders_tags migration committed and applies cleanly on a fresh DB.
- User-story coverage (UAT 18) reaches organizar + classificar + buscar + filtrar + reutilizar end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/05-catalog-grecia-acceptance/05-04-SUMMARY.md`
</output>
