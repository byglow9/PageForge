---
phase: 02-multi-tenancy-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/web/prisma/schema.prisma
  - apps/web/prisma/migrations/0001_multi_tenancy_foundation/migration.sql
  - apps/web/src/lib/db/tenant-db.ts
  - apps/web/src/lib/workspaces/guards.ts
  - apps/web/src/lib/workspaces/schema.ts
  - apps/web/src/lib/workspaces/actions.ts
  - apps/web/src/app/workspaces/new/page.tsx
  - apps/web/src/app/w/[slug]/layout.tsx
  - apps/web/src/app/w/[slug]/page.tsx
  - apps/web/tests/permissions.test.ts
  - apps/web/tests/workspaces.test.ts
  - apps/web/tests/tenant-isolation.test.ts
autonomous: true
requirements:
  - WS-02
  - WS-04
  - WS-05
requirements_addressed:
  - WS-02
  - WS-04
  - WS-05
must_haves:
  truths:
    - "WS-02: a verified user can create a workspace with name and slug"
    - "WS-04: owner/admin/editor/viewer roles gate actions through a shared permission matrix"
    - "WS-05: TenantIsolationProbe rows are inaccessible across workspace boundaries by direct ID"
    - "D-04: workspace creation is explicit after signup; no personal workspace is auto-created"
    - "D-05: active workspace is resolved from `/w/{slug}` and supports multi-workspace membership"
    - "D-08: owner/admin/editor/viewer role behavior is tested"
    - "D-09: owner/admin can manage members and settings; editor/viewer cannot"
    - "D-10: viewer has read/preview/export permissions but no create/edit/duplicate permissions"
    - "D-11: editor has content permissions but no workspace settings/member management permissions"
    - "D-12: every workspace guard validates slug plus server session membership before returning context"
    - "D-13: migration enables and forces RLS on TenantIsolationProbe"
    - "D-14: tenant-db helper sets app.current_workspace_id inside transaction and injects workspaceId"
  artifacts:
    - path: "apps/web/src/lib/workspaces/guards.ts"
      provides: "server-side session, workspace membership, and role guards"
    - path: "apps/web/src/lib/db/tenant-db.ts"
      provides: "central tenant-scoped Prisma helper with SET LOCAL and workspaceId injection"
    - path: "apps/web/prisma/migrations/0001_multi_tenancy_foundation/migration.sql"
      provides: "workspace schema migration plus RLS policy contract"
---

<decision_traceability>
- D-04: explicit workspace creation.
- D-05: multi-workspace membership with active workspace from `/w/{slug}`.
- D-08 through D-11: RBAC role/permission matrix.
- D-12: session-derived tenant context validated against membership.
- D-13: app-level scoping plus PostgreSQL RLS.
- D-14: central tenant data layer, not hand-written per-query filters.
</decision_traceability>

<objective>
Implement workspace creation, slug-based workspace context, RBAC guards, and the central tenant
data layer with PostgreSQL RLS as the non-forgettable backstop.
</objective>

<context>
@/home/glow/Documentos/projetos/PageForge/.planning/ROADMAP.md
@/home/glow/Documentos/projetos/PageForge/.planning/REQUIREMENTS.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-CONTEXT.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-RESEARCH.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-PATTERNS.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace actions, schemas, and explicit creation page</name>
  <files>apps/web/src/lib/workspaces/schema.ts, apps/web/src/lib/workspaces/actions.ts, apps/web/src/app/workspaces/new/page.tsx, apps/web/src/app/w/[slug]/layout.tsx, apps/web/src/app/w/[slug]/page.tsx</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/apps/web/src/lib/auth/auth.ts
    - /home/glow/Documentos/projetos/PageForge/apps/web/prisma/schema.prisma
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-CONTEXT.md
  </read_first>
  <action>
    Create Zod schemas for workspace name and slug. Implement `createWorkspaceAction(input)`
    that requires a verified server session, creates the workspace, creates the creator member
    with role `owner`, and redirects to `/w/{slug}`.

    Create `/workspaces/new` with a name/slug form. Create `/w/[slug]/layout.tsx` and
    `/w/[slug]/page.tsx` as the first workspace shell. Do not auto-create a workspace during
    signup or login.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter @pageforge/web test apps/web/tests/workspaces.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `createWorkspaceAction` rejects missing/unverified sessions
    - `createWorkspaceAction` creates exactly one owner membership for the creator
    - `/workspaces/new` exists and `/w/[slug]/page.tsx` exists
    - No code path creates a workspace automatically on signup
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement workspace guards and RBAC matrix tests</name>
  <files>apps/web/src/lib/workspaces/guards.ts, apps/web/src/lib/auth/permissions.ts, apps/web/tests/permissions.test.ts</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/apps/web/src/lib/auth/permissions.ts
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-CONTEXT.md
  </read_first>
  <action>
    Implement:
    - `requireUser()`
    - `requireVerifiedUser()`
    - `getWorkspaceContext(slug)`
    - `requireWorkspace(slug)`
    - `requireWorkspaceRole(slug, allowedRoles)`
    - `can(role, resource, action)`

    `getWorkspaceContext(slug)` must look up the workspace by slug and verify membership for
    the current session user. It must return `workspaceId`, `workspaceSlug`, `userId`, and
    `role`. The slug is a routing hint only; never trust it without membership validation.

    Add tests covering owner/admin/editor/viewer:
    - owner/admin can invite/remove/change roles and update settings;
    - editor cannot manage members or settings;
    - viewer cannot create/edit/duplicate content but can read/preview/export.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter @pageforge/web test apps/web/tests/permissions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `guards.ts` exports all guard functions listed in the action
    - `getWorkspaceContext` queries membership by session user and workspace slug
    - `permissions.test.ts` covers all four roles
    - `permissions.test.ts` proves editor/viewer cannot manage members
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add tenant DB helper and RLS migration</name>
  <files>apps/web/prisma/migrations/0001_multi_tenancy_foundation/migration.sql, apps/web/src/lib/db/tenant-db.ts, apps/web/tests/tenant-isolation.test.ts</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/apps/web/prisma/schema.prisma
    - /home/glow/Documentos/projetos/PageForge/apps/web/src/lib/db/prisma.ts
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-RESEARCH.md
  </read_first>
  <action>
    Create the baseline SQL migration for the Prisma schema and add RLS SQL for
    `TenantIsolationProbe`.

    Required SQL behavior:
    - enable row level security on the probe table;
    - force row level security on the probe table;
    - create a policy using
      `current_setting('app.current_workspace_id', true)::uuid` in both `USING` and `WITH CHECK`.

    Implement `withTenantDb(ctx, callback)` in `tenant-db.ts`. It must open a Prisma transaction,
    run `SET LOCAL app.current_workspace_id = {ctx.workspaceId}`, and pass a tenant-scoped client
    to the callback. For `TenantIsolationProbe`, expose helper methods that always write/read with
    `ctx.workspaceId` and never accept workspaceId from client input.

    Add tests that create two workspaces and prove:
    - workspace A can read its probe row;
    - workspace A cannot read workspace B's probe row by ID;
    - a write with a mismatched workspaceId is rejected by RLS or helper validation.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter @pageforge/web test apps/web/tests/tenant-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - migration SQL contains `ENABLE ROW LEVEL SECURITY`
    - migration SQL contains `FORCE ROW LEVEL SECURITY`
    - migration SQL contains `current_setting('app.current_workspace_id', true)`
    - `tenant-db.ts` contains `SET LOCAL app.current_workspace_id`
    - `tenant-isolation.test.ts` proves direct-ID cross-workspace read denial
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Generate and apply Prisma schema migration</name>
  <files>apps/web/prisma/schema.prisma, apps/web/prisma/migrations/0001_multi_tenancy_foundation/migration.sql</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-PATTERNS.md
    - /home/glow/Documentos/projetos/PageForge/apps/web/.env.example
  </read_first>
  <action>
    Run the database schema commands after schema and migration files are complete:
    - `pnpm --filter @pageforge/web prisma generate`
    - `pnpm --filter @pageforge/web prisma migrate dev --name multi_tenancy_foundation`

    If `DATABASE_URL` is missing or PostgreSQL is not reachable, stop execution and request manual
    database setup. Do not mark this plan complete based only on typecheck/test commands.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter @pageforge/web prisma generate && pnpm --filter @pageforge/web prisma migrate dev --name multi_tenancy_foundation</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @pageforge/web prisma generate` exits 0
    - `pnpm --filter @pageforge/web prisma migrate dev --name multi_tenancy_foundation` exits 0 against local PostgreSQL
    - Executor summary records the database URL target or explicitly records a manual DB setup blocker
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Mitigation |
|-----------|----------|-----------|----------|------------|
| T-02-02-01 | Spoofing | Workspace slug | high | `getWorkspaceContext` validates slug against session membership |
| T-02-02-02 | Elevation of Privilege | RBAC | high | single permission matrix plus per-action role guards and tests |
| T-02-02-03 | Information Disclosure | Tenant reads | high | app-level helper plus RLS policy using transaction-local workspace setting |
| T-02-02-04 | Tampering | Workspace ID input | high | workspaceId comes from server context only, never client payload |
</threat_model>

<verification>
Run after this plan:

1. `pnpm --filter @pageforge/web prisma generate`
2. `pnpm --filter @pageforge/web prisma migrate dev --name multi_tenancy_foundation`
3. `pnpm --filter @pageforge/web test apps/web/tests/permissions.test.ts`
4. `pnpm --filter @pageforge/web test apps/web/tests/workspaces.test.ts`
5. `pnpm --filter @pageforge/web test apps/web/tests/tenant-isolation.test.ts`
6. `pnpm --filter @pageforge/web typecheck`
</verification>

<success_criteria>
- A verified user can explicitly create a workspace.
- Active workspace context is resolved from `/w/{slug}` plus server session membership.
- owner/admin/editor/viewer permissions are enforced by server-side guards.
- Tenant-owned reads/writes use central tenant helpers and PostgreSQL RLS backstop.
- Cross-workspace direct-ID access is denied in automated tests.
</success_criteria>

