---
phase: 02-multi-tenancy-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - apps/web/src/lib/workspaces/actions.ts
  - apps/web/src/lib/workspaces/invitations.ts
  - apps/web/src/app/w/[slug]/members/page.tsx
  - apps/web/src/app/invitations/[id]/page.tsx
  - apps/web/tests/invitations.test.ts
  - apps/web/tests/tenant-isolation.test.ts
  - apps/web/tests/workspaces.test.ts
  - apps/web/tests/schema-conventions.test.ts
autonomous: true
requirements:
  - WS-01
  - WS-02
  - WS-03
  - WS-04
  - WS-05
requirements_addressed:
  - WS-01
  - WS-02
  - WS-03
  - WS-04
  - WS-05
must_haves:
  truths:
    - "WS-03: owner/admin can invite a member by email and receive a copyable invite link"
    - "WS-03: invitee with no account is routed to signup and joins after verified account creation"
    - "WS-04: owner/admin can change roles and remove members; editor/viewer cannot"
    - "WS-05: cross-tenant tests deny direct-ID read/edit across workspace boundaries"
    - "D-06: invitations are copyable links, not automatically emailed in v1"
    - "D-07: invite accept supports account-creation-on-accept"
    - "D-09: member and role management is restricted to owner/admin"
    - "D-12: invite accept and member routes derive workspace from server-side invitation/session data"
    - "D-13: final test suite proves app-level scoping plus RLS backstop"
    - "D-14: schema convention test fails if tenant-owned models omit workspaceId"
  artifacts:
    - path: "apps/web/src/lib/workspaces/invitations.ts"
      provides: "copyable invitation link creation, lookup, and acceptance helpers"
    - path: "apps/web/src/app/w/[slug]/members/page.tsx"
      provides: "member management UI restricted to owner/admin"
    - path: "apps/web/tests/schema-conventions.test.ts"
      provides: "guardrail that future tenant-owned Prisma models include workspaceId"
---

<decision_traceability>
- D-06: copyable invite link in v1.
- D-07: account-creation-on-accept.
- D-08 through D-11: role behavior exposed through member management.
- D-12: tenant context derived server-side.
- D-13 and D-14: final isolation and schema convention tests.
</decision_traceability>

<objective>
Complete the workspace collaboration slice with copyable email-address invitations, member/role
management, account-on-accept, and final cross-tenant isolation verification for WS-01..WS-05.
</objective>

<context>
@/home/glow/Documentos/projetos/PageForge/.planning/ROADMAP.md
@/home/glow/Documentos/projetos/PageForge/.planning/REQUIREMENTS.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-CONTEXT.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-RESEARCH.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-PATTERNS.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement copyable invitation links</name>
  <files>apps/web/src/lib/workspaces/invitations.ts, apps/web/src/lib/workspaces/actions.ts, apps/web/src/app/w/[slug]/members/page.tsx, apps/web/tests/invitations.test.ts</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/apps/web/src/lib/workspaces/guards.ts
    - /home/glow/Documentos/projetos/PageForge/apps/web/src/lib/auth/permissions.ts
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/02-multi-tenancy-foundation/02-CONTEXT.md
  </read_first>
  <action>
    Implement `createInvitationAction(slug, input)` that requires owner/admin, validates email
    and role, creates a pending invitation with expiration, and returns a copyable URL for
    `/invitations/{id}`. Do not send automated invitation email in this phase.

    Implement the members page with:
    - invite form for email and role;
    - copyable invite URL display;
    - member list with role labels;
    - remove/change-role controls visible only for owner/admin.

    Add tests proving owner/admin can invite and editor/viewer cannot.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter @pageforge/web test apps/web/tests/invitations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `createInvitationAction` requires owner/admin
    - invitation input includes `email` and `role`
    - action returns a URL containing `/invitations/`
    - no automated invitation email is sent in this phase
    - tests prove editor/viewer invite attempts are denied
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement invite acceptance with account-creation-on-accept</name>
  <files>apps/web/src/lib/workspaces/invitations.ts, apps/web/src/app/invitations/[id]/page.tsx, apps/web/tests/invitations.test.ts</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/apps/web/src/lib/auth/auth.ts
    - /home/glow/Documentos/projetos/PageForge/apps/web/src/lib/workspaces/invitations.ts
  </read_first>
  <action>
    Implement invitation lookup and acceptance:
    - expired invitations cannot be accepted;
    - already accepted/revoked invitations cannot be accepted;
    - signed-out users are sent to signup/login with invitation id preserved;
    - users without a verified email cannot accept;
    - once verified and signed in, accepting creates a membership with the invitation role.

    Create `/invitations/[id]` page that presents the invitation state and routes the user to
    signup/login or accept. The workspace id and role must come from the server-side invitation
    record, not from client input.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter @pageforge/web test apps/web/tests/invitations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - unverified signed-in users cannot accept invitations
    - signed-out users see signup/login path with invitation id preserved
    - accepting a valid invitation creates exactly one membership with the stored invitation role
    - client input cannot override invitation workspaceId or role
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement member role changes and removals</name>
  <files>apps/web/src/lib/workspaces/actions.ts, apps/web/src/app/w/[slug]/members/page.tsx, apps/web/tests/workspaces.test.ts, apps/web/tests/permissions.test.ts</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/apps/web/src/lib/workspaces/guards.ts
    - /home/glow/Documentos/projetos/PageForge/apps/web/src/lib/auth/permissions.ts
  </read_first>
  <action>
    Implement server actions:
    - `changeMemberRoleAction(slug, memberId, role)`
    - `removeMemberAction(slug, memberId)`
    - `updateWorkspaceSettingsAction(slug, input)`

    All three actions require owner/admin. Prevent removing the last owner and prevent changing
    the only owner away from owner. editor/viewer must receive authorization denial regardless
    of UI state.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter @pageforge/web test apps/web/tests/workspaces.test.ts apps/web/tests/permissions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - owner/admin can change roles and remove members
    - editor/viewer cannot change roles, remove members, or update workspace settings
    - the last owner cannot be removed
    - the only owner cannot be downgraded
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add final isolation and schema-convention verification</name>
  <files>apps/web/tests/tenant-isolation.test.ts, apps/web/tests/schema-conventions.test.ts</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/apps/web/src/lib/db/tenant-db.ts
    - /home/glow/Documentos/projetos/PageForge/apps/web/prisma/schema.prisma
    - /home/glow/Documentos/projetos/PageForge/apps/web/prisma/migrations/0001_multi_tenancy_foundation/migration.sql
  </read_first>
  <action>
    Extend tenant isolation tests to cover read and edit denial by direct ID across workspaces.
    Use `TenantIsolationProbe` as the canonical proof table because template/LP/brand/assets
    tables are intentionally deferred.

    Add `schema-conventions.test.ts` that parses `apps/web/prisma/schema.prisma` and fails when
    any tenant-owned model listed in the test lacks `workspaceId`. Seed the list with
    `TenantIsolationProbe`, and document that Phase 3-5 must add `Template`, `LandingPage`,
    `BrandConfig`, `Asset`, `Folder`, and `CatalogItem` when those models are introduced.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter @pageforge/web test apps/web/tests/tenant-isolation.test.ts apps/web/tests/schema-conventions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tenant isolation tests deny cross-workspace direct-ID read
    - tenant isolation tests deny cross-workspace direct-ID edit/write
    - schema convention test fails if `TenantIsolationProbe` lacks `workspaceId`
    - schema convention test names future tenant-owned models: Template, LandingPage, BrandConfig, Asset, Folder, CatalogItem
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Run final Phase 2 verification suite</name>
  <files>apps/web/tests/auth.test.ts, apps/web/tests/permissions.test.ts, apps/web/tests/workspaces.test.ts, apps/web/tests/invitations.test.ts, apps/web/tests/tenant-isolation.test.ts, apps/web/tests/schema-conventions.test.ts</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/.planning/ROADMAP.md
    - /home/glow/Documentos/projetos/PageForge/.planning/REQUIREMENTS.md
  </read_first>
  <action>
    Run the complete Phase 2 suite and typecheck:
    - `pnpm --filter @pageforge/web test`
    - `pnpm --filter @pageforge/web typecheck`

    Confirm every WS-01..WS-05 requirement appears in at least one test name or assertion
    comment. If a test cannot run because PostgreSQL is unavailable, record the database blocker
    in the summary and do not mark the phase complete.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter @pageforge/web test && pnpm --filter @pageforge/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @pageforge/web test` exits 0
    - `pnpm --filter @pageforge/web typecheck` exits 0
    - test output includes passing coverage for auth, permissions, workspaces, invitations, tenant isolation, and schema conventions
    - executor summary maps WS-01, WS-02, WS-03, WS-04, WS-05 to concrete passing tests
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Mitigation |
|-----------|----------|-----------|----------|------------|
| T-02-03-01 | Spoofing | Invitation acceptance | high | accept uses server-side invitation record and verified session email |
| T-02-03-02 | Elevation of Privilege | Role changes | high | owner/admin guard plus last-owner protection |
| T-02-03-03 | Information Disclosure | Cross-tenant IDs | high | final direct-ID denial tests and RLS contract |
| T-02-03-04 | Tampering | Client-supplied role/workspace on accept | high | ignore client workspace/role; read from invitation row only |
</threat_model>

<verification>
Run after this plan:

1. `pnpm --filter @pageforge/web test apps/web/tests/invitations.test.ts`
2. `pnpm --filter @pageforge/web test apps/web/tests/workspaces.test.ts apps/web/tests/permissions.test.ts`
3. `pnpm --filter @pageforge/web test apps/web/tests/tenant-isolation.test.ts apps/web/tests/schema-conventions.test.ts`
4. `pnpm --filter @pageforge/web test`
5. `pnpm --filter @pageforge/web typecheck`
</verification>

<success_criteria>
- Workspace owners/admins can invite members by email address and copy an invite link.
- Invitees without accounts can sign up, verify email, and accept the invitation.
- Member role management is restricted to owner/admin with last-owner protection.
- Cross-tenant read/edit by direct ID is denied.
- Phase 2 tests cover WS-01 through WS-05.
</success_criteria>

