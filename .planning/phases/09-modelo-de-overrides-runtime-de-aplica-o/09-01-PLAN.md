---
phase: "09"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/lps/schema.ts
  - apps/web/src/lib/lps/actions.ts
  - apps/web/src/lib/brand/theme.ts
  - apps/web/src/lib/brand/theme.test.ts
autonomous: true
requirements:
  - OVR-01
  - EDIT-06

must_haves:
  truths:
    - "PfOverride Zod schema validates {path, originalHash, type, value} where type is the extensible enum 'text'|'color'|'image'|'href'"
    - "ViteSpaValuesSchema validates {overrides: PfOverride[], primaryColorOverride?: string} where primaryColorOverride matches /^#[0-9a-fA-F]{6}$/"
    - "updateLpAction VITE_SPA branch accepts and persists overrides payload into LandingPage.values via withTenantDb — overrides of LP A do not appear in LP B"
    - "buildBrandStyleTagForLp(lpColor, workspaceColor) returns the LP color style tag when lpColor is set; falls back to workspace color; returns '' when both absent"
  artifacts:
    - path: "apps/web/src/lib/lps/schema.ts"
      provides: "PfOverrideSchema, ViteSpaValuesSchema, SaveViteSpaOverridesSchema exports"
    - path: "apps/web/src/lib/lps/actions.ts"
      provides: "updateLpAction VITE_SPA branch extended to accept overrides via SaveViteSpaOverridesSchema"
    - path: "apps/web/src/lib/brand/theme.ts"
      provides: "buildBrandStyleTagForLp function exported"
    - path: "apps/web/src/lib/brand/theme.test.ts"
      provides: "Tests for buildBrandStyleTagForLp precedence + PfOverrideSchema validation"
  key_links:
    - from: "apps/web/src/lib/lps/actions.ts (updateLpAction VITE_SPA branch)"
      to: "apps/web/src/lib/lps/schema.ts (SaveViteSpaOverridesSchema)"
      via: "SafeParse validates overrides shape before db.lp.update"
    - from: "apps/web/src/lib/brand/theme.ts (buildBrandStyleTagForLp)"
      to: "apps/web/src/app/serve/[tplId]/[[...path]]/route.ts"
      via: "Plan 02 imports buildBrandStyleTagForLp and passes lp.values.primaryColorOverride"
---

<objective>
Establish the override data model and server-side persistence for Phase 9. Creates: (1) Zod schemas for the override payload (`PfOverride`, `ViteSpaValuesSchema`, `SaveViteSpaOverridesSchema`); (2) extends `updateLpAction`'s VITE_SPA branch to accept and validate overrides into `LandingPage.values`; (3) adds `buildBrandStyleTagForLp` to `theme.ts` with per-LP color taking precedence over workspace brand color.

No migration is needed — `LandingPage.values` (jsonb) is already ocioso for VITE_SPA and reused as-is.

Purpose: Plan 02 depends on these schemas and the theme function to inject the apply shim + overrides JSON into the served/exported HTML. Establishing the contract here prevents Plan 02 from having to make assumptions about the data shape.

Output: Extended schema.ts, extended actions.ts, extended theme.ts + updated tests.
</objective>

<execution_context>
@/home/glow/.claude/get-shit-done/workflows/execute-plan.md
@/home/glow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-modelo-de-overrides-runtime-de-aplica-o/09-CONTEXT.md
@apps/web/src/lib/lps/schema.ts
@apps/web/src/lib/lps/actions.ts
@apps/web/src/lib/brand/theme.ts
@apps/web/src/lib/brand/theme.test.ts
@apps/web/prisma/schema.prisma

<interfaces>
<!-- LandingPage.values is Json (jsonb). Currently stores {} for VITE_SPA LPs.
     Phase 9 reuses this same field to store: { overrides: PfOverride[], primaryColorOverride?: string }
     No DB migration required. -->

From apps/web/src/lib/lps/schema.ts (existing exports to preserve):
```typescript
export const GenerateLpSchema
export const UpdateLpSchema          // extend VITE_SPA branch in actions.ts; schema itself adds overrides field
export const GenerateViteSpaLpSchema
export const EditViteSpaLpSchema

export type GenerateLpInput
export type UpdateLpInput
export type GenerateViteSpaLpInput
export type EditViteSpaLpInput
```

From apps/web/src/lib/brand/theme.ts (existing exports to preserve):
```typescript
export function hexToHslTriplet(hex: string): string
export function buildBrandStyleTag(primaryColor: string | null | undefined): string
export function injectBrandStyle(html: string, styleTag: string): string
```

From apps/web/src/lib/lps/actions.ts — updateLpAction VITE_SPA branch (lines 366-373):
The VITE_SPA branch currently only allows name and entryRoute. This plan extends it to also
accept overrides (PfOverride[]) and primaryColorOverride (hex string) persisted into values.
The persistence is: db.lp.update(id, { values: { overrides, primaryColorOverride } }).
Only set values when overrides or primaryColorOverride are provided in input (avoid
overwriting values with an empty object when only name/entryRoute change).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PfOverride + ViteSpaValues Zod schemas to schema.ts</name>
  <files>apps/web/src/lib/lps/schema.ts</files>
  <read_first>
    - apps/web/src/lib/lps/schema.ts — read the full file before editing; append new exports after the existing EditViteSpaLpSchema block
    - apps/web/src/lib/brand/theme.ts — note the hex color regex pattern used in buildBrandStyleTag (validated by SaveBrandConfigSchema as /^#[0-9a-fA-F]{6}$/) — reuse same regex for primaryColorOverride
    - .planning/phases/09-modelo-de-overrides-runtime-de-aplica-o/09-CONTEXT.md — decisions section for field names and type enum
  </read_first>
  <behavior>
    - PfOverrideSchema.parse({ path: '/0/2/1', originalHash: 'abc', type: 'text', value: 'Olá' }) succeeds
    - PfOverrideSchema.parse({ path: '/0', originalHash: 'abc', type: 'image', value: 'https://x' }) succeeds (image/href are valid enum values even if not applied by the shim yet)
    - PfOverrideSchema.parse({ path: '/0', originalHash: 'abc', type: 'invalid', value: 'x' }) throws ZodError
    - ViteSpaValuesSchema.parse({ overrides: [] }) succeeds (primaryColorOverride optional)
    - ViteSpaValuesSchema.parse({ overrides: [], primaryColorOverride: '#ff0000' }) succeeds
    - ViteSpaValuesSchema.parse({ overrides: [], primaryColorOverride: 'red' }) throws ZodError (not a hex string)
    - SaveViteSpaOverridesSchema.parse({ id: 'cm...', overrides: [] }) succeeds
    - SaveViteSpaOverridesSchema.parse({ id: 'cm...', primaryColorOverride: '#ff0000' }) succeeds (overrides optional)
    - SaveViteSpaOverridesSchema.parse({ id: 'cm...' }) succeeds with overrides defaulting to undefined (no mandatory field except id)
  </behavior>
  <action>
Append three new exports to apps/web/src/lib/lps/schema.ts after the EditViteSpaLpSchema block:

1. PfOverrideSchema — Zod object with fields:
   - path: z.string().min(1) — deterministic node path from root (e.g. '/0/2/1/0')
   - originalHash: z.string().min(1) — hash of original node content (stored for Phase 12 drift detection; not checked in this phase)
   - type: z.enum(['text', 'color', 'image', 'href']) — extensible enum; shim only acts on 'text' and 'color' in Phase 9
   - value: z.string() — the override value (text: raw string applied via textContent; color: #RRGGBB hex)

2. ViteSpaValuesSchema — Zod object representing the full structure stored in LandingPage.values for VITE_SPA LPs:
   - overrides: z.array(PfOverrideSchema).default([])
   - primaryColorOverride: z.string().regex(/^#[0-9a-fA-F]{6}$/, 'Must be a #RRGGBB hex color').optional()

3. SaveViteSpaOverridesSchema — the payload accepted by updateLpAction for writing overrides:
   - id: z.string().cuid('Invalid LP ID') — the LP to update
   - overrides: z.array(PfOverrideSchema).optional() — absent means: do not touch existing overrides
   - primaryColorOverride: z.string().regex(/^#[0-9a-fA-F]{6}$/).optional() — absent means: do not touch

Export inferred types: PfOverride, ViteSpaValues, SaveViteSpaOverridesInput from the three schemas.

Add a JSDoc comment above each schema explaining its role (data model for override store, full values shape, action payload).
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge/apps/web && pnpm vitest run --reporter=verbose src/lib/lps/schema.ts 2>&1 | tail -20</automated>
  </verify>
  <done>PfOverrideSchema, ViteSpaValuesSchema, SaveViteSpaOverridesSchema are exported from schema.ts; TypeScript compiles without errors (tsc --noEmit); all behavior cases above pass if tests are written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend updateLpAction VITE_SPA branch to persist overrides</name>
  <files>apps/web/src/lib/lps/actions.ts</files>
  <read_first>
    - apps/web/src/lib/lps/actions.ts — read the full file; the VITE_SPA branch of updateLpAction is at lines 366-373
    - apps/web/src/lib/lps/schema.ts — after Task 1 completes, SaveViteSpaOverridesSchema is available for import
    - apps/web/src/lib/db/tenant-db.ts — understand withTenantDb signature and db.lp.update shape
  </read_first>
  <behavior>
    - updateLpAction called with { id, overrides: [{path:'/0', originalHash:'abc', type:'text', value:'Hello'}] } on a VITE_SPA LP: db.lp.update is called with { values: { overrides: [...], primaryColorOverride: undefined-omitted } } and returns { ok: true }
    - updateLpAction called with { id, primaryColorOverride: '#ff0000' } on a VITE_SPA LP: db.lp.update receives { values: { overrides: [], primaryColorOverride: '#ff0000' } }
    - updateLpAction called with { id, name: 'New Name' } only (no overrides fields) on a VITE_SPA LP: values is NOT included in db.lp.update call (no overrides present = no values key in update payload)
    - updateLpAction called with invalid primaryColorOverride 'red' on a VITE_SPA LP: returns { ok: false, error: 'Validation failed', fieldErrors: { primaryColorOverride: [...] } }
    - updateLpAction called with overrides on a LIQUID LP: the LIQUID branch (existing behavior) is taken; overrides field is ignored (UpdateLpSchema does not include overrides, it passes through to LIQUID path untouched)
  </behavior>
  <action>
In apps/web/src/lib/lps/actions.ts:

1. Import SaveViteSpaOverridesSchema and SaveViteSpaOverridesInput from './schema' alongside the existing schema imports.

2. Extend the input type of updateLpAction to accept the override fields:
   Add optional fields overrides?: PfOverride[] (import PfOverride from './schema') and primaryColorOverride?: string to the inline input type object.
   Do NOT put these fields in UpdateLpSchema (that schema controls the LIQUID path); instead, validate the VITE_SPA override payload separately inside the VITE_SPA branch using SaveViteSpaOverridesSchema.

3. Inside the VITE_SPA branch (currently lines 366-373), after the existing name/entryRoute logic:
   a. Check if input.overrides !== undefined OR input.primaryColorOverride !== undefined.
   b. If yes, call SaveViteSpaOverridesSchema.safeParse({ id: input.id, overrides: input.overrides, primaryColorOverride: input.primaryColorOverride }).
   c. If safeParse fails, return { ok: false, error: 'Validation failed', fieldErrors: ... } (same shape as the LIQUID branch).
   d. If valid, read existing values from db.lp.findById(id) and merge: build the new values object as { overrides: parsed.overrides ?? existingValues.overrides ?? [], primaryColorOverride: parsed.primaryColorOverride ?? existingValues.primaryColorOverride } — where existingValues is (existing.values as ViteSpaValues) ?? {}.
   e. Include values in the db.lp.update call alongside name/entryRoute when the override payload was provided.
   f. When neither overrides nor primaryColorOverride is in input, values is NOT included in the db.lp.update call (preserve existing behavior for name/entryRoute-only updates).

4. Import ViteSpaValues from './schema' (used for the type cast of existing.values).

Security: overrides are validated server-side by SaveViteSpaOverridesSchema (type enum prevents arbitrary types; value is a plain string applied via textContent in the shim — no innerHTML). primaryColorOverride validated as hex by the schema regex before reaching db.lp.update.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge/apps/web && pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>updateLpAction VITE_SPA branch accepts overrides + primaryColorOverride; validates via SaveViteSpaOverridesSchema; persists into LandingPage.values via withTenantDb; TypeScript compiles without errors; LIQUID branch behavior unchanged (verified by pnpm tsc --noEmit passing).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add buildBrandStyleTagForLp to theme.ts + tests</name>
  <files>apps/web/src/lib/brand/theme.ts, apps/web/src/lib/brand/theme.test.ts</files>
  <read_first>
    - apps/web/src/lib/brand/theme.ts — read the full file; understand buildBrandStyleTag and hexToHslTriplet signatures before adding the new function
    - apps/web/src/lib/brand/theme.test.ts — read the full existing test file to understand describe block style; append new tests without modifying existing ones
  </read_first>
  <behavior>
    - buildBrandStyleTagForLp('#ff0000', '#0000ff') returns buildBrandStyleTag('#ff0000') — LP color takes precedence over workspace color
    - buildBrandStyleTagForLp(undefined, '#0000ff') returns buildBrandStyleTag('#0000ff') — falls back to workspace color when LP override is absent
    - buildBrandStyleTagForLp(null, '#0000ff') returns buildBrandStyleTag('#0000ff') — null also triggers fallback
    - buildBrandStyleTagForLp(undefined, null) returns '' — both absent produces empty string (existing buildBrandStyleTag(null) behavior)
    - buildBrandStyleTagForLp('#ff0000', null) returns buildBrandStyleTag('#ff0000') — LP color works even without workspace color
  </behavior>
  <action>
In apps/web/src/lib/brand/theme.ts:

Add a new exported function buildBrandStyleTagForLp after the existing injectBrandStyle function:

```
buildBrandStyleTagForLp(
  primaryColorOverride: string | null | undefined,
  workspacePrimaryColor: string | null | undefined
): string
```

Logic: return buildBrandStyleTag(primaryColorOverride ?? workspacePrimaryColor).

Add a JSDoc comment: "Returns a <style> tag setting --primary. LP color (primaryColorOverride) takes precedence over workspace brand color. Returns '' when both are absent/null."

In apps/web/src/lib/brand/theme.test.ts:

Add a new describe('buildBrandStyleTagForLp') block after the existing injectBrandStyle describe block. Import buildBrandStyleTagForLp from './theme' (add to the existing import line). Write tests for all five behavior cases above.

Do NOT modify any existing test cases.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge/apps/web && pnpm vitest run --reporter=verbose src/lib/brand/theme.test.ts 2>&1</automated>
  </verify>
  <done>buildBrandStyleTagForLp exported from theme.ts; all five behavior cases pass in theme.test.ts; existing theme.test.ts tests continue to pass; pnpm tsc --noEmit clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → updateLpAction | Untrusted override payload (path, type, value, primaryColorOverride) crosses the Server Action boundary |
| DB read → serve/export | LandingPage.values jsonb read from Postgres; must be validated before injecting into HTML (Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | updateLpAction VITE_SPA branch — overrides payload | mitigate | SaveViteSpaOverridesSchema.safeParse validates every field server-side before db.lp.update; type enum prevents unknown types; value is a plain string (textContent only, no innerHTML) |
| T-09-01-02 | Spoofing | updateLpAction — lpId from client | mitigate | db.lp.findById scoped via withTenantDb (workspaceId from requireWorkspaceRole session) — cross-tenant LP ID returns null/404 |
| T-09-01-03 | Injection | primaryColorOverride — CSS injection via hex value | mitigate | /^#[0-9a-fA-F]{6}$/ regex in SaveViteSpaOverridesSchema; hexToHslTriplet output is only digits + '%' + spaces (no CSS escape vector) |
| T-09-01-04 | Information Disclosure | LandingPage.values jsonb cross-tenant read | accept | RLS policy on landing_page table (workspace_id = current_setting) enforced by Postgres FORCE RLS — verified existing in Phase 8 |
| T-09-01-05 | Elevation of Privilege | overrides accepted from viewer role | mitigate | updateLpAction gates on requireWorkspaceRole(['owner','admin','editor']) — viewer cannot call action |
</threat_model>

<verification>
After all three tasks complete:

1. TypeScript: `cd apps/web && pnpm tsc --noEmit` — zero errors
2. Schema tests: `pnpm vitest run src/lib/lps/schema.ts` — PfOverrideSchema, ViteSpaValuesSchema, SaveViteSpaOverridesSchema cases pass
3. Theme tests: `pnpm vitest run src/lib/brand/theme.test.ts` — all describe blocks pass including new buildBrandStyleTagForLp block
4. Smoke check: Call updateLpAction with a VITE_SPA LP ID + overrides payload in a Vitest integration test (or via a quick Node script) and verify the returned LP's values contains the override array.
</verification>

<success_criteria>
- PfOverrideSchema, ViteSpaValuesSchema, SaveViteSpaOverridesSchema exported from schema.ts with correct field names/types per CONTEXT.md decisions
- buildBrandStyleTagForLp exported from theme.ts; LP color precedes workspace color
- updateLpAction VITE_SPA branch persists overrides into LandingPage.values; LIQUID branch unchanged
- pnpm tsc --noEmit passes; pnpm vitest run passes for both test files
- No new Prisma migration (values jsonb reused as-is per CONTEXT.md decision)
</success_criteria>

<output>
After completion, create `.planning/phases/09-modelo-de-overrides-runtime-de-aplica-o/09-01-SUMMARY.md`
</output>
