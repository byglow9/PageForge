---
phase: 03-template-authoring-brand-config
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - apps/web/prisma/migrations/0004_add_template_brand_config/migration.sql
autonomous: true
requirements:
  - TPL-01
  - TPL-05
  - BRD-01

must_haves:
  truths:
    - "A 'template' table exists in the live PostgreSQL database with workspaceId, name, markup, schema (jsonb), metadataOverlay (jsonb), schemaVersion columns"
    - "A 'brand_config' table exists with workspaceId (unique), logoUrl, primaryColor, whatsapp columns"
    - "Both tables have RLS enabled with FORCE ROW LEVEL SECURITY and a tenant_isolation policy keyed on app.current_workspace_id"
    - "withTenantDb can create, read, update, and delete Template and BrandConfig rows without cross-workspace data leaks"
    - "D-10: The deployed 'template' table has a schemaVersion column (Int NOT NULL DEFAULT 1), confirming the monotonically incrementing version stamp is persisted at the database level from the first migration"
  artifacts:
    - path: "apps/web/prisma/migrations/0004_add_template_brand_config/migration.sql"
      provides: "CREATE TABLE statements generated by Prisma + manually appended RLS policy blocks"
      contains: "ENABLE ROW LEVEL SECURITY"
  key_links:
    - from: "apps/web/src/lib/db/tenant-db.ts"
      to: "template / brand_config PostgreSQL tables"
      via: "Prisma generated client after migration applied"
      pattern: "tx.template.create|tx.brandConfig.upsert"
---

<objective>
Apply the Prisma migration for the Template and BrandConfig tables, then manually append the RLS policy blocks before deploying to the live database.

Purpose: This is the [BLOCKING] schema push task. Without it, the live database has no template or brand_config tables and all Server Actions in Plans 03 and 04 will fail at runtime. Build and type checks pass without this (types come from generated Prisma client set up in Plan 01), creating a false-positive verification state — this plan must run before any end-to-end testing.

Output: Migration SQL file with CREATE TABLE + RLS policies; tables live in DB with isolation enforced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-template-authoring-brand-config/03-RESEARCH.md
@.planning/phases/03-template-authoring-brand-config/03-PATTERNS.md

<interfaces>
From apps/web/prisma/migrations/0002_rls_real_tenant_tables/migration.sql (exact RLS SQL pattern):
  ALTER TABLE "workspace_member" ENABLE ROW LEVEL SECURITY;
  ALTER TABLE "workspace_member" FORCE ROW LEVEL SECURITY;
  CREATE POLICY tenant_isolation ON "workspace_member"
      USING ("workspaceId" = current_setting('app.current_workspace_id', true)::text)
      WITH CHECK ("workspaceId" = current_setting('app.current_workspace_id', true)::text);

From apps/web/prisma/migrations/ — existing migration dirs:
  0001_multi_tenancy_foundation/
  0002_rls_real_tenant_tables/
  0003_invitation_token_rls_lookup/
  (new) 0004_add_template_brand_config/  ← Plan 02 creates this
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Generate migration, append RLS policies, deploy to live DB</name>
  <files>
    apps/web/prisma/migrations/0004_add_template_brand_config/migration.sql
  </files>
  <read_first>
    - apps/web/prisma/schema.prisma — verify Template and BrandConfig models exist (added in Plan 01 Task 1) before running migrate
    - apps/web/prisma/migrations/0002_rls_real_tenant_tables/migration.sql — exact RLS block pattern to replicate for template and brand_config tables
    - apps/web/prisma/migrations/0003_invitation_token_rls_lookup/migration.sql — confirms the last applied migration; 0004 follows it
    - .planning/phases/03-template-authoring-brand-config/03-RESEARCH.md — Pattern 4 (Migration SQL) and the migration workflow section (use --create-only then hand-append then deploy)
  </read_first>
  <action>
    Step 1 — Generate the migration SQL file WITHOUT applying it:
    Run from apps/web directory: CI=true npx prisma migrate dev --create-only --name add_template_brand_config
    This creates apps/web/prisma/migrations/0004_add_template_brand_config/migration.sql with the CREATE TABLE statements for "template" and "brand_config". Do NOT run migrate deploy yet.

    Step 2 — Append the RLS policy blocks to the generated migration SQL file.
    Open apps/web/prisma/migrations/0004_add_template_brand_config/migration.sql and append the following blocks after the Prisma-generated CREATE TABLE statements (do not modify the Prisma-generated section):

    For the "template" table:
      ALTER TABLE "template" ENABLE ROW LEVEL SECURITY;
      ALTER TABLE "template" FORCE ROW LEVEL SECURITY;
      CREATE POLICY tenant_isolation ON "template"
          USING ("workspaceId" = current_setting('app.current_workspace_id', true)::text)
          WITH CHECK ("workspaceId" = current_setting('app.current_workspace_id', true)::text);

    For the "brand_config" table:
      ALTER TABLE "brand_config" ENABLE ROW LEVEL SECURITY;
      ALTER TABLE "brand_config" FORCE ROW LEVEL SECURITY;
      CREATE POLICY tenant_isolation ON "brand_config"
          USING ("workspaceId" = current_setting('app.current_workspace_id', true)::text)
          WITH CHECK ("workspaceId" = current_setting('app.current_workspace_id', true)::text);

    Column name "workspaceId" matches the Prisma field name exactly (camelCase, quoted in SQL) — copy the pattern from 0002 verbatim.

    Step 3 — Apply the migration to the live database:
    Run from apps/web directory: CI=true npx prisma migrate deploy
    This applies 0004_add_template_brand_config/migration.sql to the database including the appended RLS blocks.

    Step 4 — Regenerate Prisma client (if not already up to date from Plan 01):
    Run: npx prisma generate
    Confirms the generated client reflects the deployed schema.

    IMPORTANT: If the database is unavailable (Docker Compose not running), start it first with: docker compose up -d postgres (or equivalent per local setup). The migration MUST be applied to the live DB before Plans 03 and 04 can be verified end-to-end.
  </action>
  <verify>
    <automated>
      pnpm --filter @pageforge/web exec prisma migrate status
    </automated>
  </verify>
  <done>
    - apps/web/prisma/migrations/0004_add_template_brand_config/migration.sql exists and contains both CREATE TABLE statements and both RLS ALTER TABLE / CREATE POLICY blocks
    - prisma migrate status reports: "All migrations have been applied" (0004_add_template_brand_config listed as Applied)
    - SQL verification (run manually if needed): psql -c "\dp template" and "\dp brand_config" show RLS enabled; or: psql -c "SELECT relname, relrowsecurity FROM pg_class WHERE relname IN ('template', 'brand_config');" returns t for both
    - pnpm --filter @pageforge/web test tests/schema-conventions.test.ts still passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DBA/migration runner → PostgreSQL | Migration SQL executed with database owner role; RLS policies added immediately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | template table without RLS | mitigate | Migration SQL appends ENABLE ROW LEVEL SECURITY + FORCE RLS + CREATE POLICY before deploy; verified by migrate status |
| T-03-02-02 | Information Disclosure | brand_config table without RLS | mitigate | Same RLS pattern applied to brand_config in same migration; both tables must have policies before app code can read/write them |
| T-03-02-03 | Tampering | Migration applied without RLS blocks (Prisma-only migration) | mitigate | Workflow is --create-only (generates SQL), then hand-append RLS, then migrate deploy — never migrate dev directly to live DB without reviewing the SQL |
| T-03-02-04 | Denial of Service | Migration failure leaving DB in partial state | accept | Prisma migrate deploy wraps in a transaction; if RLS ALTER TABLE fails, the CREATE TABLE is also rolled back. DB stays consistent. |
</threat_model>

<verification>
1. cd apps/web && npx prisma migrate status — "All migrations have been applied", 0004_add_template_brand_config listed
2. Check migration SQL file contains all 6 RLS statements: grep -c "ROW LEVEL SECURITY\|CREATE POLICY" apps/web/prisma/migrations/0004_add_template_brand_config/migration.sql — must return 6 (3 statements × 2 tables)
3. pnpm --filter @pageforge/web test tests/schema-conventions.test.ts — still passes (models in schema + test list)
</verification>

<success_criteria>
- 0004_add_template_brand_config/migration.sql exists with CREATE TABLE blocks (Prisma-generated) + RLS policy blocks (hand-appended)
- prisma migrate status shows all migrations applied
- Both "template" and "brand_config" tables exist in the live PostgreSQL database with RLS enabled
- The Prisma generated client is in sync with the deployed schema
</success_criteria>

<output>
After completion, create .planning/phases/03-template-authoring-brand-config/03-02-SUMMARY.md
</output>
