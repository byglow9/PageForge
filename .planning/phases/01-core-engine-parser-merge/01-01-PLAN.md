---
phase: 01-core-engine-parser-merge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/engine/schema.ts
  - src/engine/index.ts
  - tests/engine/e2e.test.ts
autonomous: true
requirements:
  - TPL-02
  - GEN-05

must_haves:
  truths:
    - "pnpm install conclui sem erros com liquidjs@^10.27.0, zod@^4.4.3, sanitize-html@^2.17.4 e vitest@^4.1.8 instalados"
    - "vitest run executa sem erros de compilação TS (os testes podem falhar — o scaffold é a prova)"
    - "src/engine/schema.ts exporta FieldTypeSchema, TokenFieldSchema, ParsedSchemaSchema e os tipos inferidos FieldType, TokenField, ParsedSchema, ParseWarning"
    - "src/engine/index.ts re-exporta parse e render como superfície pública do engine"
    - "tests/engine/e2e.test.ts contém um teste failing que importa parse e render e exercita o pipeline completo (parse → compile → render → HTML não-vazio)"
    - "D-03: schema.ts (FieldTypeSchema) define os seis tipos de campo — text, richtext, image, color, button, repeater"
    - "D-05: schema emitido por token é mínimo nesta fase — nome, tipo detectado e a qual repeater/global pertence; label default = nome do token"
  artifacts:
    - path: "package.json"
      provides: "Dependências pinadas: liquidjs@^10.27.0, zod@^4.4.3, sanitize-html@^2.17.4, vitest@^4.1.8, typescript@^5.x, tsx"
    - path: "tsconfig.json"
      provides: "Config TS com strict:true, target:ES2022, module:NodeNext, moduleResolution:NodeNext"
    - path: "vitest.config.ts"
      provides: "environment:node, globals:true, include:tests/**/*.test.ts"
    - path: "src/engine/schema.ts"
      provides: "Zod schemas e TypeScript types para o pipeline — FieldTypeSchema, ParsedSchemaSchema"
    - path: "src/engine/index.ts"
      provides: "Barrel export de parse() e render() para consumidores externos (Fases 3/4)"
    - path: "tests/engine/e2e.test.ts"
      provides: "Teste e2e RED que exercita parse→render e falha porque as implementações ainda são stubs"
  key_links:
    - from: "tests/engine/e2e.test.ts"
      to: "src/engine/index.ts"
      via: "import { parse, render } from '../../src/engine'"
      pattern: "from '../../src/engine'"
    - from: "src/engine/index.ts"
      to: "src/engine/parser.ts + src/engine/renderer.ts"
      via: "re-exports"
      pattern: "export.*from './parser'|export.*from './renderer'"
---

<decision_traceability>
Decisões do CONTEXT.md implementadas neste plano (rastreabilidade):
- D-03: o schema Zod define os seis tipos de campo (text, richtext, image, color, button, repeater).
- D-05: o schema emitido por token é mínimo nesta fase — nome, tipo detectado e a qual repeater/global pertence; label default = nome do token.
</decision_traceability>

<objective>
Scaffolding do projeto PageForge engine + contrato de tipos Zod + teste e2e failing (RED).

Purpose: Estabelecer que `pnpm install && pnpm test` é executável — a base sobre a qual os Planos 02 e 03 implementam o pipeline. Os tipos Zod em schema.ts são o contrato compartilhado entre parser, compiler, renderer e testes.

Output: package.json com deps pinadas, tsconfig.json, vitest.config.ts, src/engine/schema.ts, src/engine/index.ts (stubs), tests/engine/e2e.test.ts (RED — falha esperada).
</objective>

<execution_context>
@/home/glow/.claude/get-shit-done/workflows/execute-plan.md
@/home/glow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/glow/Documentos/projetos/PageForge/.planning/ROADMAP.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-CONTEXT.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-RESEARCH.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-PATTERNS.md

<interfaces>
<!-- Contratos que este plano CRIA e que os Planos 02/03 implementarão. -->

src/engine/schema.ts deve exportar:
```typescript
export const FieldTypeSchema = z.enum(['text', 'richtext', 'image', 'color', 'button', 'repeater']);
export const TokenFieldSchema = z.object({
  name: z.string(),
  type: FieldTypeSchema,
  repeater: z.string().nullable(),
  global: z.boolean(),
});
export const ParseWarningSchema = z.object({ token: z.string(), message: z.string() });
export const ParsedSchemaSchema = z.object({
  fields: z.array(TokenFieldSchema),
  repeaters: z.array(z.string()),
  globals: z.array(z.string()),
  warnings: z.array(ParseWarningSchema),
});
export type FieldType = z.infer<typeof FieldTypeSchema>;
export type TokenField = z.infer<typeof TokenFieldSchema>;
export type ParsedSchema = z.infer<typeof ParsedSchemaSchema>;
export type ParseWarning = z.infer<typeof ParseWarningSchema>;
```

src/engine/index.ts stub mínimo (para que o e2e importe sem erro de módulo):
```typescript
export function parse(markup: string): ParsedSchema { throw new Error('not implemented'); }
export async function render(markup: string, values: Record<string, unknown>, brand: Record<string, unknown>): Promise<string> { throw new Error('not implemented'); }
export type { FieldType, TokenField, ParsedSchema, ParseWarning } from './schema';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Scaffold do projeto (package.json, tsconfig.json, vitest.config.ts)</name>
  <files>package.json, tsconfig.json, vitest.config.ts</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-RESEARCH.md (§Stack Standard — versões exatas, §Instalação, linha 107 pnpm install command)
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-PATTERNS.md (§vitest.config.ts — divergência crítica jsdom vs node, linhas 326–366)
    - /home/glow/Documentos/projetos/PageForge/CLAUDE.md (§Technology Stack — stack rationale, pnpm, TypeScript 5.x)
  </read_first>
  <action>
    Criar package.json na raiz do repositório PageForge (/home/glow/Documentos/projetos/PageForge/) com:
    - name: "pageforge-engine", version: "0.1.0", private: true
    - scripts: "test": "vitest run", "test:watch": "vitest", "build": "tsc --noEmit", "typecheck": "tsc --noEmit"
    - dependencies: liquidjs@^10.27.0, zod@^4.4.3, sanitize-html@^2.17.4
    - devDependencies: vitest@^4.1.8, typescript@^5.0.0, tsx@^4.0.0, @types/node@^20.0.0, @types/sanitize-html@^2.13.0
    - type: "module" (ESM — necessário para moduleResolution NodeNext)

    Criar tsconfig.json com: compilerOptions strict:true, target:"ES2022", module:"NodeNext", moduleResolution:"NodeNext", outDir:"./dist", rootDir:"./src", declaration:true, esModuleInterop:true, skipLibCheck:true; include:["src","tests"]; exclude:["node_modules","dist"].

    Criar vitest.config.ts com: defineConfig({ test: { environment:'node', globals:true, include:['tests/**/*.test.ts'] } }). NÃO adicionar plugins react, NÃO usar environment:'jsdom' — o engine é Node puro (ver 01-PATTERNS.md §vitest.config.ts divergência crítica).

    Executar `pnpm install` para gerar pnpm-lock.yaml e node_modules. Verificar que `pnpm run build` (tsc --noEmit) não emite erros.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm install --frozen-lockfile 2>/dev/null || pnpm install && node -e "require('./node_modules/liquidjs/dist/liquid.cjs.js')" && echo "liquidjs ok" && node -e "require('./node_modules/zod/lib/index.cjs')" && echo "zod ok"</automated>
  </verify>
  <acceptance_criteria>
    - package.json existe com liquidjs@^10.27.0, zod@^4.4.3, sanitize-html@^2.17.4, vitest@^4.1.8 nas seções corretas (deps vs devDeps)
    - tsconfig.json tem "strict": true, "module": "NodeNext", "moduleResolution": "NodeNext"
    - vitest.config.ts tem environment:'node' e NÃO tem plugins react nem environment:'jsdom'
    - `pnpm install` sai com código 0 e node_modules/ é criado
    - `grep -c "liquidjs" package.json` retorna >= 1
    - `grep -c "ownPropertyOnly" package.json` retorna 0 (essa config fica no código, não no package.json)
  </acceptance_criteria>
  <done>pnpm install termina sem erros; liquidjs, zod, sanitize-html e vitest presentes em node_modules/</done>
</task>

<task type="auto">
  <name>Tarefa 2: Schema Zod (src/engine/schema.ts) + stubs do engine (src/engine/index.ts, src/engine/parser.ts, src/engine/renderer.ts)</name>
  <files>src/engine/schema.ts, src/engine/index.ts, src/engine/parser.ts, src/engine/renderer.ts</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-RESEARCH.md (§Design do Parser → Shape do Schema Zod, linhas 438–467; §Interface Pública do Engine, linhas 469–482)
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-PATTERNS.md (§src/engine/schema.ts — core pattern completo, linhas 47–83; §src/engine/index.ts — re-exports, linhas 312–323)
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-CONTEXT.md (D-03 seis tipos; D-05 schema mínimo; D-09 namespace brand)
  </read_first>
  <action>
    Criar src/engine/schema.ts exatamente conforme o shape Zod especificado em 01-PATTERNS.md §src/engine/schema.ts (linhas 56–82). Exportar: FieldTypeSchema (z.enum dos seis tipos: 'text','richtext','image','color','button','repeater'), TokenFieldSchema (name, type, repeater nullable, global boolean), ParseWarningSchema (token, message), ParsedSchemaSchema (fields array, repeaters string array, globals string array, warnings array). Exportar também os tipos inferidos: FieldType, TokenField, ParsedSchema, ParseWarning.

    Criar src/engine/parser.ts como stub: exportar `function parse(markup: string): ParsedSchema` que lança `throw new Error('parse: not implemented')`.

    Criar src/engine/renderer.ts como stub: exportar `async function render(markup: string, values: Record<string, unknown>, brand: Record<string, unknown>): Promise<string>` que lança `throw new Error('render: not implemented')`.

    Criar src/engine/index.ts como barrel export: re-exportar parse de './parser', render de './renderer', e os quatro tipos de './schema'. Verificar que não há implementação aqui — apenas re-exports.

    Criar diretório src/engine/ se não existir. Todos os arquivos devem usar extensões .js nos imports internos (required por moduleResolution NodeNext com ESM), e.g. `import { ParsedSchema } from './schema.js'`.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm run build 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/schema.ts exporta FieldTypeSchema, TokenFieldSchema, ParsedSchemaSchema, ParseWarningSchema e os quatro tipos inferidos
    - `grep -c "z.enum" src/engine/schema.ts` retorna >= 1
    - `grep -c "repeater.*nullable" src/engine/schema.ts` retorna >= 1 (campo repeater é nullable)
    - src/engine/index.ts re-exporta parse e render (verificável por `grep "export.*parse\|export.*render" src/engine/index.ts`)
    - `pnpm run build` (tsc --noEmit) sai com código 0 sem erros de tipagem
    - src/engine/parser.ts e src/engine/renderer.ts existem e têm as assinaturas corretas (parse retorna ParsedSchema, render retorna Promise<string>)
  </acceptance_criteria>
  <done>tsc --noEmit passa; schema.ts exporta todos os tipos Zod; index.ts é o ponto de entrada público do engine</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: Teste e2e RED (tests/engine/e2e.test.ts)</name>
  <files>tests/engine/e2e.test.ts</files>
  <read_first>
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-RESEARCH.md (§Exemplos de Código → Exemplo 4 e Exemplo 5, linhas 826–896; §Arquitetura de Validação, linhas 924–938)
    - /home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-PATTERNS.md (§tests/engine/parser.test.ts, linhas 370–421; §tests/engine/renderer.test.ts, linhas 426–460)
    - /home/glow/Documentos/projetos/PageForge/src/engine/index.ts (stubs criados na Tarefa 2 — importar daqui)
  </read_first>
  <behavior>
    - Teste 1: `parse('<div>{{ titulo:text }}</div>')` retorna schema com `fields` contendo um item com `name:'titulo'` e `type:'text'`
    - Teste 2: `parse('<div>{{ s:richtext }}<!-- repeat:x -->{{ y:image }}<!-- /repeat:x --></div>')` retorna schema com `repeaters` contendo 'x'
    - Teste 3: `render('<p>{{ nome:text }}</p>', { nome: 'Mundo' }, {})` resolve para string HTML contendo 'Mundo'
    - Teste 4: `render('<p>{{ msg:text }}</p>', { msg: '"><script>alert(1)</script>' }, {})` resolve para HTML sem `<script`
    - Estes testes DEVEM falhar na execução inicial (stubs lançam Error) — isso é o estado RED esperado
  </behavior>
  <action>
    Criar tests/engine/e2e.test.ts com quatro testes usando describe/it/expect do Vitest. Importar parse e render de '../../src/engine/index.js' (extensão .js obrigatória para NodeNext ESM). Os testes devem usar `vi.spyOn` ou try/catch para tratar o throw dos stubs? Não — os testes DEVEM falhar com throw, pois são RED. Vitest registrará o throw como falha de teste, que é o estado correto antes da implementação.

    Estrutura: describe('Engine e2e (walk-up slice)', () => { it('parse detecta tipo text', ...), it('parse detecta repeater', ...), it('render produz HTML com valor', ...), it('render escapa XSS em campo text', ...) }). Os testes de render devem ser async/await.

    Criar o diretório tests/engine/ se não existir.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm vitest run tests/engine/e2e.test.ts 2>&1 | grep -E "FAIL|Error: (parse|render): not implemented|Tests" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/engine/e2e.test.ts existe e importa de '../../src/engine/index.js'
    - `pnpm vitest run tests/engine/e2e.test.ts` termina com código != 0 (testes falham — RED esperado)
    - A saída do vitest contém "Error: parse: not implemented" ou "Error: render: not implemented" (confirmando que os stubs são chamados)
    - O arquivo contém exatamente 4 testes com `it(` no nível de describe (verificável por `grep -c "^\s*it(" tests/engine/e2e.test.ts`)
    - Nenhum teste usa `vi.spyOn` para suprimir os throws — os throws devem aparecer como falhas reais
  </acceptance_criteria>
  <done>vitest run falha em todos os 4 testes com "not implemented" — RED confirmado; as implementações do Plano 02 farão os testes passarem</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Dependências npm → código do engine | Libs de terceiros (liquidjs, zod, sanitize-html) entram no pipeline |
| Schema Zod → tipos TS | Tipos inferidos são a fonte de verdade; se o schema mudar, os tipos mudam |

## STRIDE Threat Register (Plano 01 — Scaffold)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json dependency versions | mitigate | Pinnar liquidjs@^10.27.0 (caret mantém minor/patch; 3 CVEs corrigidas nessa versão — não usar range mais amplo); executar `npm audit` antes de releases |
| T-01-02 | Tampering | tsconfig.json (strict:false acidental) | mitigate | strict:true obrigatório — suprime classes de erro de tipo que podem ocultar acesso unsafe a proto; verificado em Tarefa 1 acceptance_criteria |
| T-01-03 | Information Disclosure | FieldTypeSchema enum (lista de tipos válidos) | accept | A lista é documentação pública do produto; não é segredo |
| T-01-04 | Tampering | Stub throws em parser/renderer | accept | Stubs existem apenas em Wave 1 como placeholder RED; substituídos na Wave 2 antes de qualquer uso real |
</threat_model>

<verification>
Verificação geral do Plano 01:

1. `pnpm install` sai com código 0
2. `pnpm run build` (tsc --noEmit) sai com código 0 sem erros
3. `pnpm vitest run tests/engine/e2e.test.ts` falha em todos os 4 testes (RED esperado — stubs lançam)
4. `ls src/engine/` mostra: schema.ts, parser.ts, renderer.ts, index.ts
5. `grep "liquidjs" package.json` retorna a linha com "^10.27.0"
6. `grep "environment" vitest.config.ts` retorna 'node' (não 'jsdom')
</verification>

<success_criteria>
- pnpm install termina sem erros; node_modules/ criado com liquidjs, zod, sanitize-html, vitest
- tsc --noEmit passa sem erros de tipagem
- src/engine/schema.ts exporta todos os seis tipos Zod (FieldTypeSchema, TokenFieldSchema, ParsedSchemaSchema, ParseWarningSchema + 4 tipos inferidos)
- src/engine/index.ts é o barrel export da API pública do engine
- tests/engine/e2e.test.ts existe com 4 testes RED (falham por "not implemented")
- vitest.config.ts usa environment:'node' — sem jsdom
</success_criteria>

<output>
Após conclusão, criar /home/glow/Documentos/projetos/PageForge/.planning/phases/01-core-engine-parser-merge/01-01-SUMMARY.md
</output>
