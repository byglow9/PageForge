---
phase: "10"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/prisma/migrations/0010_lp_serving_read_policy/migration.sql
autonomous: true
requirements: []
must_haves:
  truths:
    - "After migration 0010 is applied, pnpm --filter web prisma migrate status shows 0010_lp_serving_read_policy as Applied"
    - "The serve route HTML for a VITE_SPA LP with saved overrides contains a non-empty pf-overrides sentinel (not []) — the serving_read policy allows SELECT inside servingRead()"
  artifacts:
    - path: "apps/web/prisma/migrations/0010_lp_serving_read_policy/migration.sql"
      provides: "serving_read SELECT RLS policy for landing_page table"
      contains: "CREATE POLICY \"serving_read\" ON \"landing_page\""
  key_links:
    - from: "servingRead helper (apps/web/src/app/serve/[tplId]/[[...path]]/route.ts)"
      to: "landing_page table"
      via: "serving_read RLS policy — app.serving='on' allows SELECT without app.current_workspace_id"
      pattern: "serving_read.*landing_page"
    - from: "Prisma WHERE clause (workspaceId from HMAC token)"
      to: "landing_page rows"
      via: "application-level tenant scoping enforced independently of RLS"
      pattern: "where.*workspaceId.*templateId"
---

<objective>
Fix the O-2 RLS policy gap that causes the serve route to return zero rows for `landing_page` inside `servingRead()`, making overrides invisible in the preview.

Purpose: The entire Phase 10 editor depends on the serve route preview correctly injecting LP overrides. Without this fix, `updateLpAction` saves overrides successfully but the preview (the editor's feedback channel) never reflects them — the editor appears broken. This plan is a blocking prerequisite for EDIT-03 delivery (not an implementation of it — EDIT-03 is delivered by plans 10-02 and 10-03); it carries no requirement IDs of its own.

Output: Migration SQL file `0010_lp_serving_read_policy/migration.sql` applied to the database. After this plan, a VITE_SPA LP with seeded overrides correctly shows them in the serve route preview.
</objective>

<execution_context>
@/home/glow/.claude/get-shit-done/workflows/execute-plan.md
@/home/glow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-editor-visual-in-iframe-texto/10-RESEARCH.md
@.planning/phases/10-editor-visual-in-iframe-texto/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create migration 0010 — serving_read policy on landing_page</name>
  <files>apps/web/prisma/migrations/0010_lp_serving_read_policy/migration.sql</files>
  <read_first>
    - apps/web/prisma/migrations/0009_serving_read_policy/migration.sql — EXACT analog; copy the pattern applied to `template` and `brand_config`; apply to `landing_page` instead
    - apps/web/prisma/migrations/0005_catalog_folders_tags/migration.sql — lines 180-196; confirms `landing_page` has only `tenant_isolation` policy (no `serving_read`) — this is the gap being closed
    - apps/web/src/app/serve/[tplId]/[[...path]]/route.ts — lines 54-69 (servingRead helper); confirms it sets `app.serving='on'` but NOT `app.current_workspace_id`; confirms why tenant_isolation fails inside servingRead
    - .planning/phases/10-editor-visual-in-iframe-texto/10-RESEARCH.md — Unknown 5 (O-2 root cause trace, migration SQL)
  </read_first>
  <action>
    Create directory `apps/web/prisma/migrations/0010_lp_serving_read_policy/` and write `migration.sql` inside it.

    The file must contain:
    - A comment block explaining the O-2 root cause: servingRead sets app.serving='on' but not app.current_workspace_id; landing_page only has tenant_isolation (requires app.current_workspace_id); inside servingRead all LP reads return zero rows.
    - A comment on security: serve route already WHERE-filters by workspaceId from HMAC token claims (never from URL params) — application-level scoping enforced independently of RLS, same dual-layer pattern as migration 0009.
    - One DDL statement: CREATE POLICY "serving_read" ON "landing_page" FOR SELECT USING (current_setting('app.serving', true) = 'on');

    Do NOT add WITH CHECK clause (SELECT-only policy; no INSERT/UPDATE gate needed).
    Do NOT modify any other table or policy.
    Migration dir naming convention: NNNN_snake_case_description (matches existing dirs: 0001_, 0009_, etc.).
  </action>
  <verify>
    <automated>test -f apps/web/prisma/migrations/0010_lp_serving_read_policy/migration.sql && grep -c 'CREATE POLICY "serving_read" ON "landing_page"' apps/web/prisma/migrations/0010_lp_serving_read_policy/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/prisma/migrations/0010_lp_serving_read_policy/migration.sql` exists
    - File contains exactly one `CREATE POLICY "serving_read" ON "landing_page"` statement
    - Policy is FOR SELECT with USING clause `current_setting('app.serving', true) = 'on'`
    - No WITH CHECK clause present (SELECT-only)
    - File contains a comment explaining the O-2 root cause and security rationale
    - Directory name is `0010_lp_serving_read_policy` (matches NNNN_snake_case convention)
  </acceptance_criteria>
  <done>Migration SQL file created with correct policy matching migration 0009 pattern for `template`/`brand_config`.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration and verify database state</name>
  <files>apps/web/prisma/migrations/0010_lp_serving_read_policy/migration.sql</files>
  <read_first>
    - apps/web/prisma/migrations/migration_lock.toml — current lock; verify provider is "postgresql"
    - apps/web/prisma/schema.prisma — confirm landing_page model exists; no schema changes needed (this migration is RLS-only)
    - apps/web/prisma/migrations/0010_lp_serving_read_policy/migration.sql — the file just created (Task 1)
  </read_first>
  <action>
    Run `pnpm --filter web prisma migrate dev` from the monorepo root to apply migration 0010. This command will detect the new migration file and apply it to the local Postgres instance.

    If `prisma migrate dev` prompts for a migration name, the directory already has the name (`0010_lp_serving_read_policy`) — the command should detect and apply it without prompting.

    After migration completes, verify with `pnpm --filter web prisma migrate status` — output must show `0010_lp_serving_read_policy` under Applied migrations.

    Do NOT run `prisma migrate reset` (would wipe dev data).
    Do NOT add `--skip-generate` unless prisma client generation fails (it should not, since no schema model was changed).
  </action>
  <verify>
    <automated>pnpm --filter web prisma migrate status 2>&1 | grep -c "0010_lp_serving_read_policy"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web prisma migrate dev` exits 0
    - `pnpm --filter web prisma migrate status` output contains "0010_lp_serving_read_policy" with no "(not yet applied)" suffix
    - No other migrations listed as pending (history is clean)
    - `migration_lock.toml` reflects the updated migration history
  </acceptance_criteria>
  <done>Migration 0010 applied to local Postgres; serving_read policy exists on landing_page; serve route LP reads will now return rows inside servingRead().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RLS policy → app session vars | `serving_read` policy is permissive for SELECT when `app.serving='on'`; tenant scoping is enforced at Prisma WHERE clause level, not by this policy |
| Migration SQL → production DB | Migration must be reviewed before applying to production; Prisma migrate deploy (not dev) used in CI/prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Elevation of Privilege | serving_read RLS policy on landing_page | mitigate | Policy is SELECT-only; serve route Prisma query already WHERE-filters by `workspaceId` from HMAC token claims (never from URL params) — application-level tenant scoping independent of the permissive RLS policy, identical dual-layer pattern as migration 0009 for template/brand_config |
| T-10-01-02 | Tampering | Migration SQL content | accept | Migration is committed to git and reviewed via PR; Prisma migrate status confirms applied state; low risk — migration only adds a policy, does not remove or modify existing policies |
</threat_model>

<verification>
After both tasks complete:

1. `pnpm --filter web prisma migrate status` → shows 0010_lp_serving_read_policy as Applied
2. `grep 'serving_read' apps/web/prisma/migrations/0010_lp_serving_read_policy/migration.sql` → returns the CREATE POLICY line
3. (Manual) Open a VITE_SPA LP preview in the browser; verify browser DevTools Network shows the served HTML contains `<script id="pf-overrides"` with the LP's overrides (not `[]`) — confirms serving_read policy is working
</verification>

<success_criteria>
Migration 0010 is applied and the `landing_page` table has a `serving_read` SELECT RLS policy. The serve route `servingRead()` helper can now read `landing_page` rows inside its transaction (because `app.serving='on'` satisfies the new policy). A VITE_SPA LP with seeded overrides shows those overrides in the serve route preview HTML, making Phase 10 editor verification possible.
</success_criteria>

<output>
After completion, create `.planning/phases/10-editor-visual-in-iframe-texto/10-01-SUMMARY.md`
</output>
