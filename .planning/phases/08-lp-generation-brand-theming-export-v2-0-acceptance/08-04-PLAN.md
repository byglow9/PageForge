---
phase: 08-lp-generation-brand-theming-export-v2-0-acceptance
plan: "04"
type: execute
wave: 3
depends_on:
  - "08-02"
  - "08-03"
files_modified:
  - apps/web/src/app/api/lps/[lpId]/export/route.ts
  - apps/web/src/app/w/[slug]/lps/[lpId]/edit/page.tsx
autonomous: true
requirements:
  - PRJ-08
  - PRJ-09
  - PRJ-10

must_haves:
  truths:
    - "Exportar uma LP VITE_SPA retorna um ZIP da árvore dist/ com o index.html contendo a <style>:root{--primary:...;}</style> baked"
    - "O ZIP não contém nenhum meta tag CSP `script-src 'none'` (que o LIQUID adiciona)"
    - "A rota de export LIQUID continua funcionando sem regressão (LP LIQUID exporta normalmente)"
    - "Abrir a página de edição de uma LP VITE_SPA exibe o ViteSpaLpForm com name e entryRoute pré-preenchidos"
    - "Abrir a página de edição de uma LP LIQUID exibe o LpForm original com parse(markupSnapshot) — sem regressão"
    - "Decisões CONTEXT.md cobertas neste plano — D-10: branch por `kind` na rota de export já existente (ZIP da árvore dist/ inteira); D-11: cor da marca assada no index.html exportado (mesmo `<style>` prepended); D-12: CSP estrita `script-src 'none'` do LIQUID NÃO se aplica ao VITE_SPA"
  artifacts:
    - path: "apps/web/src/app/api/lps/[lpId]/export/route.ts"
      provides: "Branch VITE_SPA: ListObjectsV2 → ZIP archiver com index.html tematizado, sem CSP; LIQUID path inalterado"
      contains: "ListObjectsV2Command"
    - path: "apps/web/src/app/w/[slug]/lps/[lpId]/edit/page.tsx"
      provides: "Branch VITE_SPA → ViteSpaLpForm; LIQUID branch → parse(markupSnapshot) + LpForm inalterado"
      contains: "ViteSpaLpForm"
  key_links:
    - from: "apps/web/src/app/api/lps/[lpId]/export/route.ts"
      to: "apps/web/src/lib/brand/theme.ts"
      via: "buildBrandStyleTag + injectBrandStyle para index.html no ZIP"
      pattern: "buildBrandStyleTag|injectBrandStyle"
    - from: "apps/web/src/app/api/lps/[lpId]/export/route.ts"
      to: "AWS S3 dist/ prefix"
      via: "ListObjectsV2Command paginado (ContinuationToken) com prefix workspaces/{wId}/project-templates/{tplId}/dist/"
      pattern: "ListObjectsV2Command"
---

<objective>
Slice vertical de export e edição: implementar o branch VITE_SPA na rota de export (ZIP do dist/ tematizado, sem CSP estrita) e o branch VITE_SPA na página de edição da LP (ViteSpaLpForm pré-preenchido). Após este plano, o ciclo de vida completo da LP VITE_SPA está disponível: generate → preview → edit → export.

Purpose: PRJ-09 (export ZIP sem CSP estrita) + PRJ-10 (editar rota/tema). O export branch é o ponto mais complexo desta fase (ListObjectsV2 paginado + archiver streaming).

Output: export route com branch VITE_SPA funcional; página de edit com branch VITE_SPA.
</objective>

<execution_context>
@/home/glow/.claude/get-shit-done/workflows/execute-plan.md
@/home/glow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/glow/Documentos/projetos/PageForge/.planning/PROJECT.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-CONTEXT.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-RESEARCH.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-PATTERNS.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-UI-SPEC.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-02-SUMMARY.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-03-SUMMARY.md

<interfaces>
<!-- Ponto de extensão no export route (PATTERNS.md): -->
<!-- ATUAL — guard 409 placeholder (L193-200 per RESEARCH): -->
if ((lp.kind ?? "LIQUID") === "VITE_SPA") {
  return NextResponse.json({ error: "VITE_SPA..." }, { status: 409 });
}

<!-- NOVO — substituir o bloco 409 por: -->
if ((lp.kind ?? "LIQUID") === "VITE_SPA") {
  if (!lp.templateId) return NextResponse.json({ error: "VITE_SPA LP has no template reference." }, { status: 400 });
  // fetch BrandConfig
  // ListObjectsV2 paginado com prefix = workspaces/{wId}/project-templates/{tplId}/dist/
  // archiver loop: index.html → transformToString() + injectBrandStyle; outros → transformToWebStream() + Readable.fromWeb()
  // archive.finalize() → Readable.toWeb(archive) → NextResponse zip stream
  // SEM injectCsp() — D-12
}
// ABAIXO: caminho LIQUID original inalterado

<!-- Imports a adicionar no export route (após L28-35 per PATTERNS.md): -->
import { ListObjectsV2Command } from "@aws-sdk/client-s3";
import { buildBrandStyleTag, injectBrandStyle } from "@/lib/brand/theme";
// Readable já importado (L28); archiver já importado (L30); prisma já importado (L33)
// GetObjectCommand já importado (usado no LIQUID path)

<!-- Pattern S3 ListObjectsV2 paginado (RESEARCH.md RQ-5): -->
const prefix = `workspaces/${lp.workspaceId}/project-templates/${lp.templateId}/dist/`;
let continuationToken: string | undefined;
const s3Keys: string[] = [];
do {
  const listResult = await s3Client.send(new ListObjectsV2Command({
    Bucket: process.env.S3_BUCKET!,
    Prefix: prefix,
    ContinuationToken: continuationToken,
  }));
  for (const obj of listResult.Contents ?? []) {
    if (obj.Key) s3Keys.push(obj.Key);
  }
  continuationToken = listResult.NextContinuationToken;
} while (continuationToken);

<!-- Pattern archiver loop VITE_SPA (RESEARCH.md RQ-5 + PATTERNS.md): -->
// archive = new ZipArchive (mesmo padrão do LIQUID path)
for (const s3Key of s3Keys) {
  const relativePath = s3Key.slice(prefix.length); // 'index.html', 'assets/main.abc.js'
  const s3Obj = await s3Client.send(new GetObjectCommand({ Bucket: ..., Key: s3Key }));
  if (relativePath === "index.html") {
    const html = await s3Obj.Body!.transformToString();
    const themedHtml = injectBrandStyle(html, buildBrandStyleTag(brand?.primaryColor));
    archive.append(Buffer.from(themedHtml, "utf-8"), { name: "index.html" });
  } else {
    const webStream = s3Obj.Body!.transformToWebStream();
    const nodeStream = Readable.fromWeb(webStream);
    archive.append(nodeStream, { name: relativePath });
  }
}
archive.finalize();

<!-- ZIP streaming response (reusar pattern LIQUID L278-291): -->
const webStream = Readable.toWeb(archive as unknown as Readable);
const slug = slugify(lp.name, { lower: true, strict: true }) || "landing-page";
return new NextResponse(webStream as ReadableStream, {
  headers: { "Content-Type": "application/zip", "Content-Disposition": `attachment; filename="${slug}.zip"` },
});

<!-- Edit page branch (PATTERNS.md): -->
// apps/web/src/app/w/[slug]/lps/[lpId]/edit/page.tsx
// INSERIR após if (!lp) redirect(...), ANTES de parse(lp.markupSnapshot):
if (lp.kind === "VITE_SPA") {
  return (
    <div className="px-8 py-6">
      <h1 className="text-2xl font-semibold text-gray-900 mb-6">Edit Landing Page</h1>
      <ViteSpaLpForm slug={slug} mode="edit" lpId={lp.id} lpName={lp.name} initialEntryRoute={lp.entryRoute ?? ""} />
    </div>
  );
}
// else: parse(lp.markupSnapshot) — caminho LIQUID inalterado
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch VITE_SPA na rota de export (ZIP dist/ tematizado, sem CSP)</name>
  <files>
    apps/web/src/app/api/lps/[lpId]/export/route.ts
  </files>
  <read_first>
    - apps/web/src/app/api/lps/[lpId]/export/route.ts — ler o arquivo completo para identificar: (a) todos os imports existentes (L28-35); (b) a inicialização do s3Client; (c) o bloco IDOR check + membership verification; (d) o guard 409 VITE_SPA (L193-200); (e) o padrão de `withTenantDb` para ler BrandConfig (ou acesso direto via prisma); (f) como `lp.workspaceId` é acessado; (g) o padrão `ZipArchive` e `archive.finalize()`; (h) a resposta streaming (L278-291); (i) a função `injectCsp` — confirmar que NÃO deve ser chamada para VITE_SPA
    - apps/web/src/lib/brand/theme.ts — confirmar que `buildBrandStyleTag` e `injectBrandStyle` estão exportados (Plan 01)
  </read_first>
  <action>
Em `apps/web/src/app/api/lps/[lpId]/export/route.ts`, fazer as seguintes mudanças:

**1. Adicionar imports:** Localizar o bloco de imports existente e adicionar:
- `ListObjectsV2Command` ao import de `"@aws-sdk/client-s3"` (verificar se `GetObjectCommand` já está importado; adicionar `ListObjectsV2Command` ao mesmo import)
- `import { buildBrandStyleTag, injectBrandStyle } from "@/lib/brand/theme";`
- `Readable` provavelmente já está importado de `"node:stream"` — confirmar e não duplicar

**2. Substituir o guard 409 VITE_SPA pelo branch de implementação:** Localizar o bloco:
```
if ((lp.kind ?? "LIQUID") === "VITE_SPA") {
  return NextResponse.json({ error: "VITE_SPA..." }, { status: 409 });
}
```
E substituir por um bloco `if ((lp.kind ?? "LIQUID") === "VITE_SPA") { ... return zipResponse; }` que:

a) Valida `lp.templateId` — se null, retornar 400 com mensagem `"VITE_SPA LP has no template reference."`

b) Busca BrandConfig: verificar como o route handler lê BrandConfig para o LIQUID path. Se usa `withTenantDb`, reusar esse padrão. Se tem `prisma` importado diretamente, usar `prisma.brandConfig.findFirst({ where: { workspaceId: lp.workspaceId } })`. Não importar `withTenantDb` se o handler não o usa — usar o padrão que já existe no arquivo.

c) Implementa ListObjectsV2 paginado com o prefix `workspaces/${lp.workspaceId}/project-templates/${lp.templateId}/dist/` e ContinuationToken loop (ver pattern na seção `<interfaces>`)

d) Cria novo ZipArchive (igual ao do LIQUID path — verificar a linha exata de instanciação para replicar)

e) Itera `s3Keys`:
- Para `relativePath === "index.html"`: `transformToString()` + `injectBrandStyle(html, buildBrandStyleTag(brand?.primaryColor))` + `archive.append(Buffer.from(themedHtml, 'utf-8'), { name: 'index.html' })`
- Para os demais: `transformToWebStream()` + `Readable.fromWeb(webStream)` + `archive.append(nodeStream, { name: relativePath })`

f) `archive.finalize()`

g) Resposta streaming usando o pattern `Readable.toWeb(archive as unknown as Readable)` — idêntico ao LIQUID path. Nomear o ZIP com `slugify(lp.name, ...)`.

h) **NÃO chamar `injectCsp()`** para VITE_SPA — per D-12, a CSP estrita `script-src 'none'` do export LIQUID não se aplica.

**3. Caminho LIQUID abaixo do branch:** NENHUMA mudança — o bloco existente abaixo do antigo guard (que será substituído pelo novo branch com return no final) continua intacto.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Export route não contém mais o `return NextResponse.json({ error: "VITE_SPA..." }, { status: 409 })` placeholder
    - `ListObjectsV2Command` importado de `"@aws-sdk/client-s3"` no arquivo
    - `buildBrandStyleTag` e `injectBrandStyle` importados de `"@/lib/brand/theme"`
    - Loop de paginação S3 usa `ContinuationToken` e `IsTruncated` (via `continuationToken = listResult.NextContinuationToken`)
    - Para `relativePath === 'index.html'`: `transformToString()` é chamado (não `transformToWebStream()`)
    - Para outros paths: `Readable.fromWeb()` é usado para stream → archiver
    - `injectCsp` NÃO é chamado no branch VITE_SPA (grep: `grep -n "injectCsp" apps/web/src/app/api/lps/\[lpId\]/export/route.ts` deve mostrar que injectCsp só aparece no bloco LIQUID)
    - O branch tem `return` explícito antes do código LIQUID — o LIQUID path só executa para kind=LIQUID
    - `pnpm --filter web tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>Export de LP VITE_SPA retorna ZIP da árvore dist/ com index.html tematizado e sem CSP estrita; LP LIQUID export inalterado.</done>
</task>

<task type="auto">
  <name>Task 2: Branch VITE_SPA na página de edição da LP</name>
  <files>
    apps/web/src/app/w/[slug]/lps/[lpId]/edit/page.tsx
  </files>
  <read_first>
    - apps/web/src/app/w/[slug]/lps/[lpId]/edit/page.tsx — ler o arquivo completo para identificar: (a) como o LP é carregado (getLpAction ou withTenantDb direto); (b) a linha exata de `parse(lp.markupSnapshot)` (RESEARCH Pitfall 4: deve ser protegida pelo branch); (c) o redirect para LP nulo; (d) o import list atual; (e) como `slug` e `lpId` são acessados a partir dos params
    - apps/web/src/components/lps/ViteSpaLpForm.tsx — confirmar que está disponível após Plan 02
  </read_first>
  <action>
Em `apps/web/src/app/w/[slug]/lps/[lpId]/edit/page.tsx`, fazer as seguintes mudanças:

**1. Adicionar import de ViteSpaLpForm:**
```typescript
import { ViteSpaLpForm } from "@/components/lps/ViteSpaLpForm";
```

**2. Adicionar o branch VITE_SPA:** Logo após o bloco `if (!lp) redirect(...)` e **antes de qualquer chamada a `parse(lp.markupSnapshot)`**, inserir:

```typescript
if (lp.kind === "VITE_SPA") {
  return (
    <div className="px-8 py-6">
      <h1 className="text-2xl font-semibold text-gray-900 mb-6">Edit Landing Page</h1>
      <ViteSpaLpForm
        slug={slug}
        mode="edit"
        lpId={lp.id}
        lpName={lp.name}
        initialEntryRoute={lp.entryRoute ?? ""}
      />
    </div>
  );
}
```

**3. O caminho LIQUID abaixo permanece completamente inalterado** — a chamada `parse(lp.markupSnapshot)`, o `ParsedSchemaValidator`, o `LpForm`, e qualquer outro código só são alcançados quando `lp.kind !== 'VITE_SPA'`.

**Verificação de acesso a `lp.kind` e `lp.entryRoute`:** Esses campos devem estar disponíveis no objeto LP carregado. Se a página usa `getLpAction`, verificar que o Plan 02 adicionou `kind` e `entryRoute` ao retorno. Se a página usa `withTenantDb` diretamente, os campos existem no model Prisma após Plan 01.

**Pitfall crítico (RESEARCH Pitfall 4):** O branch VITE_SPA DEVE aparecer antes de `parse(lp.markupSnapshot)`. Se aparecer depois, o `parse('')` (sentinel) rodará antes e pode causar redirect loop ou crash.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `edit/page.tsx` importa `ViteSpaLpForm` de `"@/components/lps/ViteSpaLpForm"`
    - O branch `if (lp.kind === "VITE_SPA")` aparece **antes** de qualquer chamada `parse(lp.markupSnapshot)` (verificar com `grep -n "parse\|VITE_SPA" apps/web/src/app/w/\[slug\]/lps/\[lpId\]/edit/page.tsx` — o `VITE_SPA` check deve ter número de linha menor que `parse`)
    - O `ViteSpaLpForm` no branch tem `mode="edit"`, `lpId`, `lpName`, e `initialEntryRoute` corretamente preenchidos
    - O caminho LIQUID (`parse(lp.markupSnapshot)`) permanece acessível apenas para LP com kind != VITE_SPA
    - `pnpm --filter web tsc --noEmit` sem erros
    - Abrir edit page de uma LP LIQUID não mostra ViteSpaLpForm (regressão zero)
  </acceptance_criteria>
  <done>Página de edição de LP VITE_SPA exibe ViteSpaLpForm com name e entryRoute pré-preenchidos; caminho LIQUID inalterado com nenhum parse() acidental no sentinel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Export route → S3 dist/ prefix | prefix construído de lp.workspaceId (campo de banco, workspace-scoped) + lp.templateId (campo de banco); não de input do cliente |
| Export route: IDOR check | IDOR membership check existente aplica-se igualmente ao branch VITE_SPA — sem bypass |
| Edit page: updateLpAction | workspaceId derivado de sessão em requireWorkspaceRole; entryRoute validado por Zod .max(128) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | S3 prefix para dist/ no export | mitigate | Prefix inclui workspaceId (campo banco, não input cliente); IDOR check verifica membership antes de acessar S3; ListObjectsV2 é scoped ao prefix — cross-tenant inacessível |
| T-08-04-02 | Tampering | index.html no ZIP: injeção via brand CSS vars | accept | `BrandConfig.primaryColor` validado como hex puro antes de ser salvo; triplet HSL produzido por hexToHslTriplet contém apenas dígitos, `%` e espaços; sem vetor de CSS injection no ZIP |
| T-08-04-03 | Denial of Service | ListObjectsV2 em loop: dist/ muito grande | accept | renova-turismo e LPs típicas têm dezenas de assets, não milhares; o loop ContinuationToken termina quando `IsTruncated = false`; arquivos JS/CSS são streamados direto para archiver sem carregar em memória |
| T-08-04-04 | Elevation of Privilege | Export branch VITE_SPA sem CSP | accept | VITE_SPA tem runtime JS próprio — `script-src 'none'` quebraria o SPA (D-12 locked decision). O ZIP é um arquivo estático local — o risco de XSS é do runtime do browser do usuário, não do PageForge. O usuário que exporta é o owner/admin do workspace |
| T-08-04-05 | Tampering | edit page: parse(sentinel markupSnapshot) prevenido | mitigate | Branch VITE_SPA inserido antes de `parse(lp.markupSnapshot)` — o caminho de parse nunca é alcançado para VITE_SPA; verificado por acceptance criterion de ordem de linhas |
</threat_model>

<verification>
1. Com LP VITE_SPA gerada, clicar em "Export ZIP" no menu do card → deve fazer download de um `.zip`.
2. Descompactar o ZIP; `index.html` deve conter `<style>:root{--primary:H S% L%;}</style>`.
3. Verificar que o ZIP não contém `<meta http-equiv="Content-Security-Policy" content="script-src 'none'">` (CSP do LIQUID).
4. Verificar que os assets JS/CSS do bundle estão presentes no ZIP (ex: `assets/index-*.js`).
5. Abrir edit page de uma LP VITE_SPA → deve mostrar o ViteSpaLpForm com fields pré-preenchidos.
6. Abrir edit page de uma LP LIQUID → deve mostrar o LpForm original (regressão zero).
7. `pnpm --filter web tsc --noEmit` — sem erros.
</verification>

<success_criteria>
- Export ZIP de LP VITE_SPA: árvore dist/ completa + index.html tematizado + sem CSP estrita
- LP LIQUID export inalterado
- Edit page VITE_SPA: ViteSpaLpForm com dados pré-preenchidos
- Edit page LIQUID: parse(markupSnapshot) + LpForm inalterado
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-04-SUMMARY.md`
</output>
