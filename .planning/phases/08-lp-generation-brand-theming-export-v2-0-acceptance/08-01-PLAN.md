---
phase: 08-lp-generation-brand-theming-export-v2-0-acceptance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/prisma/schema.prisma
  - apps/web/prisma/migrations/0007_vite_spa_lp_entry_route/migration.sql
  - apps/web/src/lib/db/tenant-db.ts
  - apps/web/src/lib/lps/schema.ts
  - apps/web/src/lib/brand/theme.ts
  - apps/web/src/lib/brand/theme.test.ts
autonomous: true
requirements:
  - PRJ-07
  - PRJ-08
  - PRJ-10

must_haves:
  truths:
    - "A migração `entry_route` é aplicada ao banco de dados; linhas LIQUID existentes não são afetadas (entry_route = NULL)"
    - "`hexToHslTriplet('#0d4080')` retorna `'213 90% 23%'` (formato shadcn H S% L%)"
    - "`buildBrandStyleTag('#0d4080')` retorna a string `<style>:root{--primary:213 90% 23%;}</style>`"
    - "`GenerateViteSpaLpSchema.parse({ templateId:'xxx', name:'Y' })` retorna `{ templateId, name, entryRoute: null }` sem erro"
    - "`TenantLpHelpers.create` aceita `entryRoute?: string | null` sem erro de TypeScript"
    - "Decisões CONTEXT.md cobertas neste plano — D-05: MVP injeta apenas `--primary` convertido para HSL triplet; D-08: reusar tabela LandingPage com coluna aditiva nullable `entry_route` (sentinelas markupSnapshot/schemaVersion/values para VITE_SPA)"
  artifacts:
    - path: "apps/web/prisma/migrations/0007_vite_spa_lp_entry_route/migration.sql"
      provides: "ALTER TABLE landing_page ADD COLUMN entry_route TEXT"
      contains: "ADD COLUMN"
    - path: "apps/web/src/lib/brand/theme.ts"
      provides: "hexToHslTriplet, buildBrandStyleTag, injectBrandStyle"
      exports: ["hexToHslTriplet", "buildBrandStyleTag", "injectBrandStyle"]
    - path: "apps/web/src/lib/lps/schema.ts"
      provides: "GenerateViteSpaLpSchema, GenerateViteSpaLpInput, UpdateLpSchema com entryRoute"
      exports: ["GenerateViteSpaLpSchema", "GenerateViteSpaLpInput"]
  key_links:
    - from: "apps/web/src/lib/brand/theme.ts"
      to: "renova-turismo-jornada-main/src/index.css"
      via: "hexToHslTriplet output matches --primary format"
      pattern: "\\d+ \\d+% \\d+%"
    - from: "apps/web/prisma/schema.prisma"
      to: "apps/web/prisma/migrations/0007_vite_spa_lp_entry_route/migration.sql"
      via: "entryRoute field"
      pattern: "entryRoute.*String\\?"
---

<objective>
Assentar a fundação de dados e utilitários para a geração de LPs VITE_SPA: migration aditiva da coluna `entry_route`, extensão das interfaces TypeScript do TenantClient, Zod schemas para VITE_SPA, e o helper de injeção de brand CSS vars (`lib/brand/theme.ts`). Nenhum trabalho de UI ou ação de usuário ainda — apenas contratos e migration.

Purpose: Todo plano subsequente (02–04) depende desta fundação: a migration deve estar aplicada antes de qualquer `create` VITE_SPA, e o helper de tema é consumido em três lugares diferentes (serve, preview, export).

Output: `migration.sql` + `migration_lock.toml` atualizado, `schema.prisma` com `entryRoute String?`, `TenantLpHelpers` com `entryRoute` nos tipos, `GenerateViteSpaLpSchema`/`UpdateLpSchema` estendido, e `lib/brand/theme.ts` com as três funções.
</objective>

<execution_context>
@/home/glow/.claude/get-shit-done/workflows/execute-plan.md
@/home/glow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/glow/Documentos/projetos/PageForge/.planning/PROJECT.md
@/home/glow/Documentos/projetos/PageForge/.planning/ROADMAP.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-CONTEXT.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-RESEARCH.md
@/home/glow/Documentos/projetos/PageForge/.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-PATTERNS.md

<interfaces>
<!-- Padrão da migration 0006 (analog exato) -->
<!-- apps/web/prisma/migrations/0006_kind_discriminator/migration.sql -->
<!-- Formato: ALTER TABLE "table_name" ADD COLUMN "col_name" TYPE [DEFAULT x] [CHECK x]; -->

<!-- TenantLpHelpers.create (tenant-db.ts L141-160) — interface atual: -->
create: (data: {
  templateId?: string;
  name: string;
  markupSnapshot: string;
  schemaVersion: number;
  values: Prisma.InputJsonValue;
  kind?: string;
}) => Promise<LandingPage>;

update: (
  id: string,
  data: {
    name?: string;
    values?: Prisma.InputJsonValue;
    markupSnapshot?: string;
    schemaVersion?: number;
    folderId?: string | null;
  }
) => Promise<LandingPage>;

<!-- GenerateLpSchema atual (schema.ts L18-36) — Zod pattern para replicar: -->
export const GenerateLpSchema = z.object({
  templateId: z.string().cuid("Invalid template ID"),
  name: z.string().min(1, "Landing page name is required").max(128, "...").trim(),
  values: z.record(z.string(), z.unknown()),
});
export type GenerateLpInput = z.infer<typeof GenerateLpSchema>;

<!-- UpdateLpSchema atual (schema.ts L43-64) — adicionar entryRoute: -->
export const UpdateLpSchema = z.object({
  id: z.string().cuid("Invalid LP ID"),
  name: z.string().min(1).max(128).trim().optional(),
  values: z.record(z.string(), z.unknown()).optional(),
  markupSnapshot: z.string().min(1).optional(),
  schemaVersion: z.number().int().positive().optional(),
  // entryRoute: NOVO — adicionar aqui
});

<!-- renova-turismo/src/index.css L17-19 — formato esperado das CSS vars: -->
:root {
  --primary: 213 90% 23%;   /* H S% L% — SEM "hsl(" wrapper */
  --primary-foreground: 0 0% 100%;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration aditiva entry_route + Prisma schema + TenantLpHelpers</name>
  <files>
    apps/web/prisma/schema.prisma
    apps/web/prisma/migrations/0007_vite_spa_lp_entry_route/migration.sql
    apps/web/src/lib/db/tenant-db.ts
  </files>
  <read_first>
    - apps/web/prisma/schema.prisma — ler o model LandingPage atual (linhas ~250-275) para posicionar o novo campo corretamente; verificar campos existentes (markupSnapshot, schemaVersion, values, kind)
    - apps/web/prisma/migrations/0006_kind_discriminator/migration.sql — ler o arquivo completo como template exato de cabeçalho e convenção SQL
    - apps/web/src/lib/db/tenant-db.ts — ler as interfaces TenantLpHelpers.create (L141-160) e TenantLpHelpers.update (L159-170) e a implementação do create/update abaixo dessas interfaces para adicionar o campo nos dois lugares (interface + implementação Prisma)
  </read_first>
  <action>
Criar o diretório `apps/web/prisma/migrations/0007_vite_spa_lp_entry_route/` e o arquivo `migration.sql` com o seguinte conteúdo (replicar o estilo de cabeçalho do 0006, sem CHECK constraint nem DEFAULT — NULL é a semântica correta):

```
-- Migration: 0007_vite_spa_lp_entry_route
-- ADDITIVE: adds nullable entry_route column to landing_page.
-- VITE_SPA rows: NULL = root '/'; non-null = '/grecia', '/turquia', etc.
-- LIQUID rows get NULL automatically (correct — unused column).
-- No DEFAULT needed — NULL is the correct default.
-- No CHECK constraint — free-form path; Zod validates at action boundary.
-- No RLS change needed — existing policy on landing_page covers all columns.
ALTER TABLE "landing_page"
  ADD COLUMN "entry_route" TEXT;
```

Em `apps/web/prisma/schema.prisma`, no model `LandingPage`, adicionar o campo `entryRoute String?` logo após o campo `kind` (manter convenção de comentário inline explicando o uso VITE_SPA — seguir padrão dos campos existentes).

Em `apps/web/src/lib/db/tenant-db.ts`:
- Na interface `TenantLpHelpers.create`, adicionar `entryRoute?: string | null;` ao tipo do parâmetro `data`.
- Na interface `TenantLpHelpers.update`, adicionar `entryRoute?: string | null;` ao tipo do parâmetro `data`.
- Nas implementações (`withTenantDb` internals), passar `entryRoute` via spread ao `prisma.landingPage.create(data: {..., entryRoute: data.entryRoute ?? null})` e ao `prisma.landingPage.update({ data: {..., ...(data.entryRoute !== undefined ? { entryRoute: data.entryRoute } : {})} })`.

**[BLOCKING] Aplicar migration:** Após criar o arquivo SQL, executar dentro de `apps/web/`:
```
npx prisma migrate deploy
```
Se o ambiente for de desenvolvimento e `prisma migrate deploy` reclamar de estado inconsistente, usar `npx prisma migrate dev --name vite_spa_lp_entry_route` (cria o migration.sql automaticamente se não existir, ou detecta o existente).

Executar `npx prisma generate` após a migration para regenerar o client.
  </action>
  <verify>
    <automated>
      cd /home/glow/Documentos/projetos/PageForge/apps/web && npx prisma db execute --stdin &lt;&lt;&lt; "SELECT column_name FROM information_schema.columns WHERE table_name='landing_page' AND column_name='entry_route';"
    </automated>
    Confirmar que a coluna `entry_route` existe no banco. Também verificar: `grep -n "entryRoute" apps/web/prisma/schema.prisma | grep "String?" | wc -l` deve retornar 1.
  </verify>
  <acceptance_criteria>
    - `apps/web/prisma/migrations/0007_vite_spa_lp_entry_route/migration.sql` existe e contém `ADD COLUMN "entry_route" TEXT`
    - `apps/web/prisma/schema.prisma` contém `entryRoute     String?` no model `LandingPage`
    - `TenantLpHelpers.create` e `.update` aceitam `entryRoute?: string | null` sem erro de TypeScript (`pnpm --filter web tsc --noEmit` não reporta erro neste arquivo)
    - A coluna `entry_route` existe no banco de dados ao executar a query de verificação
    - Nenhuma linha LIQUID existente foi afetada (todas com `entry_route = NULL`)
    - `npx prisma generate` executado sem erros
  </acceptance_criteria>
  <done>Migration aplicada, schema Prisma atualizado com `entryRoute String?`, TenantLpHelpers com o campo nos tipos e nas implementações, e banco com a nova coluna.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `lib/brand/theme.ts` + Zod schemas VITE_SPA</name>
  <files>
    apps/web/src/lib/brand/theme.ts
    apps/web/src/lib/lps/schema.ts
    apps/web/src/lib/brand/theme.test.ts
  </files>
  <read_first>
    - apps/web/src/lib/brand/schema.ts — ler para confirmar o regex de validação do primaryColor (`/^#[0-9a-fA-F]{6}$/`) e o padrão de importação do módulo brand
    - apps/web/src/lib/lps/schema.ts — ler o arquivo completo para ver o padrão Zod existente (GenerateLpSchema, UpdateLpSchema) e posicionar os novos exports ao final sem quebrar os existentes
    - apps/web/src/lib/serve/serve-vite-spa.ts — ler as primeiras 10 linhas para ver o padrão de arquivo server-only puro (sem "use server", sem imports externos)
    - renova-turismo-jornada-main/src/index.css — ler as linhas 1-25 para confirmar o formato `--primary: H S% L%` (ex: `213 90% 23%`)
  </read_first>
  <behavior>
    - `hexToHslTriplet('#0d4080')` → `'213 90% 23%'` (valor verificado contra renova-turismo index.css)
    - `hexToHslTriplet('#ffffff')` → `'0 0% 100%'`
    - `hexToHslTriplet('#000000')` → `'0 0% 0%'`
    - `buildBrandStyleTag('#0d4080')` → `'<style>:root{--primary:213 90% 23%;}</style>'`
    - `buildBrandStyleTag(null)` → `''` (sem injeção quando não há cor)
    - `buildBrandStyleTag(undefined)` → `''`
    - `injectBrandStyle('<html><head></head><body/></html>', '<style>:root{--primary:0 0% 0%;}</style>')` → HTML com `<style>` antes de `</head>`
    - `injectBrandStyle('<html><body/></html>', '<style>x</style>')` → `'<style>x</style>\n<html><body/></html>'` (fallback)
    - `injectBrandStyle('<html>', '')` → `'<html>'` (sem mudança se styleTag vazio)
    - `GenerateViteSpaLpSchema.parse({ templateId: 'cuid123', name: 'Test LP' })` → `{ templateId: 'cuid123', name: 'Test LP', entryRoute: null }` (entryRoute ausente → null)
    - `GenerateViteSpaLpSchema.parse({ templateId: 'cuid123', name: 'Test LP', entryRoute: '/grecia' })` → `{ ..., entryRoute: '/grecia' }`
    - `GenerateViteSpaLpSchema.parse({ templateId: 'cuid123', name: 'Test LP', entryRoute: '' })` → `{ ..., entryRoute: null }` (string vazia → null via transform)
    - `GenerateViteSpaLpSchema.parse({ templateId: 'cuid123', name: 'Test LP', entryRoute: 'grecia' })` → `{ ..., entryRoute: '/grecia' }` (sem barra → barra inicial adicionada)
    - `UpdateLpSchema.parse({ id: 'cuid123', entryRoute: '' })` → `{ id: 'cuid123', entryRoute: null }` (transform também no UpdateLpSchema)
  </behavior>
  <action>
Criar `apps/web/src/lib/brand/theme.ts` como módulo server-only puro (sem diretiva `"use server"`, sem imports externos). O arquivo exporta três funções:

- `hexToHslTriplet(hex: string): string` — algoritmo puro de conversão hex→HSL usando o algoritmo standard de 25 linhas documentado em RESEARCH.md RQ-1. Input: string `#RRGGBB` de 6 dígitos (já validado em BrandConfig). Output: `"H S% L%"` (ex: `"213 90% 23%"`). Usar `Math.round` para H, S e L. Nenhuma import necessária.

- `buildBrandStyleTag(primaryColor: string | null | undefined): string` — retorna `""` se `primaryColor` é falsy. Caso contrário, chama `hexToHslTriplet(primaryColor)` e retorna a string `<style>:root{--primary:${triplet};}</style>`.

- `injectBrandStyle(html: string, styleTag: string): string` — se `styleTag` é vazio, retorna `html` sem alteração. Se `html` contém `</head>`, substitui a primeira ocorrência de `</head>` por `${styleTag}\n</head>` e retorna. Caso contrário, prepend: `${styleTag}\n${html}`.

Para os Zod schemas em `apps/web/src/lib/lps/schema.ts`, adicionar ao final do arquivo (após os exports existentes sem remover nada):

- `GenerateViteSpaLpSchema` — z.object com `templateId: z.string().cuid(...)`, `name: z.string().min(1,...).max(128,...).trim()`, e `entryRoute: z.string().max(128).optional().or(z.literal("")).transform((v) => v ? (v.startsWith("/") ? v : "/" + v) : null)`. A transform normaliza a barra inicial: valor falsy → `null` (root `/`); valor sem `/` prefixo (ex. `grecia`) → `/grecia`, evitando URL malformada `${serveOrigin}grecia` no iframe de preview. Usar a mesma mensagem de erro de `templateId` do GenerateLpSchema existente.
- `export type GenerateViteSpaLpInput = z.infer<typeof GenerateViteSpaLpSchema>`

Estender `UpdateLpSchema` (já existente) adicionando o campo `entryRoute: z.string().max(128).optional().or(z.literal("")).transform((v) => v ? (v.startsWith("/") ? v : "/" + v) : null)` (mesma normalização de barra inicial do GenerateViteSpaLpSchema) — se UpdateLpSchema usa z.object, adicionar ao objeto. Verificar se é seguro adicionar sem quebrar chamadas existentes (campo é opcional).

Criar `apps/web/src/lib/brand/theme.test.ts` com testes Vitest que cubram todos os casos do bloco `<behavior>` acima.
  </action>
  <verify>
    <automated>cd /home/glow/Documentos/projetos/PageForge && pnpm --filter web vitest run src/lib/brand/theme.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/brand/theme.ts` existe sem diretiva `"use server"` e sem imports externos
    - Todos os testes em `theme.test.ts` passam (incluindo os casos de null/undefined e o valor de referência `#0d4080 → '213 90% 23%'`)
    - `GenerateViteSpaLpSchema` exportado em `schema.ts` sem quebrar `GenerateLpSchema` ou `UpdateLpSchema` existentes
    - `GenerateViteSpaLpSchema.parse({ templateId, name })` retorna `entryRoute: null` (não `undefined`)
    - `UpdateLpSchema` inclui campo `entryRoute` opcional sem quebrar testes/uso existente
    - `pnpm --filter web tsc --noEmit` não reporta erro nos arquivos modificados
  </acceptance_criteria>
  <done>Helper de tema com três funções exportadas + testes unitários passando + schemas Zod VITE_SPA adicionados sem quebrar os existentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BrandConfig.primaryColor → hexToHslTriplet | primaryColor é input salvo pelo usuário (admin/owner); validado como `/^#[0-9a-fA-F]{6}$/` em `brand/schema.ts` antes de persistir — portanto o input para hexToHslTriplet é sempre safe |
| TenantLpHelpers.create entryRoute | entryRoute vem de input do cliente via GenerateViteSpaLpSchema; Zod valida `.max(128)` e normaliza vazio para null |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Injection | `buildBrandStyleTag` / CSS | accept | primaryColor validado como hex puro (`/^#[0-9a-fA-F]{6}$/`) em BrandConfig.save antes de atingir o helper; triplet resultante contém apenas dígitos, `%` e espaços — sem vetor CSS injection |
| T-08-01-02 | Injection | `entryRoute` coluna | mitigate | `GenerateViteSpaLpSchema` valida `.max(128)` e normaliza vazio → null; o campo é tratado como path string no código, nunca concatenado a SQL nem usado em context de HTML direto sem escaping |
| T-08-01-03 | Information Disclosure | migration aditiva | accept | ALTER TABLE ADD COLUMN é operação DDL padrão; nenhuma dado de tenant é exposto; null default não vaza informação |
</threat_model>

<verification>
1. `cd apps/web && npx prisma db execute --stdin <<< "SELECT column_name FROM information_schema.columns WHERE table_name='landing_page' AND column_name='entry_route';"` — deve retornar 1 linha.
2. `pnpm --filter web vitest run src/lib/brand/theme.test.ts` — todos os testes passam.
3. `pnpm --filter web tsc --noEmit` — sem erros nos arquivos modificados.
4. `grep -c "entryRoute" apps/web/src/lib/db/tenant-db.ts` deve ser >= 4 (interface create, interface update, implementação create, implementação update).
</verification>

<success_criteria>
- Migration 0007 aplicada ao banco (coluna `entry_route TEXT NULL` existe em `landing_page`)
- `theme.ts` exporta as três funções; testes unitários todos verdes
- `GenerateViteSpaLpSchema` e `UpdateLpSchema` estendido disponíveis para importação
- `TenantLpHelpers` tipado com `entryRoute` sem regressão de TypeScript
- Zero mudanças no caminho LIQUID
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-lp-generation-brand-theming-export-v2-0-acceptance/08-01-SUMMARY.md`
</output>
